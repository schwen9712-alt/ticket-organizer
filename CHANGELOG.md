## 2026-09-01 — 🚑 类型词前置/后置真歧义：报文模式锁定消歧 + 🧪 解析诊断 · v26.09.01-DE

用户三图铁证（徽章 DD、重新解析 ¥95040→¥95040、分档运价对而售价 21120×5）。逐行插桩定位：预处理吞掉 `=====` 分隔线后，`婴儿` 的上一行成了 `TOTAL CNY 24596`——DB 版"后瞻优先"规则把它误判为上块后置词走护照路径，婴儿块未定型、TOTAL 2597 走通用分支、无分档铺价；原样本本地通过**全靠 SEL…IN 行兜底**，线上旧单原文（旧版预处理产物）无 SEL 即翻车。前置（成人↓FARE）与后置（TOTAL↓大人）在分隔线被吞后形态完全相同——真歧义。**消歧改为报文模式锁定**：第一个独行类型词出现在任何 TOTAL 之前→前置模式；之后→后置模式；一经确立全篇锁定。四变体（原样/无 SEL/无分隔线/无空行）全通，H（后置）G（护照前置）K（大小单字）回归不打架。新增「🧪 解析诊断」菜单：复制该单存储字段+当前解析结果+原文 rawPnr，排障铁证一键交付。

**测试**：AB3（无 SEL 无分隔线仍分档）parse 76 断言 ✓ + e2e 10 ✓ + 四审计。

**改动位置**：_fareTypeMode 状态与 dtag 消歧；diagOrder；菜单一项。

**回滚**：.backups/ 上一版。**上线自检**：认徽章 DE → 该单 ⋯ → 🔄 重新解析 → 售价 ¥71,603；若仍不对 → 🧪 解析诊断 → 粘贴发我。
---
## 2026-09-01 — 🔄 旧单按最新规则重新解析（解析器升级不再留死账） · v26.09.01-DD

用户报"解析依旧不对"。高概率场景：DC 修的是解析与建单，**已入库旧单的数据是录入时固化的**（basePrice=21120、paxPrices 空），新版本不会自动重算——上一版交付时未说明，本版根治。实现：卡片 ⋯ 菜单新增「🔄 重新解析」——按当前解析器重跑该单存储的原文（rawPnr），纯合并函数 _reparseInto 只刷新解析派生字段（航段、乘客资料按归一名合并、分档价、fareByType、basePrice、折扣（手改过的不动）、售价重算），**人工字段一律不动**（PNR/票号/卡主/状态/代理/表/备注）；toast 报告 ¥前→¥后 与乘客数变化。早期无原文的单友好提示。

**测试**：e2e 新增 E9/E10——**复现线上错误现状**（旧单 21120×90%×5=95,040）→ 重解析 → basePrice 24596、五份分档、售价 71,603、PNR 保留、婴儿生日补齐；全组 parse 75 + e2e 10 断言 ✓ + 四审计。

**改动位置**：_reparseInto/reparseOrder；卡片菜单一项。

**回滚**：.backups/ 上一版。**上线自检**：认徽章 DD → 打开那张错单 → ⋯ → 🔄 重新解析 → 售价变 ¥71,603、PRICE 行出分档明细。
---
## 2026-09-01 — 🚑 售价错用 EQUIV 裸票面 + 端到端测试制度 · v26.09.01-DC

用户截图：3 成人+2 婴儿单显示 `21120 × 90% × 5`（分档运价行却正确显示成人 ¥24,596/婴儿 ¥2,597）。追查：21120 是 FARE 行 `EQUIV CNY` 备用值——注释写着"无 TOTAL 时 fallback"，代码却**无条件覆盖**已由 TOTAL 写入的成人档（`=` 而非"仅空时写"），旧版本路径下售价被顶成裸票面。修：EQUIV 三档位均改"该档尚无值才写"。连带抓出 **CV 婴儿全价漏改的第 7 处**：建单段"规则重匹配"路径 `sum × 折扣` 未豁免婴儿。**制度性补强（用户要求：所有任务自己核对到上线状态）**：新建 `tests/e2e_test.js`——用页面**真实建单代码切片**（锚定 newOrder 建单段 … rawPnr）+ 真函数（newOrder/_infantSum/matchDiscountRule/computeFinalPrice）从原文一路跑到**卡片最终价**，覆盖婴儿单/儿童单/前置定型单三样本 8 断言（basePrice 非 21120、五份分档落库、默认 90、rmb 与卡片 fp 公式一致、儿童不豁免、婴儿豁免）。发货链自此双门槛：parse_test + e2e_test。

**测试**：parse 75 断言 ✓ + e2e 8 断言 ✓ + 四审计。

**改动位置**：EQUIV 消费三行；建单段 fullCtxRule 路径；tests/e2e_test.js 新建。

**回滚**：.backups/ 上一版。**上线自检**：重贴该单——PRICE 行应为分档明细（24596×90%×3 + 2597 婴儿全价×2）、收入 ¥71,603。
---
## 2026-09-01 — 💰 运价前置类型定型 + 👶 婴儿 48 个月口径（用户业务规则） · v26.09.01-DB

样本：KRW `FARE…EQUIV CNY` 运价块，**类型词"成人/婴儿"在块前**（H 样本是块后）。病灶：独行类型词被"护照前置标签"分支抢走，两个 TOTAL 无定型后者覆盖前者（rmb 错成婴儿价 2597、无分档）。修：独行类型词**前瞻 3 行**——下文为 FARE/SEL/TAX/TOTAL 即定型运价（_lastFareType）；**歧义消解后瞻优先**——上一非空行是 TOTAL（后置词属上块，分隔线被清洗成换行时上下皆运价块）不做前置（回归 H 组实证抓出）；`SEL PFK000VP IN NVB…` 票价基础行静默且 IN/CH 作定型信号；主循环补外置行索引 _li。**业务口径纠正**：婴儿＝起飞日未满 **48 个月**（非国际通行 24 个月），六处年龄判定统一 <2→<4（解析兜底/组装铺价/排序/拆单显示/徽章/结算豁免），_isInfantPax 改按首段起飞日算龄，无段日期今天兜底。连锁修复：解析区 getAgeAtFlight 无年份段日期原按"当前年"（9 月看 03JAN 算成过去）改"最近未来年"（回归 V 组抓出：4 岁零 12 天的孩子曾少算一岁）；婴儿档无价时铺价回落**儿童价**再成人价。该单：五客、成人 ¥24,596/人、婴儿 ¥2,597，两个 2025 年生的自动配婴儿价，零未识别。

**测试**：AB 组 3 断言（前置定型五客分档/48 月口径 3 岁 8 月配婴儿价）+ AC 组 2 断言 全组 75 断言 ✓（H/G/K 组回归证明前置/后置定型互不打架）+ 四审计。

**改动位置**：dtag 前后瞻；SEL 行；_li 索引；六处年龄阈值；_isInfantPax 起飞日；getAgeAtFlight 年份推断；铺价兜底。

**回滚**：.backups/ 上一版。**上线自检**：贴本样本——成人 ¥24,596×3 + 婴儿 ¥2,597×2，两娃标婴儿。
---
## 2026-09-01 — 解析器：SSR DOCS 六位数字生日变体 + Sabre 系统行静默 · v26.09.01-DA

用户报"识别乘客出问题"（乘客 0 人）。探针：段与金额均通（`B1`/`R1` 舱位粘座位数已容忍、`01 RLX4ISBU 10230 CNY` 运价已入），真乘客 `SSR DOCS UA HK1 P/USA/A39321637/USA/761015/M/CHANG/WEYLU` 是**罕见变体**——生日六位数字 YYMMDD（非 DDMMMYY）、无效期、无 P 尾——既有 SSR 正则拒绝致落未识别。实现：既有匹配失败后二次尝试六位变体（YYMMDD→DDMMMYY：761015→15OCT76，效期若有同法），同名归一合并；`FSI/UA/PREC` 与 `S UA  2019B01OCT…` Sabre 可用性系统行静默（此前落未识别但未被误抓成乘客——名字过滤守住了）。

**测试**：AA 组 3 断言（分单不裂/六位生日乘客/段金额清零）全组 72 断言 ✓ + 四审计。

**改动位置**：SSR 六位变体分支；系统行静默。

**回滚**：.backups/ 上一版。**上线自检**：贴本样本——CHANG/WEYLU · 15OCT76 · 男，两段含 +2 跨天，¥10,230，零未识别。
---
## 2026-09-01 — 🔬 全局对抗测试 + 🎨 长时间工作舒适层 · v26.09.01-CZ

用户令：全局对抗测试、清 bug、UI 舒适化。**对抗矩阵 24 项真跑全绿**（解析边界：空/纯噪音/仅分隔线/三种分隔混用/重复粘贴幂等/小写码/超大与零负金额/跨年段序/全角数字/5000 行性能；结算守卫：长度不一致/null 对象/折扣 null/表 G/报价 pax 0·负 usd·折扣 100；舱位：小写航司/空对象/无 cls；制裁：小写码/空段）；**UI 静态审计**（内联事件死函数引用/重复 id/死 id 引用）三项全零。已知技术债"调试日志声明未写入"经查产码零引用、不存在。**舒适层（人因）**：①字号地板——203 处 10px 微文本提至 11px（最小可读性）；②三级文字对比 4.6→5.3:1；③速览列表斑马纹+hover；④行距 1.55；⑤按钮热区 ≥28/32px、触屏 34/38px；⑥输入焦点环；⑦弹窗遮罩模糊降噪；⑧细滚动条；⑨页面底色纯白 #FFFFFF → 微暖 #FAFAF9（暗色模式不受影响）。纯 CSS 覆盖层可整段回滚。

**测试**：Z 组 5 断言（矩阵固化）全组 69 断言 ✓ + 四审计 + CSS 花括号配平。

**改动位置**：10px→11px 源替换；舒适层 CSS 块。

**回滚**：.backups/ 上一版。**上线自检**：整体观感字略大一号、列表隔行微灰、点输入框有蓝色焦点环。
---
## 2026-09-01 — 💳 CHASE TRAVEL 模块引入核价（含积分口径） · v26.09.01-CY

用户指令：Chase Travel 模块引入核价。实现：①Chase 面板「各单刷卡金额」列表每行的 **$ 金额可点**（stopPropagation 不触发行跳转，照卡主 chip 模式）直开 ⚖ 核价面板，自动带单上人民币报价/人数/折扣；②核价面板 **Chase 语境专属行**——输入 Chase Travel 页面显示的积分 pts 与兑换率 ¢/pt（默认 1.5 Sapphire Reserve；Preferred 可改 1.25，改动记入 settings.chaseCpp 记忆），**自动折算为每人美国运价填入比价**（pts 为整单，除以人数）；面板标题 Chase 语境加 💳 标识。核价→报价复制链路（CP）对 Chase 单同样可用。

**测试**：pts 折算 2 断言 + parse 全组 64 断言 ✓ + 四审计。

**改动位置**：Chase 刷卡金额行；面板 chase 行；_fareChasePts；标题。

**回滚**：.backups/ 上一版。**上线自检**：CHASE TRAVEL 页点任一行 $ → 面板带积分行 → 填 pts → 美国运价自动折算 → 差价即出。
---
## 2026-09-01 — 🔬 主动交叉质检：对抗矩阵抓出 3 处翻车并修复 · v26.09.01-CX

用户要求自查近期改动的交叉风险。构造 8 项对抗样本真链跑（归一化去重×称谓/反向空格/连字符/同姓不同名/无斜杠；大小标签×"小计"；粘连拆段×代码共享 *；婴儿判定×dob），抓出 3 处真翻车：①**连字符名**（`JIANG/JIAN-QING` 被名单正则截成 JIAN 致不合并）→ nameRe 名字字符集加连字符；②**无斜杠手打名**（`JIANGJIANQING` 与护照 `JIANG/JIANQING` 不合并）→ _sameName 二级比对（任一方无斜杠时剥斜杠比对）+ 三处合并点显示名升级为证件名；③**代码共享 `*` 段粘连不拆**（断行规则与清洗门槛正则均不认 `*`）→ 双处加 `\*?`。顺手：compactDoc（证件号在前形态）此前漏捕效期组，补捕获与合并（CW 记档缺口闭环）；_isInfantPax 加 dob 未满 2 岁兜底（fareByType 无婴儿档时仍能豁免折扣）。通过项（不需改）：称谓尾/反向空格/同姓不同名不误合并/"小计"不当儿童（负向前瞻生效）。

**对策记档（不改码，需你拍板）**：历史单的折扣字段是入库时固化值——CV 改革只对新单默认 90，旧表 A 单若字段为 100 仍按旧口径结算（"改革从今起"语义）；若要旧单一并 9 折，可做一次性批量迁移工具。

**测试**：Y 组 5 断言（矩阵固化）全组 64 断言 ✓ + 四审计。

**改动位置**：nameRe；_sameName；三合并点；compactDoc；断行+门槛；_isInfantPax。

**回滚**：.backups/ 上一版。**上线自检**：贴带连字符名或无斜杠名的单——乘客不重复且显示证件名。
---
## 2026-09-01 — 解析器：乘客名归一化去重（名单/护照写法不一致致重复计人） · v26.09.01-CW

用户纠正：名单 `1.JIANG/JIAN QING 2.YAN/XIAO FENG` 与护照 `.../JIANG/JIANQING/P1`（名字空格有无不一致）被识别成 4 人，实为 2 人。根因：全部 21 处乘客去重点用 `p.name === name` 精确比对。修正：新建 `_nameKey`/`_sameName`（去空格·点·连字符、多斜杠归一后比对），**21 处去重点同源升级**为归一化比对——名单版先入列保留显示名，护照行按归一键命中后**合并资料**（生日/性别/效期/类型）不再另立。解析区首函数变为 _nameKey，测试抽取锚同步更新。记档（不扩刀）：本样本护照行为"无 P 前缀且无前导国籍"变体，所走分支未提取效期组，效期字段暂缺，低优。

**测试**：X 组 3 断言（分单不裂/两人合并含资料/两段+清零）全组 59 断言 ✓ + 四审计。

**改动位置**：_nameKey/_sameName；21 处去重比对。

**回滚**：.backups/ 上一版。**上线自检**：贴本样本——乘客区恰 2 人，各自带生日性别。
---
## 2026-08-31 — 💰 结算改革：表 A/Y 统一默认 9 折 + 婴儿全价 · v26.08.31-CV

用户改革令两条：①**取消表 Y 特殊性**——原引擎中表 Y 强制 override 90 的专属逻辑删除，改为表 A/表 Y **一律默认 90**（discountRules 细则仍优先可覆盖代理特例；表 G 卡主渠道不参与默认折扣返 null 走原路）；②**婴儿票无折扣**——新建 _isInfantPax（forcedType 直标或价签匹配 fareByType.infant 双信号）与 _infantSum，**六处分型结算点**统一改造：`(总价 - 婴儿价) × 折扣 + 婴儿价`，婴儿按全运价向代理收取；单价×人头的四处旧路径无分型信息维持原状（婴儿单必有分型价不走那里）。内部复制/报表 PRICE 明细行婴儿项显式标注 `2176(婴儿全价)` 与成人 `× 90%` 并列，代理一眼看懂。

**测试**：W 组 6 断言（婴儿合计/结算公式 64508.2 实证/表A默认/表Y默认/表G豁免/价签匹配无标婴儿）+ parse 全组 56 断言 ✓ + 四审计。

**改动位置**：_isInfantPax/_infantSum；折扣引擎；六结算点；两明细模板。

**回滚**：.backups/ 上一版。**上线自检**：开含婴儿的单——收入=（成人合计×90%）+婴儿全价；PRICE 明细行婴儿项显示"(婴儿全价)"。
---
## 2026-08-31 — 解析器：大/小单字类型标签 + AA 代码共享段全通 · v26.08.31-CU

新样本（AA 代码共享 `*AA8439`+`OP-JL60` 承运尾巴+FARE JPY/EQUIV CNY 外币运价块+`大`/`小`单字类型标签）。探针：段（* 前缀既有容忍）/三客/`第2单 商务9折` 头行（舱位+折扣）/FARE JPY 静默全部既通——唯"小"被歪路误标 infant 且"大/小"独行实为无人消费（hook 追踪实证：四行全落未识别，单字被报告过滤器隐藏）。修正：**三处词表统一收编单字**——fareTypeMatch 提取（垫底交替+汉字负向前瞻防误吃"大人"前缀）、inline/normKind 两处归一（大→ADULT、小→CHILD）、TOTAL 行 inline 词表同扩。修后：fbt {adult:39234, child:30024}、三人铺价 [39234,30024,30024]（两娃 age 兜底入 child 档）、¥39,234 契约每人、商务舱 9 折、零未识别。

**测试**：V 组 3 断言（分单不裂/代码共享段+头行/大小定型铺价清零）全组 56 断言 ✓ + 四审计。

**改动位置**：四处词表。

**回滚**：.backups/ 上一版。**上线自检**：贴本样本——AA8439 C 舱、商务 9 折、成人 ¥39,234/儿童 ¥30,024、三人价齐。
---
## 2026-08-29 — ⛔ 受制裁航点录单强阻拦（CCS/古巴/俄罗斯等） · v26.08.29-CT

用户指令：CCS 这类美国敌对/制裁辖区机场无法出票，录单时强阻拦。实现：①**制裁航点表** _SANCTIONED_APTS——七辖区主要机场（俄罗斯 20 港/古巴 7/委内瑞拉 6 含 CCS/伊朗 7/朝鲜 FNJ/叙利亚 3/白俄 MSQ），码→国家名；②检测纯函数 _sanctionHits（segs 的 from/to 扫描去重）；③**双入库点硬拦**——录单主入口命中即 alert 阻断（⛔ 无法录入+航点明细+"请直接回复代理无法承接"，强阻拦场景为 alert 降级立法之豁免例）不入库；代理门户合并入口命中单跳过+toast 计数拒收。与既有 OFAC 乘客姓名比对互补（人名一条线、航点一条线）。

**测试**：U 组 3 断言（CCS 命中/多辖区去重/正常单零命中）+ parse 全组 53 断言 ✓ + 四审计。

**改动位置**：_SANCTIONED_APTS/_sanctionHits；两入库点。

**回滚**：.backups/ 上一版。**上线自检**：贴一单含 CCS 的行程尝试保存——应弹 ⛔ 阻断且列表不出现该单。
---
## 2026-08-29 — 模板舱位实时推导：出模板一律按航司舱位表现算 · v26.08.29-CS

用户指令：出模板时舱位必须按舱位对应表。病根：存量单的 o.cabin 是**解析时代的快照**——CQ 之前的错标（DL P→头等等）已固化在字段里，模板直读它就永久带病；且段行渲染中存量词优先级还压着实时推导。实现 `_liveCabinZh(o)` **实时推导函数**（按航司舱位表对全部段 cls 现算取最高舱，存量 o.cabin 仅作无段兜底），五点统一接入：①②两处模板头（表-代理-舱位 与内部复制头）；③段行 cabinTag 反转优先级（段级实时 > 订单词）；④订单词来源本身换实时；⑤卡片舱位 tag。效果：**同一张历史错标单，不改数据、出模板即正确**。发货链补记：T 组断言两连炸（朴素切割裹入常量致 return 落顶层 → 大括号配平抽取；eval 内 const 不泄漏 → globalThis 赋值），抽取法入档。

**测试**：T 组 3 断言（DL P 存量"头等舱"被实时推导覆盖为超经 / UA P 实时商务 / 无段兜底存量）+ parse 全组 53 断言 ✓ + 四审计。

**改动位置**：_liveCabinZh；五接入点。

**回滚**：.backups/ 上一版。**上线自检**：找一单 CQ 前录的 DL P 舱 → 复制代理商报表 → 头部舱位应显超经非头等。
---
## 2026-08-29 — 解析器：中文城市航段格式 · v26.08.29-CR

新形态：`1. UA134  12月20日  纽约纽瓦克—苏黎世 1825 0820+1`（序号+航班号+中文月日+**中文城市对**+四位时刻）。实现：①专用分支——全角/半角破折连接、城市段容忍内部空格（"纽约 纽瓦克"）、四位时刻+跨天原样存（_t2min 兼容）、12月20日→20DEC；②**城市查表最长词优先**——去空格后先精确再按键长降序包含匹配（"纽约纽瓦克"命中 EWR 而非"纽约"的 JFK）；③城市表补欧洲三城（苏黎世 ZRH/日内瓦 GVA/维也纳 VIE）；查不到城市的行安全落未识别不误拼。TOTAL USD 既有分支消化（$25,243.15 → ¥181,750.68）。另记：容器重置一次，五文件包+自包含测试解包即满血恢复（基线 50 绿验证后动刀）。

**测试**：J 组 3 断言（分单不裂/两段全字段含跨天/金额清零）全组 53 断言 ✓ + 四审计。

**改动位置**：中文城市段分支；城市表三城。

**回滚**：.backups/ 上一版。**上线自检**：贴本样本——UA134 EWR→ZRH 20DEC（0820+1）+ UA9748 ZRH→EWR 30DEC、¥181,751。
---
## 2026-08-27 — 舱位航司感知修正：DL P/A=超经、UA P=商务（用户纠正） · v26.08.27-CQ

用户纠正两组舱位错标：DL 的 P/A 应为 Premium Select 超经却显头等；UA 的 P 应为 Polaris 商务却显经济。三层病根三层修：①**内置 DL 表**（DEFAULT_SETTINGS 与 STATE 双份同修）P 误标 First → 归 Premium（P/A/G 三舱同档）；②**settings 旧表压新表**——fareClassByAirline 首启深拷贝入 settings 并同步 Firebase，内置修正会被存量旧表永久覆盖——照 UA T 迁移先例加 **DL P 热迁移**（旧值 First 启动时自动改 Premium）；③**粗表函数航司化**——`_cabinFullName` 原为不分航司三桶粗表（F/A/P 一律 FIRST），改航司感知三级链（settings 航司表 → 内置航司表 → 通用 classMap → 粗表末级兜底），段渲染调用点补传订单航司（缺失时从航班号前两位推）。

**测试**：R 组 5 断言（DL P/A 超经 / UA P 商务 K 经济 / 无航司兜底）+ parse 全组 50 断言 ✓ + 四审计。

**改动位置**：DL 表双份；迁移热修；_cabinFullName 航司化；调用点。

**回滚**：.backups/ 上一版。**上线自检**：贴一单 DL P 舱与一单 UA P 舱——前者 PREMIUM ECO/超经、后者 BUSINESS/商务。
---
## 2026-08-27 — ⚖ 核价面板升级：一键复制重新报价（USD/人均/RMB/结算四件套） · v26.08.27-CP

用户诉求：出现运价差需手动调价重新报价给代理时，要一键复制含**总 USD / 人均 USD / 人民币运价 / 结算价**的报价文本。实现：①核价面板扩两输入——**人数**（卡片价格点入自动带单上人数）与**结算折扣 %**（自动带单上折扣，可空则报价省略结算行）；②底部新增 **「📋 复制报价（发代理）」**——纯函数 `_fareQuoteText` 生成微信可直发文本：`【重新报价】/ 美国运价 $X/人 × N人 = $总 / 人民币价 ¥Y/人 × N = ¥总（汇率 R）/ 结算价（D%）¥Z/人 × N = ¥总`，单人自动省略乘法段；clipboard API + execCommand 双通道复制。核价→发现差→调价→复制→发代理，一个面板闭环。

**测试**：报价 Q 组 3 断言（三人带折扣全文逐字 / 单人省略 / 无美金拒绝）+ parse 全组 50 断言 ✓ + 四审计。

**改动位置**：_fareQuoteText/_fareQuoteCopy/fallback；面板两输入一按钮；卡片入口带上下文。

**回滚**：.backups/ 上一版。**上线自检**：点任一单价格 → 人数折扣已预填 → 填美金 → 📋 复制 → 微信粘贴看四件套。
---
## 2026-08-27 — 解析器：Delta/Chase 列式布局全通 · v26.08.27-CO

新形态：Delta/Chase 积分页**列式排版按列复制**——结构彻底散架（`JFKMCO` 两段到达港竖排粘死、`Air LinesLIMDL 6068` 航司名/机场码/航班号三连粘、Flight 2 整块嵌在 Flight 1 行中、头行 `(FK)` 还丢字符）。实现**全文级列式预扫描**：Flight 块日期锚定位、块内首时刻=出发、剩余时刻按序=到达、`Air Lines` 粘码抓出发港、粘连双码行拆到达港、航司白名单抓航班号——**五数组长度一致才产出段**（守卫防误拼），`pts or $` 金额既有分支消化，Delta One → 商务舱，该模式下布局噪音行静默清零。三坑连环（全部插桩/二分实证）：①三字码后瞻撞粘连航班号；②**\b 词边界在 `LIMDL` 处堵死航司码**（CE 粘连立法第二案）；③裸 `Flight N:` 多匹配致 flIdx 数组不齐被守卫误杀——改与日期锚同源。

**测试**：I 组 3 断言（分单不裂/两段全字段/金额舱位清零）全组 50 断言 ✓ + 四审计。

**改动位置**：列式预扫描块；噪音静默拦截。

**回滚**：.backups/ 上一版。**上线自检**：原样贴该单——DL6068 LIM→JFK 25OCT + DL1511 LGA→MCO 26OCT、段间 ✂ 换城衔接 JFK→LGA、$3,678.57、商务舱。
---
## 2026-08-22 — 🚑 分单器孤儿块修复：Sabre ====== 运价块不再撕裂 + 测试真链立法 · v26.08.22-CN

用户三报"识别不出"终结案。真凶不在解析器在**分单器**：`======` 独立/行内分隔线把单撕成两块（段+首价块 / 次价块+名单），且我全部探针**直接调 parseSingleBooking 绕过了 splitIntoBookings 真实入口**——本地全绿线上全裂。三层修复：①既有"无段块并入下一块"合并机制的**末尾孤儿**（最后一块无段无下家）改前向并入最后有段单；②行内 `======` 分隔路径原本**无合并机制直接 return**——合并器抽共享双路复用；③段判定的 SSR 排除只认行首——**粘连巨行中部的 `SSR DOCS UA HK1` 里 HK1 被误认确认段**拒绝合并，改为剥除行内 SSR DOCS 尾巴后再测。真链四版回归全 1 块全字段等价，两真单 `-----` 分隔照常切 2。**测试立法**：全部样本断言升级走 `splitIntoBookings → parseSingleBooking` 真链＋块数断言（G0/G0b/H0/H0b），新增 S1 多单不误伤——探针绕过真实入口的盲区从制度上绝根。

**测试**：全组 47 断言 ✓ + 四审计。

**改动位置**：_mergeChunks 共享化三处；_hasConfirmedSeg 行内剥除；tests 真链化。

**回滚**：.backups/ 上一版。**上线自检**：原样贴该单——单卡完整（四段四客同卡）、成人 ¥42,682/人、婴 ¥4,298。
---
## 2026-08-22 — 解析器：粘连清洗门槛放宽 + 运价块再压行全通 · v26.08.22-CM

用户贴回同单再压行变形（段号粘 FARE、TAX+TOTAL+类型词+分隔线一锅粥、分组头+NM+四条 SSR 名单巨行）报"依旧无法识别"。三层追查：①新八条断行规则实跑验证全部正确；②真凶=**清洗链的"≤2 行"单行门槛**——此样本十几行不满足门槛，整条链被跳过；③修法=门槛放宽：除原单行判定外，**任一长行（>60 字符）内含粘连特征**（行中部藏 SSR DOCS / FARE CNY / NM 名单 / TOTAL+类型词）也进清洗——断行规则均为结构特异前瞻，对已断行输入幂等无害。八条新规则（v-CM 半程已入）：SSR/NM/分组头/FARE 前断、段号粘 FARE 剥离、====== 分隔线独立、TOTAL 金额后接中文断行、空段占位行清除。四版回归等价：h2 再压行版与 h 多行版、g2/g 婴儿单双版全部同值零未识别。

**测试**：H3 再压行等价断言，全组 42 断言 ✓ + 四审计。

**改动位置**：门槛条件；预处理链八条。

**回滚**：.backups/ 上一版。**上线自检**：原样贴这坨——四段四客、成人 ¥42,682/人、婴儿 ¥4,298、零未识别。
---
## 2026-08-22 — 解析器：Sabre FARE/TAX/TOTAL 运价块样本收尾 · v26.08.22-CL

新样本探针：主干**已由既有分支全通**——四段（含 NH 混航司、+1、E 3 3 尾巴）、FARE/TAX/TOTAL 三件套×2 正确消化（TOTAL 无类型词由块后独行"大人/婴儿"回溯定型）、CK 每人价语义即刻生效（成人 ¥42,682/人 + 婴儿 ¥4,298 直存、paxPrices 四份齐、rmb 契约每人）。收尾两分支：①`3个大人`/`1个婴儿` 人数分组头静默；②**NM 粘连名单**（`NM1LONG/QI MS1SUN/XIAOMAN MS1WANG/ZEYU MR`——称谓与下一序号粘连）逐名提取、剥称谓尾、同名与 DOCS 合并去重（NM 分支位于 isMetaName 声明前，TDZ 约束下以结构特异性替代元词过滤）。婴儿 WANG/RUOCHU（dob 03MAR25）型别由年龄推断兜底铺价正确。零未识别达成。

**测试**：H 组 2 断言（四段混航+运价块每人 / 四客铺价+清零）全组 41 断言 ✓ + 四审计。

**改动位置**：分组头静默；NM 名单分支。

**回滚**：.backups/ 上一版。**上线自检**：贴本样本——四段四客、成人 ¥42,682/人、婴儿 ¥4,298、零未识别。
---
## 2026-08-21 — 🚑 金额语义纠正：TOTAL 分型 CNY = 每人价（用户纠正） · v26.08.21-CK

用户纠正：`TOTAL 成人 CNY 20591` 是**成人每人运价**，不是全员合计——CH 的"÷人数"是我的错误臆断（LAX-PVG-MNL-SFO 多段国际每人两万余才是合理价位，除成 6,863 时就该起疑）。修正：totCn 分支**直接存每人价**入 fareByType（RMB 每人契约），删除全部除人头运算；该单正确形态：成人 ¥20,591/人 × 3 + 婴儿 ¥2,176 = 整单票面 ¥63,949，9 折收入 ¥57,554.1，铺价 paxPrices=[20591×3, 2176]、rmb=20591。**教训入档（rmb 契约大戏第三幕）**：BP 折扣误折进票价 → CH 每人价误除人头——金额语义歧义时禁止自作聪明做乘除，保守存原值、显示层标注口径、让用户"错了再改"。

**测试**：G2/G3 按正确语义重写，全组 39 断言 ✓ + 四审计。

**改动位置**：totCn 分支除法删除；两断言重写。

**回滚**：.backups/ 上一版。**上线自检**：重贴该单——ADULT PRICE ¥20,591/人、收入 ¥57,554、总额 USD ≈ $9,490。
---
## 2026-08-21 — 分档运价显示主次对调：CNY 为主、USD 为辅 · v26.08.21-CJ

用户截图指正（连续两轮质询终于有靶）：ADULT/INFANT PRICE 行把美金换算值放大字主显（`$1,018.44/人`），代理报的人民币原价缩成右侧裸小字（无 ¥ 符号）——数值全对但**主次颠倒**，视觉上就是"CNY 被当成美金"。修正：分档价两行改 **`¥6,863.67/人 × 3人 × 90% ≈ $1,018.44/人`**——人民币加重主显（代理报价口径）、美金折算蓝色辅注；「= 订单总金额 USD」行保持美金主显不动（出票视角，两行分工清楚）。顺修：总额括号内浮点噪声（22767.010000000002 → 22767.01）。

**测试**：全组 39 断言 ✓ + 四审计 + 视觉断言（cnyMain/usdAside 就位）。

**改动位置**：分型价行模板；总额行 toFixed。

**回滚**：.backups/ 上一版。**上线自检**：重开该单——ADULT PRICE 行应为 ¥6,863.67 加重主显、≈$1,018.44 蓝色辅注。
---
## 2026-08-21 — 解析器：单行粘连流拆段（微信压行版） · v26.08.21-CI

用户贴回同单的**单行粘连版**（微信复制把换行全压成空格）报"运价识别错误"。探针实况：乘客/TOTAL 分型/清零全对，**只丢一段**（Sabre 段粘在名单与段间接缝上没断开）。预处理链补三条断行规则：①**Sabre 段前断**——`序号. 航班号 舱位 星期日期` 四要素前瞻（`5.  UA198  P   TH03SEP`），特异结构不会误断名单里的 `4.LIU/RENRAN`；②`护照信息` 标签前断；③独行类型标签（婴儿/儿童/小孩）与其后 DOCS 拆行（DOCS 四字段前瞻守卫，不伤 `TOTAL 婴儿 CNY` 行）。双版探针等价：粘连版与多行版同出 3 段 4 客 ¥6,863.67/人+婴 ¥2,176、零未识别。

**测试**：G3 粘连版等价断言，全组 39 断言 ✓ + 四审计。

**改动位置**：预处理链三条 replace。

**回滚**：.backups/ 上一版。**上线自检**：把微信原样单行粘贴——应与多行版完全一致（三段四客分型价）。
---
## 2026-08-21 — 解析器：婴儿名单/无P DOCS/TOTAL 分型三缺陷修复 · v26.08.21-CH

样本三缺陷（探针实证：三成人全被错标 INFANT、真婴儿被错切成假乘客 US/A、TOTAL 分型总额未除人头）。修复：①**婴儿判定逐名制**——名单行 `4.LIU/RENRAN（婴儿）` 的行级 /婴/ 判定曾把整行全员错标，改为只有紧跟名字后的（婴儿）/（儿童）尾注才标该名（行级男婴/女婴仅单人行保留）；②**括号标注补扫**——nameRe 不认全角括号尾致 LIU 丢名，补扫捕获入列带 forcedType；③**独行"婴儿"标签悬挂**（_docTypePend）+ **无性别双日期 DOCS 分支**（`US/…/26JUL26/19AUG31/LIU/RENRAN/P1`：性别缺位、第二日期即效期）——同名合并补字段，假乘客绝根，dob ≤2 岁自动推断 INFANT 兜底；④`护照信息` 标签静默；⑤**TOTAL 分型除人头**——`TOTAL 成人 CNY 20591` ÷ 3 = 6,863.67/人入 fareByType（婴儿同理），铺价按型：paxPrices=[6863.67×3, 2176]、rmb=6863.67 契约每人。跨窗补记：①③前半上一窗口已落，本窗补齐并删除两处跨窗重复建设（独行标签/无性别 DOCS 双分支、G 组双注入），单一路径保留。

**测试**：G 组 2 断言（四乘客型别与婴儿双日期/三段+分型每人化+清零）全组 38 断言 ✓ + 四审计。

**改动位置**：nameMatches 逐名判定+括号补扫；_docTypePend 链；护照信息；totCn 分支。

**回滚**：.backups/ 上一版。**上线自检**：贴本样本——四乘客（仅 LIU 标 INFANT）、三段、每人 ¥6,863.67+婴 ¥2,176、零未识别。
---
## 2026-08-21 — 解析器：婴儿名单/双日期 DOCS/TOTAL 分型金额 · v26.08.21-CH

用户样本三缺陷探针实锤：**三成人被整行 `（婴儿）` 错标 INFANT**、真婴儿 DOCS（无性别双日期）被错切成假乘客 `US/A`、TOTAL 分型总额未除人头。六补丁：①nameRe 环视加全角括号（`4.LIU/RENRAN（婴儿）` 第四人不再丢失）；②**婴儿判定改逐名制**——只有紧跟名字的（婴儿）/（儿童）尾注才标该名，行级"男婴/女婴"仅单人行生效；③**双日期无性别 DOCS 专用分支**（婴儿护照常见：国籍/号/国/生日/效期/姓/名，可选 P 前缀，bareDoc 前置拦截，同灭 US/A 假乘客），与名单行同名合并补生日效期；④独行 `婴儿/儿童/成人` 标签悬挂 _docTypePend，下一条 DOCS 消费；⑤`TOTAL 成人 CNY 20591` 分型金额——总额 ÷ 该类型当前人数 = 每人价入 fareByType（成人 20591÷3=6863.67、婴儿 2176÷1）；⑥`护照信息` 噪音。探针终态：四乘客全对、rmb=6863.67/人、paxPrices 四份差价铺全、零未识别。注：G 组注入判定词首选 `G1 `（带空格）撞上 F 组样本舱位码 `G1 SA22AUG` 被误判已存在——判定词唯一性立法再次验证必要性。

**测试**：G 组 2 断言全组 38 断言 ✓ + 三审计。

**改动位置**：nameRe；逐名判定；双日期分支；标签悬挂；TOTAL 分型；噪音一项。

**回滚**：.backups/ 上一版。**上线自检**：贴本样本——LIU/RENRAN 独标婴儿、三成人无型别、成人价 ¥6,863.67/人。
---
## 2026-08-20 — 速览面板：代理名可点直开快速切换 · v26.08.20-CG

用户指出快速切换代理在「各订单美金金额」速览面板没有入口（CD 只挂了 PENDING 卡片 ⋯ 菜单）。补齐：速览每行的**代理名直接可点**（stopPropagation 不触发行跳转，照卡主徽章 💳 的成熟交互模式）→ 直开该单的切换器；hover 下划线+染色提示可点。至此切换入口双通道：速览行代理名（最快）+ 卡片 ⋯ 菜单。

**测试**：全组 36 断言 ✓ + 三审计。

**改动位置**：速览行 agent span 一处；hover CSS 一行。

**回滚**：.backups/ 上一版。**上线自检**：速览面板点任意行的代理名 → 弹切换器；点行其它位置 → 照旧跳转订单。
---
## 2026-08-20 — 解析器：UA 中文官网详情分行版全通 · v26.08.20-CF

用户报"没有解析到内容"（中文详情展开复制）。与既有中文流的差异：**头行与日期时刻分两行**（老正则要求同行）。六补丁：①中文头行独立分支（`旧金山SFO至香港(HKG)HKG` 行尾锚防误吃老同行形态）暂存；②中文日期时刻行接续 → 存入既有 _uaPendSeg **等航班号落座**（对齐老中文流语义，cls/舱位回填照旧）；③**变更警告时序修正**——`请注意，本航班涉及日期变更`（中文词并入）出现在航班号之前，改双路：段已落座直接修末段，未落座悬挂 flag 由航班号落座时消费 +1；④`旅客:1` 升级提取 travelerCount（每人化契约链）；⑤`^ 隐藏详细信息` 前缀符号容忍；⑥`TOTAL USD 9,323.19` 金额行（×汇率入 rmb，usd 并存）。探针实证：两段带航班号（UA869 · 18:55+1 / UA34）、¥67,126.97、零未识别。

**测试**：C 组 2 断言（两段+航班号+悬挂变更 / TOTAL USD+清零）全组 36 断言 ✓ + 三审计。

**改动位置**：uhSolo/uhDT 分支；变更双路；落座消费；旅客提取；白名单一项；TOTAL USD 分支。

**回滚**：.backups/ 上一版。**上线自检**：贴本样本——两段各带航班号、第一段到达 +1、金额 ¥67,127。
---
## 2026-08-20 — 解析器：UA 英文官网粘连形态全通 · v26.08.20-CE

新样本（无空格粘连版）：`Oct 2Friday, October 21:25 PM to 6:55 PMNonstop`——四段文字粘死，`October 21:25 PM` 看似 21:25 实为 October 2 + 1:25 PM。实现：①**英文航段头三形态统一分支**（`City (XXX) XXX / City XXX / 混合` 皆认，无数字守卫防误吃）暂存 _uaEnPend；②**日期时刻行标准/粘连统一消费**——12 小时制正则（小时 ≤12）天然免疫粘连歧义（引擎自动跳过 `21:25` 落到 `1:25`），落座含 12→24 转换、舱位回填、date change +1 照旧生效；③`3 adults 18+` 升级为**人数提取**（travelerCount，每人化契约链修通：Total $9,323.19 ÷ 3 × 汇率入 rmb + paxPrices 三份）；④噪音三补丁：kg CO2 行内式、Price breakdown、Flight N of N。

**测试**：N 组 3 断言（粘连两段含 +1/第二段/每人化+清零）全组 34 断言 ✓ + 三审计。

**改动位置**：_uaEnPend 状态；英文头+日期统一分支；人数提取；噪音三项。

**回滚**：.backups/ 上一版。**上线自检**：贴本样本——两段落座（SFO→HKG 带 +1）、¥每人价三份、零未识别。
---
## 2026-08-19 — 🔁 代理快速切换：选错无需删单重录 · v26.08.19-CD

用户痛点：录单选错代理只能删单重录（乘客资料全白打）。实现：订单卡三点菜单新增「🔁 切换代理」→ 小窗：当前代理明示 + 候选 chips（presetAgents ∪ 现有订单出现过的代理，去重、排除当前）一点即换 + 自由输入（回车即确认）兜底。切换走完整安全链：o.agent 更新 + _lastEdit 落章 + persistOrdersNow() 咽喉持久化 + 全列表重渲染（单子自动移入新代理分组）+ toast 报告 from→to；其它字段零触碰；同名切换幂等静默。**流程立法追加**：本刀第 N 次裸计数断言口算翻车（==3/==5 实为 2/4）——自此计数断言一律 >= 下限，精确性交给功能断言。

**测试**：全组 31 断言 ✓ + 语法/写手/桩泄漏三审计（切换走 persistOrdersNow，写手恒 2）。

**改动位置**：openAgentSwitch/_doAgentSwitch；订单菜单一项。

**回滚**：.backups/ 上一版。**上线自检**：任一单 ⋯ 菜单 → 🔁 切换代理 → 点候选 chip → 单子跳入新分组、toast 显示 from→to。
---
## 2026-08-19 — 治理复查（六刀零新债）+ 调试面板考古与两缺陷修复 · v26.08.19-CC

**体检报告**（1.30MB · 28,947 行）：死函数零、定时器 5/5 平衡、三审计绿——BW–CB 六刀（含线上事故与立法）后零新债。**记录不动**：重复内联样式 9 组（动 18 处模板锚风险>收益）；toast 前缀 95/292（多为中性信息）；parseSingleBooking 3041 行（样本驱动必然，拆分毁锚点）。**澄清**：`ticket-dark-mode` 为会话前旧暗色功能（带开关），"不做夜间"指不新做，保留。**调试面板考古**：老 PENDING"面板恒空"记录**失实**——parseSingleBooking 内部一直有完整彩色渲染（本轮为它写新管道属重复建设，已撤销）；真缺陷两个并修复：①**多单覆盖**（后单渲染覆盖前单）→ 改追加模式带分隔线、parsePNR 入口清一次；②**硬依赖 DOM**（无 document 环境即炸）→ typeof 守卫（解析器纯度原则）。桩泄漏审计名单增补 _parseDebugLog。**PENDING 盘点**：滚动快照已销（BX）；调试面板记录改正（本版）；腾讯云拍板/BK 部署验证/agent 回环/手机端仍挂账待用户。

**测试**：D1 改测调试模式无 DOM 安全 + 全组 31 断言 ✓ + 三审计。

**改动位置**：旧渲染块两处（守卫+追加）；parsePNR 入口面板清空。

**回滚**：.backups/ 上一版。**上线自检**：开 🔍 → 贴多单样本解析 → 面板应逐单显示彩色日志（单间分隔线）。
---
## 2026-08-19 — ⏱ 机场最短转机参考提示（MCT 分级） · v26.08.19-CB

用户提议：按机场给转机时长风险提示（截图样本 SFO 1h 7m 转国际）。实现：①内置**常用枢纽 MCT 参考表**——美国 12 枢纽四口径（国内→国际/国际→国内含 CBP 入关/国内→国内/国际→国际）+ 亚洲 12 枢纽国际口径，取各枢纽官方 MCT 保守下限；②40 个美国机场清单自动判段属性（是否跨美边界）选对口径；③转机行三级提示：**低于参考线红色 `⚠ 低于 SFO 参考最短转机 1h0m，风险高`**、1.5 倍内琥珀 `偏紧（SFO 参考最短 1h0m）`、宽裕不打扰；表外机场回落旧 60 分钟兜底。**定位声明**：参考值非违规判定——官网可售衔接均合法，价值在出票前看见风险敞口。样本实证：LAX→SFO→ICN 的 1h 7m = 国内转国际、SFO 参考 1h0m → 琥珀"偏紧" ✓。

**测试**：M 组 4 断言（截图样本偏紧/低线风险/亚洲宽裕+未知机场静默/国际入境含入关口径）全组 30 断言 ✓ + 三审计。**上线自检**：重开该单看 SFO 转机行出现琥珀偏紧提示。

**改动位置**：_MCT_TABLE/_US_APTS/_mctCheck；转机分支判级。

**回滚**：.backups/ 上一版。
---
## 2026-08-19 — 段间行可读性提亮 + 回程语义修正 · v26.08.19-CA

用户截图验收两条：①转机行 muted 色太浅看不清——提亮加重（12px / var(--ink-2) / 600 重，回程行 700）；②ORD→LAX 36 天被标"换城衔接"语义不对——那是**回程**（LAX→HKG 终点回到行程起点）。判定升级：整程往返（末段 to === 首段 from）时，长间隔断口（>24h 或时长未知的换城）标 **`↩ 回程 · 停留 36天 · ORD → LAX 自行前往`**；真开口票（终点不回起点）才保留"✂ 换城衔接"；同城转机/停留分类不变。

**测试**：全组 26 断言 ✓ + 语法/写手/桩泄漏三审计。**上线自检**：重开该单——段1-2「⏱ 转机 SFO · 9h 50m」（更清晰），段2-3「↩ 回程 · 停留 36天 · ORD → LAX 自行前往」。

**改动位置**：CSS 两行；渲染分支一处。

**回滚**：.backups/ 上一版。
---
## 2026-08-19 — 段间信息三分类：转机 / 停留 / ✂ 换城断口 · v26.08.19-BZ

用户指出三段单只显示一个转机。真相：段2 到 ORD、段3 从 LAX 起飞隔 36 天——那不是转机是**开口票断口**，旧逻辑">3 天不显示"技术对但沉默错：断口与长停留正是出票最该标出的结构。段间升级三分类全覆盖：**同城 ≤24h = ⏱ 转机**（<60 分钟红标紧张）；**同城 >24h = 🛏 停留 N天**（灰弱化）；**换城 = ✂ 换城衔接 ORD → LAX · 36天 · 非转机，出票请核对**（琥珀警示，时长算不出也标断口）。_fmtLayover 升级天级（`36天`/`2天 3h`）；_layoverMinutes 上限放至 370 天。

**测试**：L6 截图样本（ORD→LAX 51,846 分钟=36天）/L7 天级格式，全组 26 断言 ✓ + 语法/写手/桩泄漏三审计。

**改动位置**：两纯函数；渲染三分类；.layover-gap 样式。

**回滚**：.backups/ 上一版。**上线自检**：推送硬刷后重开截图那单——段1-2 间「⏱ 转机 SFO · 9h 50m」、段2-3 间「✂ 换城衔接 ORD → LAX · 36天」。
---
## 2026-08-19 — 🚑 线上事故修复：渲染失败 `_MN3 is not defined` + 桩泄漏审计立法 · v26.08.19-BY

**事故报告（用户截图报障）**：BW 的转机功能导致订单卡渲染失败。根因：`_dateOrd` 引用 `_MN3` 月份表——它在浏览器里是 parseSingleBooking 的**内部局部量**，顶格函数够不到；而**测试桩恰好全局声明了同名变量**，24 项断言全绿掩盖了作用域差异——测试环境与真实环境的偏差放走了必炸的代码。讽刺注脚：上一版刚上线的 🚨 错误红点，第一个抓到的就是自己人。**修复**：_dateOrd 自包含月表（渲染路径共享纯函数禁止引用解析器闭包/桩变量，已写入函数注释）。**全库排查**：十个桩名全扫，唯一泄漏即此处，其余引用均有全局定义或属 parseSingleBooking 内部自引。**审计立法（发货链常驻新步骤）**：桩变量泄漏检查——任何顶格函数引用了"无全局定义的测试桩名"即拦停发货（parseSingleBooking 豁免），本类事故从机制上绝根。**流程承诺**：每版发货说明将附"上线后请先贴一单验证渲染"的自检提示。

**测试**：parse 24 断言 ✓ + 语法失败块=2 + 写手=2 + **桩泄漏=0（新法首跑）**。

**改动位置**：_dateOrd 一处；审计脚本新增泄漏检查。

**回滚**：.backups/ 上一版。
---
## 2026-08-19 — 🛟 数据保险箱（IndexedDB 每日快照）+ 🚨 错误可观测 · v26.08.19-BX

用户再问极限。诚实答案：所有功能之上悬着一把剑——localStorage 是唯一数据源，浏览器清缓存=全部业务数据蒸发，"每日提醒手动导出"依赖人不忘记。当年删本地滚动快照的理由（云同步兜底）失效后此洞敞了很久。**①数据保险箱**：每日自动滚动快照存 **IndexedDB**（独立仓库不占 localStorage 配额、清 localStorage 通常不伤它）——启动 9 秒后静默存今日份（IndexedDB 写入不违反"启动期不写库"原则）、保最近 7 份；头部新增 🛟 面板：列快照（日期/单数/大小/时刻）一键恢复——**恢复前自动把当前状态另存一份**（双保险防手滑），恢复写 pending 键严格走 {orders, currentIdx} 包装（咽喉契约）后刷新；「📸 立即快照」手动加存。与手动导出 JSON 互为双保险。**②错误可观测**：63 处静默 catch 之外，window error/unhandledrejection 全局收集（环形 20 条），出错时头部亮 ⚠N 红点，点开看时刻+消息+位置，报修截图即证据；无错误时按钮隐形零打扰。

**测试**：包内 parse 24 断言全绿 + 语法失败块=2 + 写手=2 + 恢复直写=1（专用豁免，带包装）。

**改动位置**：快照模块（六函数）；错误模块；头部 🛟/⚠ 按钮；启动挂点。

**回滚**：.backups/ 上一版。
---
## 2026-08-19 — ⏱ 段间转机时长 + 审查复扫 + 动效人体工学 · v26.08.19-BW

用户以 Amex 出票页对照（`3h 5m in DFW`）：系统展开卡段间无转机时间，比对行程靠心算。**①转机时长**：纯函数组——`_t2min` 时刻归一（12 小时制 5:50PM / 4 位 1915 / 带冒号 17:50 / 跨天 +N）、`_dateOrd` 日期序（同年假设，DEC→JAN 跨年 +365 修正）、`_layoverMinutes` 段间停留分钟（非正或 >3 天不显示）；展开卡段间插 `⏱ 转机 DFW · 3h 5m`——**<60 分钟红标"转机紧张"、>5 小时弱化"长停留"**，样式融入虚线时间轴设计语言。Amex 样本实证 17:50→20:55 = 3h 5m 与官网一致。**②审查复扫**：死函数/孤儿/log/重复全零。**③动效人体工学**：verify-modal 全家族统一淡入上浮入场（一处 keyframes 全家受益）；卡片 hover 微阴影；prefers-reduced-motion 时全动画关闭。测试抽取锚固化：起点恒 `^function _ordKey`。

**测试**：L 组 5 断言（12h/4位跨天/Amex 同日 185 分钟/跨日 100 分钟/缺数据守卫）+ 全组 24 断言回归。语法失败块=2 + 写手=2。

**改动位置**：四纯函数；段循环转机行；CSS 三组。

**回滚**：.backups/ 上一版。
---
## 2026-08-19 — ⚖ 快速运价核对小插件（RMB 折算 vs 美国运价） · v26.08.19-BV

用户诉求：代理报人民币价，需最快核实折算美金与美国运价是否一致。实现：①纯函数 `_fareCheckCalc(rmb, rate, usd)` → 折算美金/差额/差率（每人口径，与 rmb 契约一致）；②⚖ 核价面板：三输入（代理 ¥ 价 / 汇率自动带系统实时值可改 / 美国运价 $）边输边算零按钮，大字折算美金＋差额差率——美国价更低显绿（有空间）、更贵显红；焦点自动落下一个待填框；③双入口：待出票工具条 ⚖ 按钮；**卡片右侧价格直接可点**（点哪单带哪单每人 ¥ 进面板，只差手输一个美金数）。

**测试**：核价 4 断言（正差有空间/负差/仅折算/零守卫）+ 包内 parse 全绿。语法失败块=2 + 写手=2。

**改动位置**：_fareCheckCalc/_fareCheckRender/openFareCheck；工具条 ⚖；cs-price 点击联动。

**回滚**：.backups/ 上一版。
---
## 2026-08-18 — 待出票折叠头信息层级重设计（人因第一刀） · v26.08.18-BU

用户拍板：不做夜间模式，做卡片信息层级与人因改版。诊断（扫读原则）：折叠头摘要是一坨等权重文本——乘客/日期/价格三个判别锚点无分层眼无落点；价格淹没句尾；行动级警示（临近起飞）混在信息徽章堆。**呈现层重构（数据与事件零改动）**：①摘要拆三层——`.cs-pax` 乘客姓加重（判别锚）、`.cs-mid` 行程舱位可截断（中段让位保两端）、**`.cs-price` 价格右锚定**（margin-left:auto 固定列位、等宽 800 重、空价显 — 弱化）；②**临近起飞 chip 前移判别区**（航司徽章后，行动级优先于信息级）；③`ORDER #N` 瘦身为 `#N`；④未加急 ○ 钮弱化至 35% 透明度（行 hover 时 80%）降噪。窄屏依托中段截断自适应。**迭代约定**：这是第一刀，用户截图反馈后继续；展开卡操作按钮区（出票/删除距离等 Fitts 审查）留待下轮。

**测试**：包内 tests 全绿 + 语法失败块=2 + 写手=2 + 视觉断言（.cs-price 就位/旧文案清零）。

**改动位置**：折叠头构建四段；CSS 三层类与加急弱化。

**回滚**：.backups/ 上一版。
---
## 2026-08-18 — 测试资产固化进仓库 + 键盘流 · v26.08.18-BT

用户质询"这是你能做到的最好吗"——诚实答案：不是，BS 把风险纪律用成了避重就轻。本版补两项实质：①**测试资产固化**——容器三次重置三次损失测试的教训终结：19 项解析断言与全部样本（三大样本内嵌）打成自包含 `tests/parse_test.js` **进 zip 进仓库**（第五文件），重置后解包即恢复完整测试，发货链改从包内测试跑（parser.js 抽取法写在文件头注释）；②**键盘流**——录入高频动作不离键盘：输入框内 **Ctrl/Cmd+Enter 直接解析**（placeholder 同步提示），**Esc 关闭最上层弹窗**（verify-modal 家族通用，全局一次注册）。**三个大项留用户拍板**（不擅动）：深色夜间模式（CSS 变量体系已就位，需截图协作迭代）；待出票卡片信息层级重设计（盯得最久的界面、徽章渐多，动它需共同定稿）；手机端体验专项。

**测试**：自包含 tests/parse_test.js 手术前后双跑全绿 + 语法失败块=2 + 写手=2。**zip 自本版起五文件**（+tests/parse_test.js）。

**改动位置**：tests/ 新目录；rawInput 键盘钩子与 placeholder；Esc 全局监听。

**回滚**：.backups/ 上一版。
---
## 2026-08-18 — 四维打磨：审查/流程/错误/界面 · v26.08.18-BS

**审查**：BO–BR 四刀后复扫——新死码 **0**、孤儿 **0**、console.log **0**、重复函数 **0**。**流程**：消掉每单必踩的摩擦点——**表 Label 记忆**：选过一次写入本机，后续新草稿自动带出上次选择（下拉可改、解析必填校验原样保留；批次内同表是常态命中率高；清记忆删 localStorage 键 to-last-table-label）。**错误整理**：13 处 alert 全数分类——11 处应拦阻断保留（黑名单/OFAC/出票保存失败/批量不完整/导入合并失败等），2 处轻量输入校验降级 toast；catch 空吞 63 处属防御模式记录在案不动。**界面美化**（克制路线）：头部工具按钮内联样式提取 `.tool-btn` 类＋hover 微交互（描边染色+1px 上浮）；focus-visible/自定义滚动条已有、btn 体系 162 枚统一，底子健康不折腾。

**测试**：parse 全组断言 ✓ + 语法失败块=2 + 写手=2。

**改动位置**：草稿 tableLabel 默认；select onchange 存储；两 alert 降级；.tool-btn CSS 与按钮类化。

**回滚**：.backups/ 上一版。
---
## 2026-08-18 — 算式运价语义纠正：票面归票面、折扣归折扣（用户验收质询修正） · v26.08.18-BR

用户验收质询点破：`3807*0.9*3` 中 **3807 就是每人票面原价**、总运价 3807×3，`*0.9` 是与代理的**结算折扣**（代理发的即出票单）——BP/BQ 把 0.9 折进每人价（存 3426.3/人）是语义错误。追查发现系统**早有正确语义的现成分支** pCabinMulti（`9120*0.88*3` 一类：base 原价、<1 因子入 parsedDiscount、人数直铺 paxPrices），本样本没进它只因正则被 `1PC` 行李缀挡住。修正：①放宽 pCabinMulti 正则（+公务、可选 `\\d+PC` 行李缀、宽空格）——样本走既有正确轮子全链贯通：rmb=3807/人、paxPrices=[3807×3]、**discount=90 入折扣字段**、cabin=经济舱 顺带识别、`=10278.9000` 为结算总额核对行；②裸算式分支（无舱位前缀）同构化：首数=每人票面、<1 因子=折扣入 parsedDiscount（10-100% 范围守卫、不覆盖已有）、≥2 整数=人数仅记录。

**测试**：F1/F2/F3 按业务真相重写全绿（票面/折扣/舱位/paxPrices 四通道核验）+ 全组回归。语法失败块=2 + 写手=2。

**改动位置**：pCabinMulti 正则；裸算式分支重写。

**回滚**：.backups/ 上一版。
---
## 2026-08-18 — 金额契约修正：rmb=每人价 遵约化 + TRAVELERS 恢复消费 · v26.08.18-BQ

BP 的 F1 断言失败经三层追查（逐行注入定位→_d hook 追踪→组装段审读）结案为**认知反转**：系统契约 `rmb=每人价`（组装段注释 per-pax by contract），引擎填 3426.3 是对的、我的断言与分支写法才违约。修正：①算式分支 rmb 遵约存每人价（!rmb 守卫），总额仅入 _d 供人工核对；②`=合计` 行降级为核对记录不写 rmb；③**TRAVELERS 恢复**——BM 噪音拦截曾把 `ROUNDTRIP (2 TRAVELERS)` 整行吞掉致人数解析断供，改为就地消费（提取 travelerCount 再跳过），UA 英文 Total due 恢复"总额→每人化+铺 paxPrices"契约链（E3 重写：rmb=¥9,134.14/人 × 2）。契约备忘另录：同价多人单不铺 paxPrices 属设计（rmb×人数=总额）。**发货链立法**：BP 暴露 `node test | tail` 管道吞退出码——测试步一律改先跑后看（重定向再 tail），本版该立法首战即拦停一次带伤发货。

**测试**：F1（每人 3426.3+同价不铺契约）/F2/F3（每人化）/E3（每人化+paxPrices 双份）全绿；全组 30+ 断言通过。语法失败块=2 + 写手=2。

**改动位置**：算式分支两处；ROUNDTRIP 行分支；测试三断言重写。

**回滚**：.backups/ 上一版。
---
## 2026-08-18 — 乘法算式运价识别 + 摘除出票利润核算强弹窗 · v26.08.18-BP

**①乘法算式运价**：`普通经济1PC 3807*0.9*3` + 下行 `=10278.9000` 新形态——因子智能分类（<1.5 判折扣、≥1.5 判人数），每人价=单价×折扣入 parsedFareByType.adult（RMB 口径，铺价机制自动接管），总额=显式 `=` 值优先否则乘积入 rmb；独立 `=金额` 行单独消费；容忍舱位行李前缀（普通经济1PC/公务2PC）、×/x 号变体、两因子省略式。折扣因子刻意不写入订单 discount 字段（那是代理折扣规则的地盘）。**②摘除利润核算强弹窗**：用户指令——分配卡主后自动弹的「💰 出票前利润核算」多此一举（收益在账单/对账自然可见）。整套摘除：自动触发点 + openProfitCheckDialog / updateProfitPreview / closeProfitCheckDialog / saveProfitCheck 四函数；miles/积分类型录入不受影响（#f-miles 表单通道独立存在）。

**测试**：parse_test 增 F 组 3 断言（全样本两段三人 ¥10,278.9+每人 3,426.3+零未识别/两因子折扣型/两因子人数型）+ 弹窗四函数清零断言。语法失败块=2 + 写手=2。

**改动位置**：算式运价分支与 = 行分支；卡主分配尾触发点；四函数切除。

**回滚**：.backups/ 上一版。
---
## 2026-08-16 — 📊 订单更改分析：录入时自动比对该代理已上单 · v26.08.16-BO

用户诉求：每次录入与该代理已上单比对——什么单不在了（疑似撤单）、新增了什么。实现（在既有 strong/weak 重复检测基建上补齐缺口翼）：①**纯函数分析器** `_ordKey`（乘客名集合排序+首段航班+日期，大小写空格归一）+ `_analyzeAgentChanges`——键口径刻意窄于 fingerprintOrder 模糊匹配，宁可多报交人工确认，**绝不自动删单**；②**parsePNR 挂点**：解析入库后取本批代理，基线=该代理全部待出票单（排除本批新增与 strong 覆盖旧单），算"本批未见"，toast 摘要追加 `⚠️ 本批未见 N单`；③**分析面板**（有重复拦截或未见时 0.6s 自动弹，纯新增不打扰）：🆕 本批新增 / ♻️ 重复已拦 / ⚠️ 本批未见（按语：疑似撤单，代理增量发单可忽略；确认撤单到卡片手动删）。行格式 `乘客 等N人 · 航班 日期 From→To`。

**测试**：parse_test 增 O 组 3 断言（未见识别+归一/乘客序不敏感/首段日期改判不同单）。语法失败块=2 + 写手=2。

**改动位置**：_ordKey/_analyzeAgentChanges/_ordLine；parsePNR toast 前挂点；_showOrderChangeAnalysis 面板。

**回滚**：.backups/ 上一版。
---
## 2026-08-14 — 阶段性代码治理：整理/审核/清洁/优化/美化 · v26.08.14-BN

全维度体检（1.29MB · 28,683 行）与定点治理。**审核**：全局孤儿调用扫描——真孤儿 **0**（大量候选经定性均为注释英文词/CSS 函数/module import 误报）；零引用定义 15 个中 14 个为已归档误报（purgeRollingBackups/checkDailyBackupReminder 具名 IIFE + 12 个函数内局部量），真死 1 个；TODO/FIXME 7 处全为占位符误报（真待办 **0**）；console.log=0、重复函数=0、setInterval/clearInterval 收支平衡 5/5。**清洁**：删 BH 手滑的废常量 `_SLACK_WH_KEEY`；行尾空白 3 行清零；3+ 连续空行压缩。**整理**：主脚本头部新增**区块索引 TOC**（十大区块导航，不带行号防漂移）。**审核-注释矫正**：五处 Firebase 时代过时注释改真——最误导的 `Data safety is provided by Firebase cloud sync` 等已在撒谎的描述全部改为现状（本地唯一数据源+备份提醒兜底），两处历史事故文献（07-23/07-25 根因）保留原文并加过去式标注。**优化/美化立场声明**：拒绝全文件格式化重排（28k 行锚点体系与备份 diff 的破坏远超收益）；无 O(n²) 热点报告；区块头风格已在历次新增中统一。**遗留拍板项**：本地滚动快照是否恢复（当年因"有云同步"删除，理由已失效）。**误报名单固化**（供未来扫描对照）：purgeRollingBackups / checkDailyBackupReminder / expYY / expMM / expDD / headerLabel / hdrStyle / logoSvg / chaseUrBadge / maxUsd / makeSelect / oldDiscount / todayStr / progressPct。

**测试**：parse 18 断言 ✓ + 语法失败块=2 + 写手=2 + 十枚关键函数存活 + 零 log/零重复。

**改动位置**：废常量删除；五处注释矫正；TOC；空白卫生。零逻辑改动。

**回滚**：.backups/ 上一版。
---
## 2026-08-14 — 解析器：UA 官网英文版行程（金额污染修复 + date change） · v26.08.14-BM

用户举例：代理会发 UA 官网英文行程。探针：两段航段与 12 小时制转换**早已工作**（英文头行+日期行既有分支），但三处真伤：①**金额污染**——`$1,180.20/person` 被当整单入 rmb（¥8,497，真实整单 $2,537.26≈¥18,268，差一倍多）；②`Please note this flight involves a date change` 未消费，SFO→PVG 段丢 +1；③Basic Economy 舱位未接。修复：英文票价区状态机——`/person` 行入 parsedFareByType.adult（每人 RMB）、`Total due` 后首个 $（行内或下一行）为整单 rmb、Fare/Taxes 标签行拦截防污染；date change 警告 → 最近落座段 arrTime 补 +1（未带 + 时）；舱位行（扩 Economy/Premium Economy/Business/First）存 pendCabin 同时回填已落座末段（英文流先落座后见舱位，中文流不受影响）；ROUNDTRIP/Show details/Nonstop/kg CO2/N adults 等噪音显式跳过。注：英文官网复制不含航班号（需点 Show details），flight 留空待人工补。

**测试**：parse_test 增 E 组 3 断言（两段+跨天警告消费+12→24/舱位双段回填/整单与每人金额语义+未识别归零）。语法失败块=2 + 写手=2。

**改动位置**：_uaSawTotalEn 状态；舱位分支回填；英文票价区/警告/噪音分支。

**回滚**：.backups/ 上一版。
---
## 2026-08-14 — 🧼 微信聊天记录智能清洗：国内代理零门槛通道 · v26.08.14-BL

用户点破 BK 门户前提：国内很多代理不会用 VPN。替代后端全盘（LeanCloud 要备案域名、Supabase/CF 大陆可达性不稳、GitHub Issues 有乘客隐私死结）后结论：**唯一正规国内解=腾讯云开发（约 ¥20/月起+实名+另一套运维），是否上马留用户拍板**。本版落地零成本双轨第一轨：**微信聊天记录智能清洗**——与代理的聊天记录整段复制（含名字/时间戳/[图片]）直贴解析框，parsePNR 入口自动剥说话人前缀（独行 `东莞:`、行内 `东莞: 内容`、时间戳头 `东莞 2026/8/14 10:22`）、删时间戳独行与 [图片]/[转账] 等占位、长横 `———` 删而 ASCII `-------`（多单分隔符）保留；**唯一说话人自动填入代理框**。防误伤三重保险：说话人计票 ≥2；31 词解析关键词黑名单；多说话人保守模式（不剥前缀+提示分开贴）。清洗透明：结果写回输入框，toast 报删行数与代理名。BK 门户保留给有网络条件的代理（双轨并行）。

**测试**：parse_test 增 W 组 5 断言全绿（三形态清洗+代理识别/清洗后全管道解析/关键词零误伤/多说话人保守/纯 PNR 零动作）。语法失败块=2 + 写手=2。

**改动位置**：_WC_BLACKLIST + _wechatClean；parsePNR 入口挂钩。

**回滚**：.backups/ 上一版。
---
## 2026-08-14 — 📥 代理自助门户：agent.html 提交 / 撤单 / 查状态 + 主站收件箱 · v26.08.14-BK

用户诉求：代理微信发单还要手工录入太累，要一个代理自己录入/撤单/查状态的接口。架构如实：纯静态页无法让代理数据流到本机，必须共享后端——方案盘点后选 **Firestore 代理收件通道**（新集合 agent-orders；**不是恢复旧同步**，主数据仍只在本地；前提：代理需可访问 Google 服务的网络，与其使用 WhatsApp/Discord 同一前提）。三件套：①**agent.html 代理页**（新文件，手机优先中文 UI）：名字（八人 datalist+自由输入，记住）→ 贴原文＋备注 → 提交；「我的提交」列表带四态徽章（待接收/已接收/已拒收/已撤单），pending 可一键申请撤单；匿名 Auth 自动隔离（代理互不可见）；permission-denied/断网均有人话提示。②**主站 📥 收件箱**（🧹 旁）：惰性连接（点开才连 Google），列 pending 提交（代理/时间/备注/原文预览），「✓ 接收」= 原文入 #rawInput＋代理名入 #f-agent＋走 parsePNR 全管道解析（表 Label 与保存仍人工核对——不绕必填校验），状态回写 accepted；「✕ 拒收」回写；开窗期 60s 轮询；首行显示本设备 UID 一键复制（配置管理员白名单用）。③**firestore.rules**（新文件）：create 限本人+pending+字段限长、读改限本人、撤单仅 pending→cancelled 且只许动两字段、管理员白名单（admin/config.uids）全权、**其余路径全封死——旧同步集合与两个月的测试模式悬案就此销案**。

**部署（一次性三步）**：a) 四文件全放仓库根目录推送；b) Firebase 控制台 → Firestore → 规则 → 粘贴 firestore.rules 全文 → 发布；c) 控制台建文档 `admin/config`，字段 `uids`（数组），把主站收件箱里复制的设备 UID 填入（每台管理设备一条）。代理链接：`…/agent.html` 发微信即可。

**测试**：语法失败块=2（两 module script 属预期，审计口径更新）+ 写手=2 + agent.html 五要件断言 + console.log/重复函数零。

**改动位置**：新文件 agent.html / firestore.rules；主站 _fbx 桥 module、📥 按钮、收件箱模块。**zip 自本版起为四文件**。

**回滚**：.backups/ 上一版（agent.html/rules 不影响主站，可单独回滚）。
---
## 2026-08-13 — 解析器：DOCS 缺国籍变体 + 小写 p 尾 · v26.08.13-BJ

三行 DOCS 变体样本：①`P/EJ1525540/CN/…` **缺国籍字段**（P/护照号/国家 排列）——旧正则错位匹配，名字生日对但**效期被吞成空**（比不识别更阴险）；修复：正则头改双排列非捕获交替 `(?:国{2,3}/号{5,}|号{5,})`，捕获组号不动，效期归位；②行尾**小写孤 `p`**（`/p` 无数字）卡死 `\\/P\\d+`——两条 DOCS 正则尾放宽为 `[-/]P\\d*`（/i 下 p/P/P1 全收）；③小写月份 `18dec07`——handler 既有 toUpperCase+补零管道，正则通了即自动规范为 `18DEC07`。

**测试**：重置后按需重建 parse_test——本刀三断言（缺国籍效期归位/小写p尾/小写月份规范+三人全通零未识别）+ 历史抽查两枚（Sabre 星期前缀跨天标、全字段斜杠 DOCS）。语法失败集=4 + 写手=2。

**改动位置**：bareDocMatch 头；两正则尾序号。

**回滚**：.backups/ 上一版。
---
## 2026-08-13 — Telegram 预填通道：别人的群两击可达 · v26.08.13-BI

用户点破边界：Webhook/Bot 三条全自动路都要求对群有接入权，只适合自己的群。补齐**别人的群**的合规通路：Telegram 新增 `t.me/share` 预填跳转（与 WhatsApp 的 wa.me 同级）——点开 Telegram、文本已预填、任意群选中即发，两次点击。发送窗 Telegram 区改双按钮：「Bot 直发（自己的群）」+「预填 — 任意群」。明确不做：Discord selfbot（违反 ToS 封号风险高）；Slack 他人 workspace 无合规通路。业务建议：自建收分群可让全自动通道全线成立。

**容器重置记录**：本轮容器第三次重置，已按守则从交付包自恢复 BH 并重建 ship.sh；/tmp 测试资产损失，本版以语法失败集=4、写手=2、十枚关键函数存活断言代偿，纯函数测试后续按需重建。

**测试**：语法失败集=4 + 写手=2 + 关键函数存活 ✓ + console.log/重复函数零。

**改动位置**：sendBuyingToTgShare；发送窗 Telegram 区双按钮。

**回滚**：.backups/ 上一版。
---
## 2026-08-13 — 📤 发送扩展：Telegram Bot 直发（带回执）+ Slack Webhook（盲发） · v26.08.13-BH

用户追问 Slack/Telegram。四通道边界如实：**Telegram = 全场最优**——Bot API 支持浏览器跨域、真自动且逐条回执可判成败（@BotFather 建 bot → 拉进收分群 → 填 Token+Chat ID；4096 上限按 4000 分段；401/404 判 Token 错、400 判 Chat/未入群，均给修复指引）；**Slack = Incoming Webhook 盲发**——hooks.slack.com 不返回 CORS 头，以 no-cors 模式发出（消息可达但浏览器读不到回执，提示去频道确认）；Discord/WhatsApp 沿用。配置各自独立存本机 localStorage（_TG_CONF_KEY / _SLACK_WH_KEY，同 API Key 策略不进备份，可清除），发送窗四区块紧凑排列，未配置通道按钮置灰。

**测试**：send_test 增至 6 断言（T1 合法 Token/负数群 ID/@频道名；T2 三种非法形态拒收）+ 全量回归 + 语法失败集=4 + 写手=2。

**改动位置**：发送模块三函数与两常量；发送窗四通道 UI。

**回滚**：.backups/ 上一版。
---
## 2026-08-13 — 📤 收分需求一键发送：Discord 直发 + WhatsApp 预填 · v26.08.13-BG

用户诉求：每次手动往 WhatsApp/Discord 发收分需求太累。能力边界如实拆分：**Discord 走 Webhook 真·一键直发**（纯前端 POST，无需后端/登录；频道 ⚙→整合→Webhook→复制链接，粘贴一次长期生效）；**WhatsApp 无后端做不到全自动**（商业 API 需 Meta 认证且有封号风险），做成 `wa.me` 预填跳转——点开 WhatsApp 文本已在输入框，选群按发送即可。实现：①「确认复制」时文本存入会话（_lastBuyingText），toast 提示可发送；②工具条新增「📤 发送」→ 发送窗：字数/分条预览、Webhook 配置（格式校验；**仅存本机 localStorage，同 API Key 策略不进备份**，可清除）、「发送到 Discord」（_splitForDiscord 段落≤1900 智能分条＋行切＋硬切兜底，条间 450ms 防限流；401/403/404 识别失效引导重配）、「WhatsApp ↗ 预填」。

**测试**：send_test 4 断言（单条/段落打包 2 条完整/行切/硬切）+ parse/pack/fx/wrap 全量回归 + 语法失败集=4 + 写手=2。

**改动位置**：BUYING LIST 区头发送模块；确认复制存档；工具条 📤。

**回滚**：.backups/ 上一版。
---
## 2026-08-12 — 解析器：DOCS 行尾类型词扩表（成人/小孩）并入 forcedType · v26.08.12-BF

用户报连字符 DOCS 仍不识别。定位：行尾缀了「成人」「小孩」——两条 DOCS 正则的尾类型表只有 儿童/婴儿/CHD/INF，新词卡死 `$` 锚整行落空。修复：①尾表扩为 儿童/婴儿/小孩/小儿/成人/大人/CHD/CHILD/INF/INFANT/ADT/ADULT 并升为捕获组6（前五组号不变）；②bare handler 消费组6——小孩/小儿/儿童 → forcedType=CHILD（儿童价体系自动生效），婴儿/INF → INFANT，成人明确放行不标；合并分支不覆盖已有 forcedType；③SSR handler 同款兜底（given 尾剥离优先，组6 补位）。

**测试**：parse_test 增至 23 断言——S21 成人尾放行全字段且无 forcedType；S22 小孩（2019 年生）入 CHILD；S23 未识别归零。全量回归 + 语法失败集=4 + 写手=2。

**改动位置**：两条 DOCS 正则尾；bare/SSR 两 handler 类型消费。

**回滚**：.backups/ 上一版。
---
## 2026-08-12 — 解析器：连字符 DOCS 支持（同行程双单样本全通） · v26.08.12-BE

用户报"无法识别"（同行程公务/经济双单样本）。探针定位：多单拆分（段序号回绕）、三段（含 1640+1 跨天）、裸运价行 `01 DNX0ZKRK+* 39822 CNY`（序号+`+*` 形态，PENDING 销案）、`公务2PC` 舱位行李行——**全部已工作**；唯一缺口是**连字符 DOCS**：`P-CHN-EJ4854521-CHN-04JAN77-F-12AUG31-YANG/MINGYAN/P1` 护照字段用 `-` 分隔（现役正则只认 `/`）。修复：bareDocMatch 字段分隔 `\\/` → `[-\\/]`（捕获组结构不变，姓名内 `/` 与尾 `/P1` 照旧）。抽取魔法数 9314 因 BA 删 HTML 漂移——测试抽取起点改为 splitIntoBookings 精确锚定。

**测试**：parse_test 增至 20 断言——S17 拆双单；S18 公务单三段+¥39,822+YANG/MINGYAN 生日效期；S19 经济单 ¥17,662+YU/DONGHONG；S20 双单跨天段保留。全量回归 + 语法失败集=4 + 写手=2。

**改动位置**：bareDocMatch 一条正则。

**回滚**：.backups/ 上一版。
---
## 2026-08-12 — 解析器：跨天标 +1 修复 + 中文标签乘客资料块 · v26.08.12-BD

用户报行程识别问题（Sabre 可利用段样本）。探针定位两缺口：①**跨天标丢失**——TravelSky/Sabre/Amadeus 三族 segPatterns 的时刻捕获组 `(\\d{3,4}[AP]?)` 不认 `+1`，`1915+1` 被截成 `1915`（LAX→HKG 看似当天到，临近判断会错一天）——六处时刻组统一升级容忍 `+N`；②**中文标签乘客资料块**（姓名：/性别：/出生年月日：/护照号：/护照有效期：/护照签发国家：逐行）全落未识别——新增状态机分支：姓名行 `LI CHUNXUE → LI/CHUNXUE` 建档并锚定当前乘客，性别男女转 M/F，出生 `1994.02.13 → 13FEB94`，护照有效期容忍 `24/04 2035` 与 `13/01/2030` 两形态转 `24APR35`，护照号/签发国静默跳过。注：`ENOCH HU` 这类西式名序按原序转 `ENOCH/HU`（无 DOCS 佐证时不自动倒序，需人工留意）。

**测试**：parse_test 增至 16 断言——S13 三段+`1915+1` 保留+舱位 P；S14 LI/CHUNXUE 全字段；S15 三乘客含 2020 年幼儿；S16 运价 18384+未识别归零。全量回归 + 语法失败集=4 + 写手=2。

**改动位置**：segPatterns 六处时刻组；_cnPaxRef 状态变量；中文标签乘客块分支。

**回滚**：.backups/ 上一版。
---
## 2026-08-11 — 解析器：UA 官网中文行程详情（航段头/航班号分行 + 十二小时制） · v26.08.11-BC

新样本形态：UA 官网复制的中文行程详情。状态机支持：①**OD 总览行跳过**（含"次经停/直飞"+"时长"的汇总行不产假航段）；②**航段头暂存 → 航班号落座**——`上海(PVG)PVG至|旧金山SFO8月24日·下午12:10至上午8:55` 先暂存（容忍重复机场码、`至|` 分隔、括号可有可无），隔行遇 `航班号:UA858` 才入座，新头覆盖旧暂存；③**十二小时制转 24**（下午/晚上 h<12 加 12；上午/凌晨 12 点归 0）——`下午1:00→13:00`；④舱位行暂存随段落座入 cls；⑤**合计应付区**取最大 ¥ 为整单人民币（¥8,380 胜出 7,970/人 与 410 税费）；⑥时长/机型/排放/旅客数等噪音行显式跳过。

**测试**：parse_test 增至 12 断言——S9 两真段且无 PVG→EWR 假段；S10 段1 全字段（含 cls 与跨日界线到达 08:55 原样）；S11 段2 下午进位；S12 rmb=8380 · airline=UA。全量回归 + 语法失败集=4 + 写手=2。

**改动位置**：解析器状态变量三枚；行循环 UA 详情分支。

**回滚**：.backups/ 上一版。
---
## 2026-08-08 — 删除订单修复 + 全代码体检清理 · v26.08.08-BB

**删除订单故障根因（自认）**：BA 摘除 Firebase 主区时带走了 `_recordDeletedOrderId` 与 `loadDeviceName` 的定义而调用残留——摘除断言只查了符号字符串清零，未查"调用-定义"配对。点「永久删除」→ ReferenceError 当场崩；init 尾部第一句 loadDeviceName 崩 → 设置区状态恢复/AI Key 状态/每日备份提醒整段断链（"故障太多"的体感来源）。

**修复与体检**（全局孤儿调用扫描器照单）：①重建 `_recordDeletedOrderId` 本地版（墓碑休眠账本，封顶 800）——删除功能复活；②删 init 的 loadDeviceName 死调用——init 尾部链恢复，备份提醒复活；③笔误修复：`renderCardholders` → `renderCardholdersList`（该刷新点因 typeof 保护静默失效已久）；④**密文残端体系整摘**：tryAutoDecryptCloudKey / unlockAnthropicKey / pw 三函数与常量 / 保存流程同步密码询问段 / updateAiKeyStatus 云端分支与 🔓 按钮——保存与状态一律「仅本设备」，历史密文字段遇到即清，同步 checkbox 停用；⑤删死函数 `_packPick`（AS 后孤儿；_packPickIdx 为现役，不受影响）；⑥误报澄清：purgeRollingBackups / checkDailyBackupReminder 为已自执行具名 IIFE（活）。**审计基线固化**：语法失败集 expected="4"；孤儿断言组纳入发货链。

**测试**：pack/parse/fx/wrap 全量回归 + 写手=2 + 孤儿断言组 + 语法失败集=4。

**改动位置**：墓碑重建；init 两行；笔误一处；密文体系六处；_packPick。

**回滚**：.backups/ 上一版。
---
## 2026-08-08 — 摘除 Firebase：本地为唯一数据源 · v26.08.08-BA

用户决定：Firebase 功能一直未用，取消。**物理摘除**：SDK module script、设置区 🔥 同步面板、FIREBASE FIRESTORE SYNC 主区（config/init/onSnapshot/fbApplyRemote/fbScheduleWrite/fbPushNow/fbHookPersistence，含 AZ 的截图剥离与回填）、init 启动段、全部 fbScheduleWrite 调用行（正则清扫 + storageSet 尾钩子块）。**连锁立法更新**：pending 键唯一合法写手由三收敛为二（persistOrdersNow / persistOrders 防抖体）——fbApplyRemote 豁免直写随体摘除，审计不变量 `grep -c = 2`；咽喉 _stampChangedOrders 的 applying 恒 false（仲裁结构保留）；_lastEdit 落章与 deletedOrderIds 墓碑转休眠字段（无害保留，恢复同步或备份合并可复用）。**API Key 子系统**：落实"key 不上云"既有决策——删 syncAnthropicKeyToCloud 与保存流程云分支（一律「仅本设备」），tryAutoDecryptCloudKey 保留并接入启动（导入带密文备份仍可自动解密）。**急救链改造**：storageRescue 由"清本地→云端拉回"改为「①自动下载全量 JSON 备份 → ②清缓存刷新 → ③导入备份找回」；🧹 清理文案同步；每日备份提醒地位升级为唯一兜底。**影响声明**：多设备不再互通（各设备独立数据，跨设备迁移用 导出/导入备份）；Firestore 安全规则悬案（原全局风险第一位）就此销案。

**测试**：pack/parse/fx/wrap 全量回归 + 全文 fb 符号清零断言（firebasejs/FB_DEFAULT_CONFIG/fbScheduleWrite/fbPushNow/fbApplyRemote=0）+ 写手=2 不变量 + AES/PW 依赖存活断言。语法块数因移除 module script 较基线少一块，属预期。

**改动位置**：SDK/设置区/主区三段切除；storageSet 尾；咽喉 applying；API Key 流程；init；storageRescue/清理文案。

**回滚**：.backups/ 上一版。
---
## 2026-08-08 — 白屏防护与存储瘦身：三层防护 + 云端截图剥离 · v26.08.08-AZ

用户报浏览器白屏（疑缓存上限）。诊断：localStorage ~5MB 配额被订单截图 base64 与无限增长的出票记录顶穿；更严重的隐患是 Firestore 单文档 `ticket-organizer/main` 有 **1MiB 硬限**，整包含截图推送时 setDoc 抛错仅 console.warn——云同步可能早已静默失败。手术：①**急救双保险**——全局 error 监听：首屏渲染成功（renderPendingList 置 _bootOk）之前任何未捕获异常 → 顶部红色急救条「⚠️ 启动异常 + 🧹 清理缓存并从云端恢复」（storageRescue 清 pending/settings 两键后刷新，云端自动拉回）；②**云端推送剥离截图**——fbPushNow 的 orders 去除 screenshots（_ssCount 占位），截图改为设备本地资料不再上云（1MiB 硬限所迫）；fbApplyRemote 合并后从本地旧值回填，remote 胜出不丢图；③**出票记录封顶 400**（三条出票路径 unshift 后统一裁剪，更早记录以 Excel 月度备份为准）；④**新截图压缩收紧** 1200px/0.80 → 900px/0.72（存量不动）；⑤**🧹 一键释放**（头部 🌙 旁）：每单截图保最新 2 张（标 _ssTrimmed）/出票记录裁 400/清 -corrupt 隔离键，前后用量对比 toast；⑥启动 4s 后用量巡检（≥80% 黄提示）。

**白屏设备恢复步骤**：推送本版后 Ctrl+Shift+R 强刷即可（HTML 从 Pages 拉取，崩的是旧 JS）；若仍异常会出现红色急救条，点按钮即从云端恢复。

**测试**：pack/parse/fx/wrap 四套全量回归 + 语法基线一致 + pending 三写手不变量 ✓ + 封顶×4/_ssCount/急救条 grep 断言。

**改动位置**：存储健康模块；renderPendingList 置位；fbPushNow/fbApplyRemote；三处出票封顶；compressImage 默认参；头部 🧹。

**回滚**：.backups/ 上一版。
---
## 2026-08-08 — 解析器：中文混排 PNR 五处补强（含 DOCS 脏数据容错） · v26.08.08-AY

用户报解析问题（国内代理风格中文混排样本，规则与 AI 兜底均失败）。五处补强：①**中文航段行**新分支——`1. UA772 08月17日 北京首都 - 洛杉矶 12:00 09:30` → 航班/日期(17AUG)/城市（经 cnCityToIATA 转三字码，缺映射保留中文）/起降时刻（+2 跨天保留）；②**USD 带运价码分价行**——`成人：01 PPX03PTP+* 5695.23 USD`（序号前缀+运价码+可选 CNN/CHD/INF+USD），入 parsedFareByType（×rate 转 RMB 口径），顺带覆盖了此前挂账的运价码 `+*` 尾巴形态；③**乘机人 CHD/INF 尾巴剥离**——`4.XUE/MIAWISEMAN CHD` → 名剥后缀 + forcedType（儿童价铺设自动生效）；④**DOCS 点号姓名**——`ZHANG.JINHE/P1` 两条 DOCS 正则姓名分隔改 `[/.]`；⑤**DOCS 同姓模糊合并**——`XUE/FUQIN` 与名单 `XUE/FUQIU`（录入误差）按同姓+前缀/编辑距离≤1 合并，生日与护照效期不丢，SSR 行 given 尾 CHD 同步剥离并回填 forcedType。

**测试**：重建 parse_test（历史配方修正：真函数头 9204 splitIntoBookings，单订单入口 parseSingleBooking）——用户样本 8 断言全绿（四乘客/CHD/模糊合并生日+效期/点号 DOCS/三航段/中文城市转码/跨天/成人儿童 USD 分价）+ pack 34/fx/wrap 全量回归 + 语法基线一致。

**改动位置**：乘机人提取体；中文航段分支；USD 运价码分价分支；DOCS 两正则；SSR 合并体。

**回滚**：.backups/ 上一版。
---
## 2026-08-07 — 出票前价格确认降级：不再阻断，对账时最终核实 · v26.08.07-AX

用户指令：价格最终在对账环节核实，出票前的重复价格确认取消。改动：出票入口的结算校验（刷卡金额必填、刷卡vs运价 ±1.5% 限差、-5% 仓位警告、卡主/刷卡美金/折扣/里程/CPP/积分方式/直折/直接结算必填）**由 alert 阻断＋滚动定位 降级为轻 toast 放行**——missing 照常收集，提示「结算信息待补 N 项（前两项… 等）· 对账前核实即可」，**不再 return**，照常进入出票弹窗。保留：多行程方案 confirm（防出错票，非价格）、出票弹窗本身（卡主记账用）。

**测试**：pack 34 项/fx/wrap 全量回归 + 语法基线一致 + 「无法标记为已出票」文案清零断言。

**改动位置**：出票入口 missing 阻断段一处。

**回滚**：.backups/ 上一版。
---
## 2026-07-31 — 收价机制：付给卖分卡主 = 组合合计 × bp%（新卡主默认 35%） · v26.07.31-AW

接入收价（bp）机制：①快配条新增「收价 %」输入（默认 35，会话记忆，改动即时重算）；②每个组合行尾直接显示 **付 $X**（=组合实际合计 × bp%，_packPayUsd 精确到分）；③复制文本追加 `｜付 $X（35%）`（quick/single/multi 三路一致）；④多账户结果每户行带付款、summary 带付合计、「复制全部方案」逐行带付款；⑤💠 选定章存入 bp 与 pay，待出票徽章悬停可见 `付 $1,939（35%）`。**假设声明（错了再改）**：付款基数按组合实际合计而非报的额度；暂只显示美元（要折 ¥ 说一声，挂多源汇率即可）。

**测试**：pack_test 增至 34 项（Q7 付款三例含分位精度）+ 全量回归 + 语法基线一致。

**改动位置**：_packPayUsd；快配条收价输入；组合行/复制文本/multi 三处付款；packTag bp/pay 与徽章 title。

**回滚**：.backups/ 上一版。
---
## 2026-07-30 — 💠 选定方案自动标识待出票 · v26.07.30-AV

配账闭环最后一步：①快配与高级单模式每个组合行新增「✓ 选定」，多账户结果新增「✓ 全部选定」——选定即给组合内订单盖 packTag（航司/额度/合计/账户名/时间戳），逐单 _lastEdit 落章走 persistOrdersNow 咽喉，跨设备同步；②待出票行新增 **💠 配账** 徽章（蓝系，档位在 🔒等放位 之后 ⏸无位 之前，纳入 _capRowBadges 封顶体系），悬停显示方案详情（【UA】≈$5,540 · 账户名 · 07-30 18:32），**点击徽章可解除**（confirm 后删标+落章）；③已标记单在快配 A 段淡显 💠 且**默认退出计算**（on=false，防止同一单许给两个卖分人；展开 A 段可手动重新勾入）；④选定后对话框与待出票列表即时刷新。

**测试**：pack 33 项/fx/wrap 全量回归 + 语法基线一致 + pending 三写手不变量 ✓ + markPackChosen/clearPackTag/💠 grep 断言。

**改动位置**：行徽章 packBadge 与数组档位；markPackChosen/markPackMultiChosen/clearPackTag；组合行与 multi 按钮；items tagged/on 默认。

**回滚**：.backups/ 上一版。
---
## 2026-07-30 — ⚡ 快配：一个数进去，答案出来 · v26.07.30-AU

用户反馈"使用起来不方便"。诊断：现实工作流是微信上有人报一个额度，需要的是"敲进去立刻出组合"，而不是管理账户列表→选目标→按计算。**重排信息架构**：①对话框以「⚡ 额度」输入框为主角，打开即聚焦，**边输边出结果**（220ms 防抖；支持 55万/8160/100k；≥2 万自动按积分、否则按美元，自动/分/$ 三粒可手动锁定）；②结果为按航司隔离的合并排名（复用 _packSingleMerged），首屏 6 组+展开，行内一键复制（格式 ≈$额度｜【航司】组合=合计｜超/差）；③A 段折叠为一句摘要（参与 N/M 单·合计 $X），展开才见勾选行，勾选变动即时重算；④多账户自动分配与临时账户池整体收入「高级」折叠区，功能与格式原样保留（目标下拉/六种子/复制全部方案/⚠计数）；⑤排序/超额/警戒提到快配条，改动即时重算；⑥单航司超 40 单跳过并明示。runPacking 结果改定向写入 #packAdvRes。

**测试**：pack_test 增至 31 项——Q 组六断言：大数判分/小数判$/手动锁定/后缀/空输入/合并纯净+冠军+停用单排除。K/M/N 全量回归 + fx/wrap + 语法基线一致。

**改动位置**：对话框层整段重写（_packCapC/_packQueueCalc/_packQuickCalc/_packComboRowsHtml/copyPackingQuick/_renderPackingDialog/runPacking）；openPackingDialog 初始化。

**回滚**：.backups/ 上一版。
---
## 2026-07-30 — 配账航司隔离：一个账户只吃一个航司 · v26.07.30-AT

业务规则：账户返现按航司锁定，混航司组合无效。**全链路航司隔离**：①items 携带 al（航司，缺省 '—' 独立成组）；②新增 _packGroupByAl/_packBestDistAl/_packBestFillAl——按航司分组分别求解取最优（平手律沿用：不超优先、单数少）；③multiSolve 三处求解（种子/单户重解/seqSolve 双三户重排）全部改走航司隔离版，换航司时旧单自然整体回池；④单账户模式撤航司混算——逐航司各算 top-100 合并全局排名，组合行前挂航司徽章，哪个航司最贴该账户一眼可辨；⑤多账户结果每户名旁挂所吃航司徽章；⑥复制格式（单组合与全部方案）均带【航司】标；⑦顶部说明标注隔离规则。

**测试**：pack_test 增至 25 项——N 组四断言：禁混航司（混合零偏差解必须弃选）、同航司凑满、多账户航司纯净+零偏差+池空、fill 不混。K/M 组全量回归 + fx/wrap + 语法基线一致。

**改动位置**：openPackingDialog items al；隔离求解器三件；multiSolve/seqSolve 内部；runPacking single 逐航司合并；渲染徽章与复制格式。

**回滚**：.backups/ 上一版。
---
## 2026-07-30 — 配账上限 20→40：折半枚举引擎 · v26.07.30-AS

用户问"为什么不能超过 20"。20 是全枚举 2^n 的物理墙（22 单=420 万组合、30 单=10 亿浏览器冻死），继承自 v4 原件。本版新增**折半枚举（meet-in-the-middle）**：订单劈两半各枚举（B 半按合计升序、同额去重保最小单数），二分对拼——2^40 空间只花 2×2^20 代价，**上限抬到 40 单**。路由：≤20 单走原全枚举（行为逐位不变），>20 自动切折半。bestDist/bestFill 折半结果**精确**（冠军与全枚举一致），top-100 前排精确、深尾近似（每 A 半探 ±12 邻位；同额组合去重后每额度保留一种代表组合）。multiSolve 内部三处求解与种子全部改走自动路由（结果从位掩码改索引数组，>32 位安全），双户重排候选上限 20→24、三户 14→18。

**测试**：pack_test 增至 21 项——新增 M 组交叉验证：16/17/18 单随机 ×3 轮 bestDist/bestFill/top 冠军 折半=全枚举逐项一致＋idxs 自洽；26 单埋 5000+3160=8160 精确对折半必中 dist=0。K1-K8 原精确路径回归全绿 + fx/wrap 回归 + 语法基线一致。

**改动位置**：_packMitmHalf/_packLB/_packMaskIdxs/_packMitm{BestDist,BestFill,Top}/_packAuto*/_packPickIdx；multiSolve/seqSolve 内部路由；runPacking single 与渲染/复制改 idxs。

**回滚**：.backups/ 上一版。
---
## 2026-07-30 — 配账改速算器：B 段临时账户池（收分来源随机，不绑卡主名册） · v26.07.30-AR

用户反馈：提供积分的卡主是随机的，只需要快速匹配金额。**撤销卡主名册绑定与分配闭环**：①B 段改为会话级临时账户池 _PACK_ACCOUNTS——名字可选＋积分＋启用开关＋删行，「+ 添加账户 / 批量粘贴 / 清空账户」，批量粘贴按 v4 规则取每行最后一个数字（支持 万/w/k/千 后缀，名字自动剥离行尾冒号），会话内保留、关弹窗不丢、不写 settings；②删除 cardholder.pointsBalance 落库与 applyPackingCombo/applyPackingMulti/_assignPackPicks 分配闭环（清零断言），组合动作改「复制」——单组合复制 v4 行格式，多账户保留「复制全部方案」；③**修 bug**：ENUM_CAP=20 守卫提到双模式公共段（原多账户模式无守卫，23 单会分配 2×8M Float64Array 内存爆炸）；④目标账户改索引寻址（仅列启用且积分>0 的行）；⑤桥 payload 带行开关状态。

**测试**：pack_test 增至 10 项（新增 K8 批量行解析：名字剥离/万k 后缀/无数字行跳过）+ fx/wrap 回归 + 语法基线一致 + 三个闭环函数清零断言。

**改动位置**：_PACK_ACCOUNTS/_packParseBulkLines；openPackingDialog B 段；渲染 accEdit/accOptions/批量粘贴区；runPacking 守卫与寻址；copyPackingCombo。

**回滚**：.backups/ 上一版。
---
## 2026-07-30 — 配账引擎对表 v4 原件：整数分/平手律/六种子局部搜索/互通桥 · v26.07.30-AQ

用户上传独立计算器 points_combo_calculator_v4.html（权威规格）。AP 系照截图逆向，本版逐条对表采纳 v4 语义：①**整数分（cents）记账**防浮点（Float64 lowbit DP 子集表）；②knapTop **K=100**，比较器双模式——最贴近: 距离→**不超优先**→单数；订单最少: 单数→距离→不超（AP 缺不超平手律）；首屏 10 个 +「展开全部 100+ 个方案」；③单模式空可行 v4 文案（最小单已超额度＋允许超额）；④**多账户弃 FFD 改 multiSolve**：六种子（额度降/升/原序 × bestFill/bestDist 种子）各跑 12 轮局部搜索（单户重解/双户双序重排 cand≤20/三户六序重排 cand≤14）取贴近偏差合计最小，结果映射回 B 段原顺序，补 ⚠超警戒户计数与「复制全部方案」（v4 行格式）；⑤_packParseNum 支持 万/w/k/千 后缀备用；⑥**互通桥**「↗ 送独立计算器」——按 v4 的 #data=Base64(JSON) 协议（orders 含 id 回传位/accounts/rate/allow/pct/mode/autorun:1）打开同站点 points_combo_calculator_v4.html，把 v4 文件放进仓库即完成双向互通。旧 _packEnumerate/_packRank/_packMulti 全文清零断言。

**测试**：pack_test 重写 9 项（真实 13 单 cents 冠军复核/双排序律/over 平手/bestFill/双种子完美装箱构造例/后缀/分级精度）+ fx/wrap 回归 + 语法基线一致。

**改动位置**：引擎区整段置换（_packParseNum…_packMultiSolve）；runPacking 双模式；渲染展开/空态/multi summary；copyPackingPlan/sendToComboCalc。

**回滚**：.backups/ 上一版。
---
## 2026-07-30 — 🧮 配账结算：未分配订单 × 卡主积分额度装箱，一键分配 · v26.07.30-AP

按用户独立「订单配账计算器」窗口的规格移植进本体并接通闭环。**A 段**自动灌入未分配卡主的待出订单（USD，勾选参与）；**B 段**为卡主积分额度（新增 cardholder.pointsBalance 字段，对话框内可编辑并落库；全局换算 settings.pointsPerUsd 分=$1，默认 100）。**单账户凑满**：≤20 单穷举子集（>20 提示关单保护），可行=组合 ≤ 额度+允许超额（默认 $100），排序 最贴近/订单最少，超额 ≥警戒%（默认 1%）标 ⚠，结果前 10 组各带「应用此组合 →」——**一键把该组订单全部分配给目标卡主**（cardholderId + _lastEdit 落章 + persistOrdersNow）。**多账户自动分配**：FFD 贪心（大单先入余量最大账户），放不下的明示，「全部应用 →」按方案批量分配。入口：面板 🧮 配账（⚡MAX 旁）。

**测试**：pack 7 项——用用户截图的真实 13 单/816,000 分账户断言：可行边界、最贴近榜首、已知优解 5793+2373=8166（超$6 未达警戒 81.6）入前二、最少单模式单单最优 7311、FFD 装箱与溢出。+ fx/wrap 回归 + 语法基线一致。

**改动位置**：面板按钮；_packEnumerate/_packRank/_packMulti 纯函数；配账对话框与两条应用路径。

**回滚**：.backups/ 上一版。
---
## 2026-07-30 — 🌙 收工小结：一天的句号 · v26.07.30-AO

用户问"还能怎么提升幸福感"。诊断：瓶颈不在缺功能，在①收不住工②成果看不见③背景焦虑。本版先做最值的①+②：头部新增 🌙 按钮 → 收工小结弹层——**今日成绩单**（出票单数/收入¥$/服务乘客数，按 ticketedAt 当日聚合）+ **明日预览**（待出单数/🔥加急/🔒等放位/最早首飞：DDMMM 解析取最近未来班，已过归明年）+ **收工语**（按当日形态五种语气：硬仗/清空/铺垫/平静/如常）+ 白鸽轻礼。点遮罩或「晚安 →」关闭。纯函数 _dayWrapStats/_dayWrapLine 可测。

**幸福感菜单（待拍板，未实施）**：①早安摘要（当日首开自动展示今日战况）；②加急临期浏览器通知（免于反复盯屏）；③深夜深色模式（23 点后自动，需截图协作迭代，盲改风险高）；④月度回顾（单量/收入/最远航线，意义感非压力型）；⑤同步健康角标（回滚创伤后的安心指示）。建议顺序 ①②⑤。不建议：打卡连胜类游戏化（压力型）。

**测试**：wrap 6 项（当日聚合/昨日排除/三态计数/最早航班跨年解析/收工语）+ fx 回归 + 语法基线一致。

**改动位置**：头部 🌙 按钮；_dayWrapStats/_dayWrapLine/showDayWrap（放松层模块内）。

**回滚**：.backups/ 上一版。
---
## 2026-07-30 — 放松层：航班天气 + 出票彩带 + 清零白鸽 · v26.07.30-AN

用户要求按订单积累程度加入放松动画。设计原则：**放松来自完成的仪式感而非喧闹**。三件套：①「航班天气」底部环境层——待出订单越多，缓缓漂过的 ✈️ 越多（0→🕊️ 白鸽 zen；1-3→1 架；4-7→2；8-12→3；13+→封顶 4 架，速度随桶微增），pointer-events 全穿透绝不挡操作，桶未变不重建（防动画重启抖动）；②三条真·出票路径（批量在 forEach 后只爆一次/划卡/主流程）成功瞬间彩带迸发 🎉✨🎫✈️ 约 1.5 秒自散；③待出清零触发白鸽大庆 + 「辛苦了，歇一会儿」。🎐 右下角标一键开关，偏好存 settings.ambientFx 随云同步；尊重 prefers-reduced-motion（系统减动画则完全静默）。

**容器重置记录**：本轮容器被重置（/home 与 /tmp 清空），已按守则从 /mnt/user-data/outputs/repo-update.zip 自恢复 AM 全量并重建 ship.sh；历史 27 个测试文件损失，本版以 fx 10 项断言 + 全文语法基线比对代偿，后续版本按需重建相关测试。

**测试**：fx_test 10 项（桶映射 8 + 开关尊重 2）+ 语法基线一致 + console.log/重复函数零。

**改动位置**：keyframes fxDrift/fxFall；renderPendingList 前功能模块与首行挂载；三处出票成功挂钩。

**回滚**：.backups/ 上一版。
---
## 2026-07-27 — OFAC 查询闸门修正：中文拼音短名不再被跳过 · v26.07.27-AM

用户报 ZHU/YU 无绿标。根因：isNameQueryable 除"≥2 词且每词≥2"外另设**总长≥6 字母地板**——ZHU YU(5)/LI NA(4)/HE YU(4) 这类真实拼音名被系统性跳过（合规空洞：绿条计入"姓名过短未查询"、P 行 chip 静默 return '' 留白）。修复：①撤总长地板，规则改为 ≥2 词且每词≥2 字母（仍挡 "ZHU Y" 类输入碎片；单词名照旧不查）；②仍不可查的姓名渲染灰色虚线 `– OFAC` 占位标（title：补全姓名后自动比对），P 行永不留白；③既有缓存为 idle 的短名下次打开自动补查（checkOFACDebounced 只跳过已定态）。绿条跳过计数沿用同一闸门自动收敛。ZHU/YU 部署后重新打开即自动比对出结果。

**测试**：gate 6 项（ZHU YU/LI NA/长名可查；单词/碎片/空不查）+ ofac 7 项 + 27 文件全量回归全绿。

**改动位置**：isNameQueryable；buildOFACInlineChip 不可查分支。

**回滚**：.backups/ 上一版。
---
## 2026-07-27 — OFAC 人工放行完全体：跨设备同步 + 时间戳 + 撤销传播 · v26.07.27-AL

用户要求增加 OFAC 人工 clear。现状勘察：确认/撤销/拦截豁免/绿条计数的骨架已存在，但**只存 localStorage（本机）、无时间戳**——多设备工作流下等于没有。本版补全：①存储迁至 `settings.ofacConfirmed = {key: 时间戳}`（正=放行时刻、负=撤销时刻），随 settings 走 Firebase 同步；②双侧合并新增 _mergeOfacConfirmed——**每键取绝对值更大者**，撤销也能跨设备传播、旧值不倒灌；③确认条显示「人工放行于 MM-DD HH:mm」（审计可视）；④撤销加 toast；⑤遗留 localStorage 记录启动一次性吸收进 settings 并删除旧键；⑥确认弹窗文案改为"将同步到所有设备并记录放行时间戳"。出票拦截豁免、绿条"N 名匹配后已人工确认"计数沿用既有逻辑自动受益。STATE.ofacConfirmed 全文清零断言。

**测试**：ofac_test 7 项（遗留吸收/放行态含时刻/撤销负值/再放行/合并三律，夹具键经 normalizePaxName 规范化）+ 27 文件全量回归全绿。

**改动位置**：getOFACStatus；confirmOFACClear/confirmOFACForOrder/unconfirmOFAC；loadOFACConfirmations 重写；_mergeOfacConfirmed 与双侧 settings 合并；确认条时刻。

**回滚**：.backups/ 上一版。
---
## 2026-07-25 — 复检：同步完整性专项全绿，堵住备份恢复野路子写手 · v26.07.25-AK

用户要求再检测。基础卫生全绿（语法基线一致/console.log·debugger·TODO·重复函数·真死函数全零/27,886 行 1.31MB）。**新增同步完整性专项**（AF/AJ 仲裁链逐环审计）：落章咽喉 2/2、影子刷新 2/2、首拉闸门置位 3+守卫 1、墓碑登记 2+过滤 2+settings 同步 2、合并方向拉取 remote 先/推送 local 先、_lastEdit 章 7 处、迁移纯度 0 persist、applying 旗配对正常（计数含声明行）。**唯一发现并已修**：`importAllData`（JSON 备份恢复）raw 直写 pending——无章无影子，恢复的数据一同步反而会被云端平局冲掉（恢复等于白恢复）。修复语义：恢复=用户显式全量本地编辑 → 逐单盖新章（仲裁获胜）→ 刷影子基线 → 经 persistOrdersNow 咽喉写入。**立约（可审计不变量）**：pending 键唯一合法写手 = persistOrdersNow / persistOrders 防抖体 / fbApplyRemote 豁免直写，共 3 处——本版起复检脚本以 `grep -c = 3` 把关。

**测试**：不变量断言 + 27 个测试文件全量回归全绿。

**回滚**：.backups/ 上一版。
---
## 2026-07-25 — 二次回滚修复：持久化咽喉全量自动落章 · v26.07.25-AJ

用户在 v26.07.25-AI（部署确认）上再报严重回滚。**根因是 AF 的设计错误（本人）**：拉取侧平局改判云端治了陈旧设备，但 _lastEdit 只盖了 7 个改动点——其余上百条编辑路径无章，多设备实时同步下，活跃设备的在编内容会被任何一次远端快照（他机推送或回声之外的写）在 400ms 防抖窗口内平局冲掉；AF 当时把 P2 风险评估为"失效方向安全"是错的。修复改为**咽喉级脏检测自动落章**：`_persistShadow` 影子基线（排除 _lastEdit 的 JSON 快照）+ `_stampChangedOrders` 在 persistOrders/persistOrdersNow 两个落库咽喉逐单比对——任何路径改了任何单，落库瞬间自动盖章，零散点维护；**远端应用期间刻意不盖**（防"最后接收者赢"的时间戳通胀），仅在 fbApplyRemote 落库后刷新基线；启动在迁移后播种基线（派生字段并入基线不算编辑）。AF 的 7 处手动章保留（显式意图，无冲突）。**运维提示**：AJ 全设备部署完成前建议单设备操作；今日被冲掉的编辑需人工补录，系统无法追溯。

**测试**：stamp_test 7 项（播种不盖/改动即盖/戳不自激/应用期不盖且基线收编/新单盖章）+ 全历史回归全绿。

**改动位置**：persistOrdersNow 前新增影子三件套；两个落库咽喉挂钩；fbApplyRemote 落库后刷基线；启动播种。

**回滚**：.backups/ 上一版（注意：回滚将重新暴露活跃编辑被冲风险）。
---
## 2026-07-25 — UI 舒适度：行徽章优先级封顶 +N 折叠；字号菜单销案 · v26.07.25-AI

用户拍板执行 -W 美化菜单。**②行徽章封顶（主刀）**：紧凑行二十余种徽章按优先级排序——身份(表Y) > 安全旗(⛔满座/禁拆 > 🔒等放位 > ⏸无位 > ⚠资料 > 黑名单) > 状态(临近/混舱/AI) > 资讯(票面/序票/关联) > 历史——**最多展示 4 枚**，其余折叠为 +N 灰章，悬停列出全部被折叠标记（纯函数 _capRowBadges，标签自动从徽章 HTML 提取）。姓名行从此不再被徽章挤爆。**①字号收敛销案**：普查证实 9.5/10.5/11.5/12.5px 半像素档成建制使用（34/31/37/19 处）——是刻意的微调排版体系而非杂乱，不动；仅两处孤档 8.5px→9px、一处 19px→20px 并入邻档（±0.5~1px 无感）。

**测试**：_capRowBadges 6 项断言（直出/封顶/计数/悬停列表/空安全）+ 全历史回归全绿；8.5px 残留清零。

**改动位置**：_capRowBadges 新增；行徽章插值改优先级封顶；两处字号孤档。

**回滚**：.backups/ 上一版。
---
## 2026-07-25 — 同步进单也过标记迁移 + 部署核对升级为第一道门 · v26.07.25-AH

用户再报「禁止拆单没有识别到」（样例含 表Y头行/尾部===/禁止拆单 [炸弹]）。全链实测（分块→方案→解析）第三次证明代码路径完整：noSplit=true urgent=true 同出。**双因处置**：
**R1 部署疑云**：卡片"加急有、禁拆无"恰是 -T 之前构建的特征指纹（urgent 为老功能）。已要求用户核对线上徽章——若 < v26.07.22-Z，则 T→AG 十余版均未生效，四轮"没识别"全部自洽。**运维规矩（跨会话）：此后一切"功能没生效"类报告，第一步先核对线上徽章，再排查代码。**
**R2 第五层真实缺口（已修）**：多设备下若**贴单设备为旧版**，其产出订单无 noSplit/seatCount 字段，经 Firebase 同步至新版设备——而迁移只在启动时跑，远端合并中途进来的单不过迁移。修复：fbApplyRemote 订单合并后同样调用 _migrateMarkersFromRawPnr（纯内存、不落库不推送，遵守 07-24 回滚事故守则）；空工作区守卫顺序同步理顺。**运维规矩：贴单的设备必须同样升级到最新版。**

**测试**：全链样例实测 + mig/sync + 全历史回归全绿。

**回滚**：.backups/ 上一版。
---
## 2026-07-25 — 收分模式开关：默认整单，⚡MAX 一键全拆单 · v26.07.25-AG

按用户要求重排收分默认哲学：**默认整单模式**——所有多人单（含儿童/受约束单）输出纯一行 `Buying NK AMEX BP-XX`，不标多人、不给组合/配对标注、无英文脚注（把满座的呈现推广为常态）；**⚡MAX 按钮**（收单需求旁，仅选择模式内可点，会话级、进入选择模式自动复位）一键切换为既有的全拆单标注体系（组合小计/1A+1C 配对/no split 标注/脚注照旧生效）。**双击变种两模式均可用**——它是"特殊情况点名拆"的显式动作：默认模式下被双击的单单独展开各档、其余保持纯行、仍无脚注。_buildBuyingList 增 maxMode 第三参；历史测试全部补传 true 保持既有语义断言（正则补参在嵌套括号处打滑，六处带 Set 调用二次精准修补后全绿）。

**测试**：test11 三项（默认整单逐字符/默认+变种例外展开/MAX 复现完整标注）+ 全历史回归全绿。

**改动位置**：buyMaxBtn 按钮；BUY_MAX 状态/toggleBuyMax；copyBuyingList 三处串接；_buildBuyingList 模式分支与脚注条件。

**回滚**：.backups/ 上一版。
---
## 2026-07-24 — 数据回滚事故修复：同步仲裁四层加固 · v26.07.24-AF

用户报"数据发生回滚"。根因链（全部实证）：①逐单仲裁字段 `_lastEdit` **从未被任何代码写入**——合并一直是"平局本地赢＋按 id 并集"，删除无墓碑（删单会被任何旧设备的推送复活）；②-AD 启动迁移在 Firebase 首拉前 persistOrders → 触发推送，**陈旧设备一开机即以旧数据清洗云端**，把偶发病变成流行病（AD 触发角色成立，本条为其修正）。四层修复：
**L1** 迁移改纯内存（启动期永不触发推送；逐设备幂等派生，待真实编辑随手落库）。
**L2** 首拉闸门 `_fbInitialPullDone`：首次云端拉取完成前 fbScheduleWrite 一律不推；离线失败也放行（fbPushNow 自带读-合并-写兜底）。
**L3** 拉取侧合并翻转为 **remote 先入座**：平局/无章判云端——陈旧设备开机即采纳云端真相；本地真正较新的编辑因落章仍胜。推送侧保持 local 先（配合落章语义正确）。
**L4** 删除墓碑：`settings.deletedOrderIds`（封顶800、两侧 settings 合并同步、两侧订单合并后过滤），"删了又复活"关门。
**L5** `_lastEdit` 首次真正落章：新单装配、_syncOrderMarkers（覆盖 doReparse/弱更新/强跳过）、五个开关（禁拆/满座解除/无位/放位/航司补码）共 7+ 处；其余散点编辑路径列入 P2 待补，当前 L3 已保证陈旧设备不再回滚。

**数据恢复指引已在会话中给出**：最新数据所在设备勿刷新，先导出 JSON 与出票记录备份。

**测试**：sync_test 6 项（平局云端赢/落章本地胜/墓碑登记去重过滤/封顶）+ mig_test 改纯内存断言 + 全历史回归全绿。

**回滚**：.backups/ 上一版（注意：回滚本版将重新暴露数据回滚风险）。
---
## 2026-07-23 — 收分语义修正：等待放位照收，暂时无位不收 · v26.07.23-AE

用户修正业务语义（推翻 -M 的等待放位排除）：**等待放位**=代理手里有位只是尚未释放，积分早晚要用 → 照常进收分池；**暂时无位**（noAvailability，⏸标记）=代理手中也没位 → 不收分。改动三处：_buyEligibleEntries 过滤对调（waitingForRelease 放行、noAvailability 排除）；_buyIneligibleToast 给「暂时无位」具体原因（恢复有位后自动回归）；空批说明同步。等待放位单在收分文本中无特殊标注（卖家侧无关）。恢复有位（再点⏸）即自动回归收分池。

**测试**：buy_test7 按新语义重写 5 项（放位收/无位排/恢复回归/两类提示）+ 全历史回归全绿。

**改动位置**：_buyEligibleEntries；_buyIneligibleToast；copyBuyingList 空批提示。

**回滚**：.backups/ 上一版。
---
## 2026-07-23 — 存量单标记回填：启动时依据 rawPnr 一次性迁移 · v26.07.23-AD

用户报满座单收分仍显示多人组合且无标记。全链实测（splitIntoBookings→splitChunkByProposals→parseSingleBooking）证明 AC 的新粘贴路径完整无缺（「第1单」头部行保留、seatCount=4/4人）——问题订单为 **AC 之前入库的存量状态**（无 seatCount 字段）。根治而非再叫用户重贴：新增 _migrateMarkersFromRawPnr，**启动时对 noSplit/seatCount 为 undefined 的订单，依据其 rawPnr 原文回填两字段**（正则与 parseSingleBooking 内保持一致，互为镜像必须同改）；迁移后写 false/null 防重复扫描；有变化即落库并 toast「♻ 已为 N 个历史订单回填 禁拆/满座 标记」。waitingForRelease/urgent 刻意不迁移（手动状态/倒计时语义）。手动改过的订单（字段已定义）不受影响；云端同步来的旧形态订单于下次刷新迁移。

**测试**：8 项迁移断言（满座/双标记/干净落定/已迁移跳过/无原文/落库一次/二次零操作）+ 全链满座实测 + 全历史回归全绿。

**改动位置**：clearSeatTight 前新增迁移函数；启动序列 restored 块后挂载。

**回滚**：.backups/ 上一版。
---
## 2026-07-23 — 智能满座判定：座位数=乘客数 → 按 1 个订单处理 · v26.07.23-AC

用户样例：「刚好4个位置」+ 4 名 SSR 乘客。新语义链：解析层提取 seatCount（限定词+数量+座位名词双条件正则：刚好/只有/仅/正好/最后 + N + 个位置/座位/席位/位，实测不误触 舱位/放位/价位）→ 走 noSplit 同管道（八返回/装配/同步器三线）→ **派生判定 _isSeatTight**（seatCount≥2 且 =当前乘客数；改乘客自动失效，不留死旗）。生效面：收分需求**纯一行不标多人**（`Buying 87K AMEX BP-UA`，压过组合小计与儿童配对，最高优先）；脚注排除；变种双击拦截「满座…按整单处理」；卡片 🎯满座整单 红横幅（含 ✕解除=清 seatCount）+ 紧凑行 ⛔满座 章（与 ⛔禁拆 同槽不重复）；装配即时 toast 明示自动判定。座位数≠乘客数（如 5席4人）完全不影响正常拆分标注。

**测试**：3 项解析（样例4/4、舱位防误触、老样式只有N个位）+ 8 项收单行为（纯一行/变种压制/5≠4 正常/儿童单同压/混批脚注/开关拦截/同步携带清空）+ 全历史回归全绿。

**改动位置**：解析 _seatCountN 与八返回；装配/同步器；_isSeatTight；收单条目/输出/脚注/开关；卡片横幅与徽章；装配 toast；clearSeatTight。

**回滚**：.backups/ 上一版。
---
## 2026-07-22 — 出票记录：卡主可搜、时刻可见、一键全量备份 · v26.07.22-AB

用户要"定位某笔出票的卡主/时间/积分 + 出票记录备份"。事实澄清：已出票行本就显示 💳卡主/里程/付卡主费，搜索框已存在——真实缺口三个并已补齐：①**搜索匹配加入 卡主渠道+姓名/积分/表/代理**（输入卡主名即列出其全部出票），占位符同步提示；②出票时间补**时刻**（renderTicketed 与同款格式第二处一并统一）；③头部新增「⬇ 备份出票记录」——一键导出**全部**记录为单表 Excel（25 列含 出票时间/卡主渠道/卡主/卡尾号/积分/积分类型/CPM/付卡主$/PNR/票号/收入成本汇率等，按时间倒序，文件名带时间戳），与按月分表的月度对账导出互补。恢复功能原样保留。行拼装抽为纯函数 _ticketedBackupRows。

**测试**：5 项备份行拼装断言（卡主解析/时刻格式/航线/缺失容错）+ 全历史回归全绿。

**改动位置**：renderTicketed 搜索堆；两处出票时间格式；搜索占位符；TICKETED 头部按钮；_ticketedBackupRows/exportTicketedBackup 新增。

**回滚**：.backups/ 上一版。
---
## 2026-07-22 — 第四条装配线：重复检测的更新与跳过同步四字段 · v26.07.22-AA

用户重贴同两单仍无禁拆标记。定位：**重复检测层**——①弱重复"更新既有订单"的挑拣清单同样漏 noSplit/waitingForRelease/urgent/fareByType，且连 paxPrices 都不复制；②**强重复直接跳过、既有订单原样不动**——重贴带新批注的同一单（代理后补「禁止拆单」正是此场景）批注永远落不上。修复：新增统一 _syncOrderMarkers（文本即真相：四字段赋值、urgent 保留倒计时时间戳、无标记清旗），三线共用——弱更新补齐（含 paxPrices 有则覆盖无则清）；**强重复被跳过时仍将标记/分档同步到既有 pending 订单**（已出票记录不动，toast「♻ N 个完全重复单已跳过，其 标记/分档 已同步」）；doReparse 改为复用同一同步器（-Z 内联逻辑收编，单源防再漏）。**版本序约定**：同日 Z 后进位双字母 AA（避免同日 A<X 歧义）。**部署核对提醒**：本链修复自 -T 起层层叠加，线上徽章必须为本版才生效。

**测试**：6 项重复层端到端（真实 parse+detectDuplicates：强重复判定/同步落旗/时间戳/分档/清旗）+ 8 项 doReparse 回归（复用同步器后全绿）+ 全历史回归全绿。

**改动位置**：detectDuplicates 前新增 _syncOrderMarkers；弱更新块；强重复跳过同步块；doReparse 收编。

**回滚**：.backups/ 上一版。
---
## 2026-07-22 — doReparse 补齐四漏字段：禁拆/放位/加急/分档运价 · v26.07.22-Z

用户实测两单带「禁止拆单」重录后无标记。逐层实证排查：splitIntoBookings 批注行归属正确 → splitChunkByProposals 无方案标记时整块透传 → parseSingleBooking 两块均 noSplit=true → **凶手为 doReparse（「重新解析」通道）**：其显式字段回填清单漏了 noSplit、waitingForRelease、urgent、fareByType 四项——凡经重解析更新的订单三旗全丢、成人/儿童分档价丢失（连带 -F/-G 拆分判定失真、多人分档 rmb 重算失基）。本版对齐 parsePNR 装配：四字段按新文本赋值（urgent 保留已有时间戳、无标记文本清旗归零）；重解析成功提示追加 ⛔禁拆/🔥加急/🔒等放位/分档运价 徽记。**并修正 -U 的诊断**：当时"存量单"结论不完整，真正的长期缺口是本通道。

**测试**：8 项 doReparse 端到端（真实 split+proposals+parse+reparse 全链：入旗/时间戳保留/清旗归零/分档入库与 Σ×折扣重算 51,489.68/提示徽记）+ 全历史回归全绿。

**改动位置**：doReparse 字段回填区与成功提示。

**回滚**：.backups/ 上一版。
---
## 2026-07-22 — 未来生日定性升级：护照有效期误填推断 · v26.07.22-Y

用户指出：未来日期的"生日"大概率是护照有效期误填。三级定性——①**实锤**：生日与本人 passportExpiry **精确一致** → 「与其护照有效期完全一致，确认为有效期误填」；②未来日期落在今天+10年窗口（护照有效期典型区间）→「大概率误填了护照有效期，请向代理索取真实生日」；③远未来 → 通用核实。

**字段事实勘误**：主 SSR 处理器一直就把有效期存为 passportExpiry（并非"提取后丢弃"）；本次实际改动只有——P/ 风格 SSR 补齐同名字段捕获、校验器读该字段。**两枚被断言网拦下的雷**：第一稿误用新名 docExpiry（同义两名，实锤分支对主 SSR 路径失效）；且当时的 push 定位滑到无关的 doc尾段处理器、注入了作用域外的 `expiry` 引用（该格式命中即 ReferenceError，回归未覆盖故未炸）——本稿回退注入并全文 docExpiry 清零断言。

**流程守则升级**：确认 `set -e` 对 `A && B` 中 A 的失败豁免（POSIX），第一稿 SSR 断言失败因此未中止流水线、短暂交付过未完整验证的包（作废）。规矩追加：**发货链关键命令独立成行，不用 && 串接**。

**测试**：7 项单元（含实锤精确一致、远未来不提护照）+ SSR passportExpiry 入库端到端 + 原样例端到端（提示含护照推断）+ 63 项全量回归全绿。

**改动位置**：P/ 风格 SSR 正则捕获与入库；_paxDobIssues 实锤分支；doc尾段 push 回退。

**回滚**：.backups/ 上一版。
---
## 2026-07-22 — 生日重大异常：正常录入＋全程标记 · v26.07.22-X

用户样例：YUAN/QUAN 生日 2029-04-11（未来日期）。要求正常录入但标记乘客信息有误。实现为**派生校验**（不落库、不改解析路径、渲染与录入时实时计算，自动覆盖存量单与全部生日来源含 SSR/AI）：_paxDobIssues 三类判定——①未来日期（两位年轴心 <30 归 20xx，11APR29→2029）②乘机日带年份且早于生日 ③约 >110 岁疑年份输错。**实现要点**：校验器自带日期解析，刻意绕开 calculateAgeAtFlight——实测确认其尾部  的防御拦截会吞掉要捕捉的负年龄（本次测试先红、定位、后绿）。呈现四处：粘贴即时 toast「已正常录入并标记」；订单卡黄色横幅（--warning 令牌）列人名+定性+核实提示；紧凑行 ⚠资料 徽章（悬停详情）；均不阻断录入与收单。边界备案：1929 年及更早出生的真实旅客会被轴心判为未来年，现网无此人群，出现时人工核实。

**测试**：4 项单元 + 端到端实测（原样例喂真解析器→两乘客正常入库、精准仅标 YUAN/QUAN）+ 63 项全量回归全绿。版本号按新规发货当刻取容器日期（v26.07.22-X）。

**改动位置**：calculateAgeAtFlight 前新增 _paxDobIssues；卡片横幅（禁拆条后）；紧凑行徽章；装配后即时提醒。

**回滚**：.backups/ 上一版。
---
## 2026-07-21 — 界面层审计：令牌归位，美化缓议清单备案 · v26.07.21-W

CSS/界面层量化审计（对应昨日 JS 体检的另一半）。**判决：界面层秩序良好**——53 令牌、POLISH 覆盖层架构、mono 身份统一；重复选择器 79 处经甄别为 keyframes 与刻意覆盖，非病。

**已修（唯一必修项）**：近期新增 UI 的调色板外硬编码色归位令牌——⛔禁拆徽章/横幅/解除按钮的 #b91c1c/#991b1b → var(--red)/var(--red-light)（横幅渐变改令牌平色）；变种标记 #1d5fd0 → var(--blue)。全文三色残留清零断言。

**备案不动（惰性遗留）**：约 20 个疑似前朝布局的零引用 CSS 类（order-tab(s)/pnr-box/segment-details/ticket-card-actions/monoclear/hint-empty/next-step-glow/brand-sub/tab-bar/order-pill/action-overflow*/daily-summary*/empty-state-* 等）——无行为风险、删除有多选择器连坐风险、收益仅数 KB，按「真的值得做吗」缓议；label-A~G 与 ui-badge--* 家族经查为拼接计算类，**存活勿删**。

**美化菜单（待用户拍板，未实施）**：① 字号 25 档（8.5~44px）可收敛，建议 ≤10px 三档并两档，全局多点触改；② 紧凑行徽章 24 种存在拥挤风险，可做优先级封顶（每行最多 4 枚 + 「+N」折叠）。

**测试**：61 项全量回归 + 语法基线一致 + 三色清零断言全绿。

**回滚**：.backups/ 上一版。
---
## 2026-07-21 — 全局体检与秩序修复 · v26.07.21-V

用户要求审视全局是否杂乱。**体检结论：大盘健康**——语法失败集与基线一致、console.log/debugger/重复函数/TODO/历史残留（-H 变种、GS、AI 图片）全零、体量较 -E 基线净减 ~62KB（清理>新增）。本版修复与勘误：

**① 版本号-日期脱钩（勘误，不重写历史）**：本轮会话实际跨 7/16→7/21，-K 起十版误沿用 07.16。实际发货日备案：K=7/17；L/M/N/O/P=7/18；Q/R/S=7/20；T/U=7/21。**规则重申（跨会话生效）：每次发货当刻取容器日期铸号。**本版起执行。
**② 卡主下拉循环 innerHTML+= 归零**：改一次性拼接（出票弹窗）。
**③ 禁拆单短路配对拆计算**：childSplit 仅在非禁拆时求值，行为不变省一次 _buySplitPlan。
**④ 审计工具勘误**：死函数扫描把具名 IIFE 误报——purgeRollingBackups（旧滚动快照存储一次性清扫）与 checkDailyBackupReminder（每日备份提醒·安全机制3）均为存活启动代码，**保留不删**；今后死函数扫描须排除 `(function name(` 模式。
**⑤ 已知问题销案**：「_parseDebugLog 空转」已不成立——现行为 _parseDebugMode + 局部 _dbg 收集、解析尾刷入 #parseDebugContent，该变量已不存在，后续会话不必再查。
**⑥ 流程整改**：三次出现"断言失败后流水线因换行接续继续打包"，本次起发货命令链强制 `set -e` 开头，任何一步失败即中止、绝不产出错配包（本条上一稿即因此病短暂交付过 -U 代码+-V 日志的错配包，已作废重打）。

**测试**：61 项全量回归（含解析器实测）+ 复检（循环 innerHTML+=0 / console.log 0 / 重复 0 / IIFE 甄别后真死函数 0）全绿。

**回滚**：.backups/ 上一版。
---
## 2026-07-16 — 禁拆手动开关（存量单补旗）· v26.07.16-U

用户报告样例单卡片无禁拆横幅。**实测判定不是解析 bug**：把 v-T 解析器抽出喂原文，输出 noSplit:true/urgent:true/3乘客/2航段/¥13,498 全对；10 个 return 中 8 个订单形态全带旗（另 2 个为内部日期助手）。根因：该单在 -T 部署前解析入库，旗标只在解析时打、不追溯存量。本版补手动通道：订单菜单新增「⛔ 禁拆/已禁拆」开关（无位按钮旁，含 -T 前存量补旗说明）；⛔ 横幅加「✕ 解除」按钮（误判可退）。toggleNoSplit 镜像 toggleWaitingForRelease：persistOrders + renderPendingList + renderAirlineUsdPanel + 选择模式补标记。**顺带观察**：`02 RHX4RJ4K+*` 这种带序号前缀/`+*`后缀的 PNR 当前未被捕获（实测 pnr:null），待用户确认是否下一版补。

**测试**：4 项开关断言 + 解析器实测 + 56 项全量回归全过。

**改动位置**：toggleNoSplit 新增（toggleWaitingForRelease 前）；订单菜单按钮；⛔ 横幅解除按钮与布局。

**回滚**：.backups/ 上一版。
---
## 2026-07-16 — 识别「禁止拆单」标注，全链路封锁拆分 · v26.07.16-T

粘贴中出现 禁止拆单/不可拆单/不能拆单/禁止拆分/不可拆分 任一 → 订单打 noSplit 旗（与 _waitRelease 同管道：解析常量 → 8 处返回对象 → 装配映射，随 Firebase 同步）。效果：**收单需求无条件 `(N pax, no split)`**，压过纯成人组合小计与儿童配对拆；双击变种最高优先拦截「代理标注禁止拆单」；纯禁拆批次不出"可自由拆"脚注（混批时脚注照常，靠 unless marked otherwise 自洽）；紧凑行加 ⛔禁拆 红章；订单卡加红色横幅「⛔ 禁止拆单 — 代理标注此单不可拆开出票」。样例中 [炸弹] 继续触发既有加急旗，两旗独立。

**测试**：5 项新断言（强制整单/变种压制+脚注排除/混批共存/儿童单同禁/开关拦截）+ 46 项历史全量回归全过。

**改动位置**：parseSingleBooking 检测常量与 8 处返回；订单装配；紧凑行徽章；卡片横幅；_buyEligibleEntries/_buildBuyingList/_toggleBuyVariant。

**回滚**：.backups/ 上一版。
---
## 2026-07-16 — 新格式：First/Last 英文标签乘客 + 月日年生日 · v26.07.16-S

支持 `First Joseph  Last Shvarts` + `06-09-2005`（美式月-日-年）录入：名字归一为 LAST/FIRST（SHVARTS/JOSEPH），生日转 DDMMMYY（09JUN05）。规则细节：生日可同行或紧跟的**纯日期行**（行内混有航班等内容不认领、不消费，防偷吃行程日期）；首位>12 自动按日-月解读（25-06-2005→25JUN05）；两位均>12 视为无效；支持 - 与 / 分隔、First name/Last name、冒号变体、复合名（SMITH/MARY ANN）；FirstClass 等词不误触（词边界）。实现为循环前独立预扫描 _parseFirstLastPax（纯函数），命中行主循环跳过并记入调试日志，与既有 20+ 格式零耦合。

**测试**：8 项新断言（样例/同行/冒号/互换/防偷行/无效/零误触/多乘客）+ 46 项全量回归全过。

**改动位置**：parseSingleBooking 前新增 _parseFirstLastPax；主行循环前预扫描并入 + 首行跳过。

**回滚**：.backups/ 上一版。
---
## 2026-07-16 — 未识别航司快速补码 · v26.07.16-R

航司面板「—」组两个补码入口：**组头「—✎」徽章单击** → 按顺序逐单弹窗批量补（取消即中止，多单时汇总"完成 N/M"）；**行内双击** → 单独补该单（未识别单不在收单合规池，与选择模式双击零冲突；行 title 加提示）。输入两位字母/数字（UA/CX/3U），非法拦截。落库走 persistOrders，随后 renderPendingList + renderAirlineUsdPanel 全量刷新，选择模式中补刷选中标记；toast 明示"分组/收分/折扣规则已按新航司生效"——设航司会让表A/表Y折扣规则重新匹配，属预期行为。

**测试**：7 项断言（合法/取消/非法/无单/批量中止/无目标/全链调用计数）+ fix 外 39 项全量回归全过。

**改动位置**：renderAirlineUsdPanel 前新增 fixOrderAirline/fixUnknownAirlinesGroup；组头徽章模板；行 ondblclick。

**回滚**：.backups/ 上一版。
---
## 2026-07-16 — 收分需求主航司按大段判定 · v26.07.16-Q

混舱联程（如 CX548 HKG→HND + AA168 HND→JFK）此前收分行标首段航司 CX；改为**大段航司**：内置约百个常用机场坐标（中港台/日韩/东南亚/北美/加/欧中东澳新），按航段大圆距离份额取多数（本例 AA≈79% → `Buying …K AMEX BP-AA`）。退化链：任一航段机场无坐标 → 段数多数决；平票 → 保持订单原航司（首段）。**只改收分标签**，订单级 airline 不动（表A/表Y 折扣规则依赖它）。变种行与开关提示经 entry.al 自动同步。

**测试**：10 项新断言（两条基准距离、本单判定、单航司/缺坐标/平票/段数多数/数字码/无航班号、端到端 BP-AA）+ test4-7/hint/merge 共 39 项回归全过。

**改动位置**：BUYING 段新增 _APT/_airportDist/_buyDominantAirline；_buyEligibleEntries 的 al 派生。

**回滚**：.backups/ 上一版。
---
## 2026-07-16 — 缺航段结构补救：AI 第三介入口 · v26.07.16-P

用户实测「有 AI key 也无法加载」的粘贴（SSR DOCS + TOTAL CNY + 乱码行程）。根因链：SSR 行被规则解析出乘客 → 全量 AI 兜底被跳过；TOTAL CNY 19933 也被规则识别 → 补价分支也不触发；乱码行程做不出航段 → isMeaningfulOrder（要求≥1有效航段）不成立 → 整单在全应用不可见，表现为"粘了没反应"。修复：新增第三个 AI 介入口——parsedProposals 全部 0 航段但认出乘客时，调 _aiFallbackParse 补结构，经 _mergeAiStructure 合并：航段整体采用 AI，已有字段（规则价/舱位等）不覆盖，乘客按姓名去重（保 SSR 细节），打 aiParsed 紫徽章提醒核对；AI 也补不出时明确提示「无法生成可见订单」。缺 key 时走 _aiKeyMissingHint。

**测试**：_mergeAiStructure 10 项断言（含用户场景逐字段）+ hint/test4-7 共 29 项回归全过，语法与基线一致。

**改动位置**：粘贴循环兜底分支后插入补救块；_aiFallbackParse 后新增 _mergeAiStructure。

**回滚**：.backups/ 上一版。
---
## 2026-07-16 — 清理：去除 Google Sheets 同步与 AI 图片识别 · v26.07.16-O

按用户要求整体移除两个子系统，净减 467 行 / 25.8KB：
**① Google Sheets 同步（全删）**：设置区整个 section、Apps Script 代码弹窗、URL 存取（GS_URL_KEY/loadGsUrl/saveGsUrl/clearGsUrl/updateGsSyncStatus/testGsConnection）、出票推送（gsPushOrder/gsPushAllTicketed）、代理/卡主结算状态回写两处、初始化调用。
**② AI 图片识别（全删）**：乘客区护照/行程拖拽上传（aiParseZone/aiParseNoKey）、handleAiImageDrop/_aiParsePassport/_aiParseItinerary/_compressImage/_aiStatus/refreshAiParseZone 及全部调用点。
**③ AI API 配置地重塑**：设置区原「AI 图片识别」section 改为「🤖 ANTHROPIC AI API」，文案改为三项文字 AI（粘贴兜底解析/缺价补价/排期建议）；key 保存/清除/加密同步/密码解锁机制原样保留；状态文案与清除确认文案同步更新。
**明确保留**：SheetJS Excel 月度对账导出、订单「添加截图」（其 compressImage 与被删 _compressImage 是两个函数）、MRZ 机读码粘贴、_aiFallbackParse/_aiFareRescue/aiTicketingAdvice、-N 的缺 key 提示。

**测试**：22 项孤儿引用全零、8 项保留项在位、hint+test4-7 共 29 项回归全过、语法与基线一致、console.log=0、无重复函数。

**回滚**：.backups/ 上一版。
---
## 2026-07-16 — AI 兜底缺 key 时不再静默 · v26.07.16-N

用户报告「AI识别录入功能不见了」。排查结论：**不是回归**——loadAnthropicKey/_aiFallbackParse/_aiFareRescue 三段与基线逐字节一致（md5 比对）；根因是 API Key 存 localStorage、按浏览器×URL 隔离（既有已拍板设计），当前浏览器 key 为空 → AI 兜底与补价静默跳过、未识别段被无声丢弃。改动：粘贴函数内新增 _aiKeyMissingHint（闭包共享每次粘贴重置的一次性旗标），规则解析失败或缺运价且无 key 时提示「⚠ …未配置 Anthropic API Key → 设置填入 sk-ant- 后重试」。有 key 时行为零变化。

**测试**：4 项断言（提示一次/文案/同批去重/有key不打扰）+ test4-7 共 25 项回归全过。

**改动位置**：粘贴函数 _aiFbToastShown 处新增内嵌 helper；兜底分支 else；补价分支前置。

**回滚**：.backups/ 上一版。
---
## 2026-07-16 — 等待放位单不进收分需求 · v26.07.16-M

订单处于「等待放位」（waitingForRelease，代理未释放座位）时：① 不进入收单合规池——不出现在收单选择、不计入默认勾选、不出现在复制文本；② 点击/双击该行给出具体原因「该单等待放位中，暂不进入收单需求（放位后自动恢复）」；③ 空批提示更新为「已分配卡主/表G/等待放位不计入」。点「✓已放位」后自动恢复合规，无需其他操作。

**测试**：5 项断言（排除/文本不含/放位恢复/两类提示）+ test4/5/6 共 20 项历史回归全过。

**改动位置**：_buyEligibleEntries 过滤；copyBuyingList 空批提示；新增 _buyIneligibleToast，两处拦截共用。

**回滚**：.backups/ 上一版。
---
## 2026-07-16 — 无新增时默认全选重发 + 全选入口提示补全 · v26.07.16-L

回答「如何强制全部重新选择」：① 选择模式中**双击面板空白处=全选**（07-10 功能，-I 挪位后提示语漏写，本次补回两处提示）；② 新逻辑：进入选择模式时若全部合规单都在 1 小时已发窗口内（无新增单），视为重发意图，**默认直接全选**并提示「全部 N 单已在1小时内发过 → 默认全选重发」。混合场景（有新增）维持只选新增的默认。不新增「清除已发标记」操作——标记只影响默认勾选，全选手势与新默认已覆盖重发场景。

**测试**：全发过→默认全选、混合→只选新增+提示含全选入口，4 项断言过；test4/5 共 16 项回归全过。

**改动位置**：copyBuyingList 进入分支默认逻辑与两条 toast。

**回滚**：.backups/ 上一版。
---
## 2026-07-16 — 受约束单按拆分组放行变种 · v26.07.16-K

修正 -I 的一刀切门控：含独立儿童价/婴儿的单，只要能成组（成人≥2）就允许双击启用变种。变种输出为去重后的**拆分单位行**（如 4人单=1真成人+7y儿童价+13y/16y成人价 → `Buying 213K`(自由成人) + `Buying 424K`(成人+儿童绑定组)，单位间不加总，避免 424/426 相近金额并排困惑卖家）。开关提示改为列出单位构成 `424K(1A+1C) + 213K×2(1A)`；真正无法成组的（如1成人+2儿童）拦截文案改为「当前人员结构无法成组」。_buySplitPlan 返回 {text, groups}（内部管道，非 -H 输出格式复活）。默认模板输出零变化。

**测试**：12 项断言全过（本单默认行/变种单位/混批脚注/无法成组回归/三类门控 + test4 全量回归）。

**改动位置**：_buySplitPlan 返回结构；_buildBuyingList useVariant 条件与单位行分支；_toggleBuyVariant 门控与提示。

**回滚**：.backups/ 上一版。
---
## 2026-07-16 — 收单默认模板追加可拆脚注 · v26.07.16-J

批内含≥1个可自由拆分多人单时，复制文本尾部空行后追加：`Note: multi-pax orders can be split freely — take 1, 2, or more pax (unless marked otherwise).` 向卖家说明可按任意人数供货；"unless marked otherwise" 兜住同批 no split / splits as 单不冲突。全单人或全受约束批次不加。变种块与脚注可共存。

**测试**：4 项断言全过（脚注出现/两类不出现/与变种共存）。

**改动位置**：_buildBuyingList 尾部。

**回滚**：.backups/ 上一版。
---
## 2026-07-16 — 收单变种改为订单级双击开关 · v26.07.16-I

替代已回滚的 -H（全员自动展开）：选择模式中**双击订单行**=切换该单「变种输出」，蓝色虚线环+「变种」角标；启用时自动选中该单。启用后该单输出为主行简化 `Buying 1602K AMEX BP-UA (5 pax)` + 各档纯尺寸行 `Buying 320K/640K/960K/1280K AMEX BP-UA`（单人K×份数，1..N-1，无任何后缀标记）。默认（不双击）与 -G 输出逐字符一致。仅自由拆分多人单可启用；独立儿童价/含婴儿单与单人单双击给出拦截提示。原 07-10「双击=全选」挪到**双击面板空白处**，功能保留。变种为会话级选择，复制/退出后清零，不持久化。批内有变种块时各单空行分隔。

**测试**：10 项断言全过（无变种=一字不差回归、变种批次逐字符、受约束单塞进变种集被忽略、开关四类门控）。

**改动位置**：CSS .buy-variant；BUY_VARIANT/BUY_META 状态；_applyBuySelectMarks；dblclick 分流；新增 _toggleBuyVariant；copyBuyingList 快照与清理；_buildBuyingList 第二参数。

**回滚**：.backups/ 上一版。
---
## 2026-07-16 — 回滚 -H：撤销 Special 变种行，恢复 v26.07.16-G

用户判定多行变种输出太乱，整体撤销。index.html 从 .backups/161333 逐字节恢复，线上徽章应为 **v26.07.16-G**。今天的 -F（独立儿童价→成人配对拆）与 -G（无独立儿童价→自由拆分）两条规则**保留生效**。-H 方案（1人份不带标、≥2人份 -Special 字母、批内 A/B/C）已否决，除非用户重提，后续会话不要重建。

**回滚**：本条即回滚记录。
---
## 2026-07-16 — 【已回滚】收单需求附 Special 变种行 · v26.07.16-H

可拆单在主行（全貌标注保留）下方附变种行：1人份为普通收分不带标 `Buying 568K AMEX BP-UA`；≥2人份带 `-Special 字母` 如 `Buying 1136K AMEX BP-UA-Special A`，同一单共用一个字母，批内跨单 A/B/C 递增（A..Z 后 AA）。自由拆单变种按**单人K×份数**计（568→1136→1704→2272），保证倍数整齐；配对拆单（独立儿童价/婴儿）每个规格出一条变种（如 734K-Special）。仅出 1 人份变种的单不消耗字母。批内有变种时各单之间空行分隔；纯单行批次维持原单换行。_buySplitPlan 返回改为 {text, groups}。

**测试**：端到端整批断言逐字符通过（含用户样例 5 人单、配对单回归、婴儿单、no split 单、字母跳配）。

**改动位置**：_buySplitPlan 返回结构；新增 _specialTag；_buildBuyingList 变种输出与分块。

**回滚**：.backups/ 上一版。
---
## 2026-07-16 — 儿童无独立运价=可自由拆分 · v26.07.16-G

三级规则定型：①儿童**有独立儿童价**（child≠成人参照价）→ 成人配对拆（-F 逻辑）；②儿童**无独立运价**（与成人同价或无分档，如 5人单儿童=成人 ¥38,511）→ 视同成人，回到纯成人组合小计 `(5 pax, $5,682 each · 2p=… · 3p=… · 4p=…)`；③**含婴儿**（标记/婴儿价/DOB<2）→ 任何情况强约束，走配对拆。fareByType.child 存在但无成人参照价 → 保守视为独立儿童价。paxPrices 不等额也视为分档。

**测试**：端到端跑真实 _buildBuyingList，5 场景（本单自由拆、上单配对拆回归、婴儿强约束、强制儿童标记无分档、只有儿童价认不出人）全过。

**改动位置**：新增 _hasDistinctChildFare / _orderHasInfant（_buySplitPlan 前）；_buyEligibleEntries 门控改 constrained。

**回滚**：.backups/ 上一版。
---
## 2026-07-16 — 含儿童单可按成人配对拆 · v26.07.16-F

放宽昨日 -E 的一刀切：儿童/婴儿仍不能单独出票，但每个成人开一组、未成年人轮转挂靠 → 成人≥2 且能识别出谁是儿童（DOB/强制标记）时输出拆法：2成人+2儿童 → `Buying 1468K AMEX BP-UA (4 pax, splits as 1A+1C x2 · $7,338 each)`；组不等额时 `1A+1C=$7,338 + 1A=$3,682 x2`。1成人带娃、无成人、或只知含儿童价但认不出具体是谁 → 维持 (N pax, no split)。各组金额按分档运价/每人价加权从整单USD摊回，与K数同源。纯成人组合小计逻辑不动。

**改动位置**：_buySplitPlan（新增，_buyEligibleEntries 前）；entries 增 childSplit 字段；_buildBuyingList 含儿童分支。

**回滚**：.backups/ 上一版。
---
## 2026-07-15 — 含儿童多人单不可拆 · v26.07.15-E

收单需求：多人单若含儿童（fareByType.child 或 forcedType CHILD/INFANT）→ 儿童不能单独出票 → 不输出拆分组合，行尾标 (N pax, no split)。纯成人多人单组合照旧。

**回滚**：.backups/ 上一版。
---
## 2026-07-14 — 运价强需求AI补救 · v26.07.14-D

规则识别出结构（航段/乘客）但无任何价（rmb/usd/paxPrices/fareByType全空）→ 自动调 _aiFareRescue（haiku，只提运价JSON，同套消毒范围），填回并打 aiParsed 紫徽章提醒核对。无Key/失败时保持缺运价原状（可点金额手工补）。

**回滚**：.backups/ 上一版。
---
## 2026-07-13 — 大人/儿童无币种标小数价识别为USD · v26.07.13-C

扩展USD分型运价：①大人=成人同义 ②币种标可省略——两金额均带小数点（4304.33/4214.33）即判USD并按汇率转CNY入fareByType；纯整数（成人25260，儿童22101）仍走人民币路径不误伤。

**回滚**：.backups/ 上一版。
---
## 2026-07-12 — 姓名存疑智能警告 · v26.07.12-B

≥3人订单中，多数姓氏与某离群姓仅差一字（编辑距离≤1/前缀）→ 主卡橙色「⚠ 姓名存疑」徽章，悬停列明细（LIN/CAMILLE vs 多数姓 LI）。照常录入不拦截。WANG等完全不同的姓不误报。

**回滚**：.backups/ 上一版。
---
## 2026-07-11 — 🚨 修复待出票清零（严重数据bug）· v26.07.11-A

**根因**：v26.07.07-W 的 usdQuickFix 与 v26.07.04-T 的收单打标两处直写 storageSet 存了裸数组，而标准格式为 {orders, currentIdx} 包装对象 → 加载端读 .orders 得 undefined → 待出票清零并经 Firebase 同步扩散。

**修复**：两处改用标准 persistOrdersNow()。

**数据恢复**：若打开仍为空，F12 Console 执行归位命令（见交付说明）将裸数组包回标准格式。

**回滚**：.backups/ 上一版。
---
## 2026-07-10 — 选择模式双击行=全选 · v26.07.10-Z

收单选择模式中，双击任意订单行 → 全选所有合规单（面板级 dblclick 捕获；双击附带的两次单击互相抵消）。按钮流程不变。

**回滚**：.backups/ 上一版。
---
## 2026-07-09 — 收单已发标记1小时时效 · v26.07.09-Y

复制过的单仅在**1小时内**默认不选；超过1小时视为需重发，下次进入选择模式自动重新选中（buyPostedAt 时效判定 <3600000ms）。

**回滚**：.backups/ 上一版。
---
## 2026-07-08 — 收单需求：多人单全组合小计 · v26.07.08-X

多人订单行内列出各拆分组合：`Buying 419K AMEX BP-DL (5 pax, $838 each · 2p=$1,676 · 3p=$2,514 · 4p=$3,352)`。2人单保持 each 简式。

**回滚**：.backups/ 上一版。
---
## 2026-07-07 — 美金运价一键修正（点击金额直改）· v26.07.07-W

**改了什么**
各订单美金金额面板：金额（含"⚠ 缺运价"红字）可直接点击 → 输入美金总价 → 按汇率换算成每人 basePrice 存回（清除旧paxPrices）、持久化+云同步、列表刷新。收单需求随即取到正确金额。

**注意**：设的是未打折票面；stopPropagation 不触发行跳转/选择。

**回滚**：.backups/ 上一版。
---
## 2026-07-06 — 收单需求：多人单标注可拆分+人均 · v26.07.06-V

**改了什么**
收单清单中 ≥2 人的订单行尾追加拆分注记：
`Buying 1256K AMEX BP-UA (can split in 2, $6,278 each)`
（人均=总美金÷人数，四舍五入取整；单人订单格式不变）

**测试**：hb2 回归更新后全过（2人单 $7,064 → 706K + can split in 2, $3,532 each）

**改动位置**：_buyEligibleEntries 加 pax；_buildBuyingList 行构建

**回滚**：.backups/ 上一版覆盖。
---
## 2026-07-05 — 全局代码审查（九维全绿）+ 微合并 · v26.07.05-U

**审查结果（本会话累计 8 项新功能后）**
1. 语法✅ 2. 重复函数0 3. console.log/debugger 0 4. 花括号平衡 5. 零引用死函数：无 6. 本会话新功能函数（收单两段式/AI兜底/AI标记/智能建议等8个）全部在位 7. 循环内innerHTML+=为0 8. 1318KB/27526行（健康） 9. 唯一微瑕：CSS.escape守卫内联2处

**改了什么**
仅一处微合并：CSS.escape 守卫模式提取为 _cssEsc() 助手，applyAiPickMarks 与 _applyBuySelectMarks 共用（消除复制粘贴漂移风险）。零行为变更。

**测试**
- 语法✅；收单两段式12项回归复跑（如测试环境在位）

**回滚方式**
从 .backups/ 找上一版覆盖。
---
## 2026-07-04 — 收单需求输出改进：升序 + 默认只选新增 · v26.07.04-T

**改了什么**
1. **输出从小到大**：清单按单笔美金升序（原按航司分组降序）
2. **默认只选新增**：确认复制时给每单打 buyPostedAt 时间戳（随订单持久化 + Firebase 同步，多设备一致）。下次进入选择模式，默认只勾选没打过标的"新增"单；已发过的仍显示可点击行补选（想重发时用）。toast 显示"默认选中新增 N 单（已发过的 M 单未选，可点行补选）"
3. 首次使用（无任何标）行为不变=全选

**测试通过（12项全过）**
- ✓ 默认只选新增/已发不高亮、取消/补选计数、升序输出（150K 在 353K 前）、已发单不进剪贴板、复制后打标、二次进入全部已发→默认0选、0选确认=取消

**改动位置**
- _buildBuyingList 排序；_buyEligibleEntries posted 标；copyBuyingList 进入/确认两处

**风险或注意事项**
- ⚠ buyPostedAt 为新 schema 字段，老订单无此字段=视为新增（首次会全选，正常）
- ⚠ 想清除某单的"已发"标重置为新增：暂无 UI，需要的话可加

**回滚方式**
从 .backups/ 找上一版覆盖。
---
## 2026-07-03 — 收单需求两段式确认（自由取舍防重复发）· v26.07.03-S

**改了什么**
「📋 收单需求」升级为两段式，防止重复发已发过的单：
1. **第一次点击**：进入选择模式 —— 全部合规单（未分配/非表G）绿色描边高亮，按钮变「✓ 确认复制 (N)」并计数，toast 提示操作方式
2. **选择模式中点击订单行**：切换取消/恢复（面板级捕获监听拦截原跳转），计数实时更新；点到已分配/表G行提示不在范围
3. **第二次点击按钮**：只复制仍高亮的单（一单一行格式不变），退出模式、清除高亮、按钮还原
4. 全部取消后点确认 = 取消操作，不复制
5. 面板重渲染（同步/筛选）后高亮自动保持（复用 renderAirlineUsdPanel 尾部钩子）

**测试通过（10项全过）**
- ✓ 进入模式全选/按钮态/高亮、行toggle取消恢复+计数、只复制选中项（288K+150K，取消的353K不在）、模式退出还原、高亮清除、全取消=取消

**改动位置**
- 按钮 id + title；CSS .buy-selected；_buyEligibleEntries / _buildBuyingList(onlyIdSet) 拆分；BUY_SELECT/BUY_ELIGIBLE 状态机 + 面板捕获监听；copyBuyingList 两段式；面板重渲染钩子追加

**风险或注意事项**
- ⚠ 选择模式期间订单行的原有点击行为（跳转等）被暂时接管，确认/取消后恢复
- ⚠ 高亮不持久化（刷新页面即退出模式）

**回滚方式**
从 .backups/ 找上一版覆盖。
---
## 2026-07-02 — 收单需求修正：一单一行不合并 · v26.07.02-R

**改了什么**
用户纠正：不同订单分开出票，收单需求**不能按航司求和**。修正为：
- **一单一行**：每个未分配订单单独一行 `Buying 288K AMEX BP-UA`
- 同航司的行相邻分组（组间按航司合计降序），组内按单笔金额降序
- 顺带修复排序比较函数缺陷（同航司分支永不返回0导致第三级排序失效）

示例输出：
```
Buying 353K AMEX BP-UA
Buying 288K AMEX BP-UA
Buying 493K AMEX BP-AA
Buying 150K AMEX BP-DL
```

**测试通过（7项全过，断言同步更新）**

**改动位置**
- _buildBuyingList：entries 逐单收集 + 分组排序

**回滚方式**
从 .backups/ 找上一版覆盖。
---
## 2026-07-01 — 新功能：📋 一键收单需求（Buying NNNK AMEX BP-XX）· v26.07.01-Q

**改了什么**
各订单美金金额面板头部新增「📋 收单需求」按钮，一键生成按航司合并的收单清单并复制：
- 换算：points = USD × 100 → 显示为 K（$2,884→288K；$1,500→150K；K = round(USD/10)）
- 合并：同航司多单求和后出一行 `Buying 642K AMEX BP-UA`，金额降序
- 口径：**仅未分配卡主的待出订单**（已分配=不需再收）；表G排除（走Chase流程）；金额用未打折票面（_orderUsd，即卡上实际刷的数）
- 航司：优先订单 airline 字段，缺失时从首段航班号前两位推导
- 复制成功 toast 预览前4行

**测试通过（7项全过）**
- ✓ 同航司合并求和、单例、航班号推导航司、已分配排除、表G排除、金额降序、空队列

**改动位置**
- HTML ~3470：面板头部按钮；JS：_buildBuyingList / copyBuyingList（renderSmartSuggestions 前）

**风险或注意事项**
- ⚠ K 取四舍五入（288.4→288）；若业务要求向上取整（宁多勿少）改一行即可
- ⚠ "AMEX BP" 为固定模板；如有其他卡种需求可扩展为可配置

**回滚方式**
从 .backups/ 找上一版覆盖。
---
## 2026-06-30 — 新功能：🤖 AI兜底解析（规则失败时自动Claude提取）· v26.06.30-P

**改了什么**
解析流程升级为混合模式：
1. **规则先跑**（免费、秒出，覆盖既有全部格式，行为零变化）
2. **规则全败的段落自动走AI**：parsePNR 的 chunk 循环中，规则产出 0乘客+0航段 的段落不再静默丢弃，改为调用 _aiFallbackParse —— 用 claude-haiku-4-5 做严格 JSON 结构化提取（航段/乘客/价格/舱位/行程），走**同一条装配线**入库
3. **消毒防线**：IATA三字码校验、姓名须含斜杠、价格范围（rmb 100-500万 / usd 50-10万）、舱位/行程白名单、fareByType 逐项过滤、markdown围栏剥离 —— AI编造的数据进不来
4. **人工确认标记**：AI识别的订单打 aiParsed 标，主卡与美金行都显示紫色 **🤖 AI识别** 徽章（悬停提示"出票前请核对"）
5. 状态提示：首个AI兜底触发时 toast "🤖 规则未识别，AI 解析中…"（每次解析仅一次）
6. 降级路径：无Key/调用失败/空结果 → 与原行为一致（该段不入库），不打扰

**成本**：haiku 提取，每段约 0.3-0.5 美分；仅规则失败时才触发

**测试通过（7项兜底+4项规则抽查）**
- ✓ 围栏剥离、大写归一、aiParsed标记、非法字段全拒、仅乘客成立、fareByType范围过滤、API失败/无Key降级
- ✓ SSR/plusFare/eterm/GDS 规则路径零影响

**改动位置**
- ~26900：_aiFallbackParse；parsePNR chunk循环 ~12810；o.aiParsed ~13005；主卡徽章 ~15060；USD行徽章 ~13500

**风险或注意事项**
- ⚠ AI提取的价格务必人工核对（徽章即提醒）——消毒只能挡格式错误，挡不住语义错误
- ⚠ 触发条件为"规则完全无产出"；规则识别一半的段落暂不做AI补全（未来可选项）
- ⚠ 每次解析会串行等待AI段落，多个未识别段落时解析按钮会多等几秒

**回滚方式**
从 .backups/ 找上一版覆盖。
---
## 2026-06-29 — AI标记再修：行内右上角小签（防裁剪终版）· v26.06.29-K

**改了什么**
上版徽章 top:-9px 伸出行外，被父容器 overflow 裁剪成紫色残条。终版方案：徽章改为**行内右上角小签**（top:2px; right:2px），完全在行边界内——任何父级裁剪都伤不到它；缩小到 8.5px 字号、1px 6px 内边距，右上角圆角略收做出"角签"感；opacity .95 更含蓄。绝对定位保持，列对齐零影响；呼吸光晕保留。

**测试通过（8项全过）**

**改动位置**
- CSS .ai-pick-badge（仅样式，JS 未动）

**回滚方式**
从 .backups/ 找上一版覆盖。
---
## 2026-06-28 — AI标记美化：悬浮徽章不再挤歪网格 · v26.06.28-J

**改了什么**
用户反馈"不美观"——根因：✨AI·N 徽章被 insertBefore 插进 airline-usd-row 的**网格第一格**，把整行列全部推歪（内容错位、"未分配"截断、✕换行）。修复：
1. 徽章 CSS 改为 **position:absolute 悬浮在行左上角**（top:-9px; left:10px），完全脱离网格/弹性流，列对齐零影响；加白色描边圈与行边框区隔；pointer-events:none 不挡点击
2. .ai-pick 加 position:relative 作为定位锚
3. JS 改 appendChild（绝对定位子元素不参与布局，追加即可）
4. 字号 10→9.5、行高收紧、white-space:nowrap 防徽章内换行

**测试通过（8项全过）**
- ✓ 原有解析/剥离/双目标装饰全部保持

**改动位置**
- CSS .ai-pick / .ai-pick-badge；applyAiPickMarks 两处 appendChild

**回滚方式**
从 .backups/ 找上一版覆盖。
---
## 2026-06-27 — AI标记修复：美金面板行同步高亮 · v26.06.27-H

**改了什么**
用户反馈"没有动画效果"——根因：AI标记只挂在下方主订单卡（.order-collapse-header），而用户实际盯的是顶部「各订单美金金额」面板的紧凑行，且该行**没有 data-order-id**，标记函数无法定位。三处修复：
1. renderAirlineUsdPanel 的行创建加 `row.dataset.orderId = o.id`
2. applyAiPickMarks 升级为**双目标装饰**：主卡头 + 美金行都加 ai-pick 光晕类和 ✨AI·N 流光徽章
3. renderAirlineUsdPanel 末尾加重挂钩子（面板自身重渲染后标记不丢）

**测试通过（8项）**
- ✓ 原6项（PRIORITY解析/剥离/提示语/主卡类/AI·1/AI·2徽章）
- ✓ 新增：美金行获得 ai-pick 类、美金行徽章 AI·1

**改动位置**
- ~13486：美金行 dataset.orderId；applyAiPickMarks 双目标；renderAirlineUsdPanel 尾部钩子

**风险或注意事项**
- ⚠ 美金行较紧凑，徽章占一点行首空间；小屏下徽章随行缩放

**回滚方式**
从 .backups/ 找上一版覆盖。
---
## 2026-06-26 — AI优先订单动画标记（✨AI·N 高亮）· v26.06.26-G

**改了什么**
AI 深度分析的建议现在会**落到订单列表上**：
1. 提示词升级：要求 Claude 在建议末尾额外输出机器可读的一行 `PRIORITY: 3,1,7`（按优先顺序，最多5个）——比解析自由文本可靠
2. 解析该行 → AI_PICKS（订单id→优先名次），展示文本中剥离该行，代之以"✨ 已在下方订单列表用动画标记 N 个优先订单"
3. 对应订单卡获得：
   - 呼吸紫色光晕边框（aiPickGlow 2.2s 循环）
   - 卡头 ✨AI·1 / ✨AI·2 渐变流光徽章（AI·1 最优先）
   - prefers-reduced-motion 下退化为静态描边
4. 标记随 renderPendingList 自动重挂（钩子 setTimeout 后置，等卡片渲染完成）；出票后订单离开列表标记自然消失；再次点 AI 分析则替换
5. **缺陷修复**：重渲染调用移出 try/catch 并自带保护——渲染小故障不再把成功的 AI 建议覆盖成错误提示

**测试通过（6项+5项解析抽查）**
- ✓ PRIORITY 解析映射（乱序 2,1,3）、展示文本剥离、提示语、ai-pick 类、AI·1/AI·2 徽章内容
- ✓ SSR/plusFare/Delta/eterm/横排新变体 解析抽查全过

**改动位置**
- CSS ~5560：aiPickGlow/aiPickShimmer/.ai-pick/.ai-pick-badge
- JS：AI_PICKS + applyAiPickMarks（renderSmartSuggestions 前）；renderPendingList 钩子；aiTicketingAdvice 提示词与解析

**风险或注意事项**
- ⚠ AI 偶尔可能不按格式输出 PRIORITY 行 → 无标记但建议正常显示（优雅降级）
- ⚠ 标记不持久化，刷新页面后消失（设计如此：建议有时效性）

**回滚方式**
从 .backups/ 找上一版覆盖。
---
## 2026-06-25 — 建议面板位置修正：横幅下方、美金面板上方 · v26.06.25-B

**改了什么**
「💡 智能出单建议」面板从 PENDING ORDERS 区块上方（页面很靠下，被19单的美金面板挡住）移到**待出票横幅正下方、各订单美金金额面板之前**——打开待出票 tab 第一眼就能看到建议。

**为什么改**
用户部署 v26.06.24-A 后反馈"建议在哪里"——面板实际已渲染，但位置在长长的美金面板之后，需滚动很久才能看到，违背"建议"应第一眼可见的初衷。

**测试通过**
- ✓ HTML顺序验证：pendingBanner → smartSuggestSection → airlineUsdPanel
- ✓ 面板块完整（4个id齐全）；9项功能测试全过；26项解析回归全过

**改动位置**
- HTML：smartSuggestSection 区块整体迁移至 ~3447（airlineUsdPanel 前）

**回滚方式**
从 .backups/ 找上一版覆盖。
---
## 2026-06-24 — 新功能：💡 智能出单建议 + 🤖 AI深度分析 · v26.06.24-A

**改了什么**
待出票 tab 顶部新增「智能出单建议」面板，双层设计：

**本地规则引擎（即时、永远可用）**——基于上单时间与代理自动分析：
1. 久等分级：>48h 🔴最优先（运价/放位风险）、>24h 🟠今日内出、全部<24h 🟢报平安；列最久3单可点击跳转
2. 代理批量：同代理≥3单 → 📦集中出票建议（减少切换）
3. 代理节奏：某代理今日上单 ≥3 且 >近两周日均2倍 → 📈提醒留意付款与放位
4. 阻塞项：⚠️缺运价（无法出，先补价，可跳转）、🔒等放位（出票前确认）
随 renderPendingList 自动刷新；无待出单时整块隐藏。

**🤖 AI深度分析按钮（可选）**——复用现有 Claude 调用管线（loadAnthropicKey + api.anthropic.com，模型 claude-sonnet-4-6）：把队列摘要（代理/等待时长/金额/乘客数/标记）发给 Claude，返回 3-5 条调度建议渲染在面板内。无 Key 时提示去设置配置；失败有错误提示；分析中按钮禁用。

**测试通过（9项+26核心解析回归 全过）**
- ✓ 面板显隐、48h预警、表G排除、批量、缺运价（修正：_orderUsd 无价返回0非null，判定用 !(v>0)）、跳转链接、AI渲染（fetch打桩）、按钮恢复、空队列隐藏
- ✓ 26项解析回归确认无副作用

**改动位置**
- HTML ~3488：面板区块；JS ~14210 前：renderSmartSuggestions / aiTicketingAdvice / 两个小助手；renderPendingList 内接入刷新

**风险或注意事项**
- ⚠ AI分析每次消耗少量 API 额度（约几百 token）；本地建议零成本
- ⚠ 节奏对比用出票历史（ticketedAt）近14天做基线，新代理无历史时不提示
- ⚠ 表G订单不参与（与待出票口径一致）

**回滚方式**
从 .backups/ 找上一版覆盖。
---
## 2026-06-23 — 移除滚动备份快照，释放本地存储 · v26.06.23-S

**改了什么**
按用户决定移除滚动备份快照功能（此前每次出票存 2 份全量状态，占用≈数据本身3倍）：
1. 删除：设置面板"🛡 本地手动快照"区块、saveRollingBackup / getRollingBackups / restoreFromRollingBackup / renderRollingBackupsList 四函数、ROLLING_BACKUP 常量、trim IIFE、两处设置页悬空调用、过时注释
2. **新增一次性清除**：加载时 `localStorage.removeItem('ticket-organizer-rolling-backup')` —— 浏览器里已存的旧快照被清掉，**空间立即释放**（关键：只删代码不删存量，空间不会回来）

**数据安全**
本地快照移除后，数据安全依赖：① Firebase Firestore 云同步（完整副本）② storageSet 的内存兜底。截图（截图附件）功能未动。

**效果**
本地占用降为原来的约 1/3（主数据 ×1，不再 ×3）。部署后打开页面即自动清除旧快照。

**测试通过**
- 语法✅ 重复函数0 残留引用0（悬空调用已清）
- 26项核心解析回归全过；核心函数冒烟（设置页渲染链等）全绿
- purge IIFE 在测试环境执行无异常

**改动位置**
- 设置UI ~3808、清除IIFE ~24580、四函数原 ~24830-24915、调用点 3029/7215

**风险或注意事项**
- ⚠ 不再有本地应急恢复入口；若 Firestore 与本地同时损坏无本地回滚（用户已知悉并选择容量优先）
- ⚠ 若还想进一步减容：下一候选是老订单归档（导出后从本地清除，云端保留）或截图外置

**回滚方式**
从 .backups/ 找上一版覆盖（恢复代码；已清除的存量快照不可恢复）。
---
## 2026-06-22 — 组合识别：乘机人列表+名姓明细块+USD分型运价+全程舱位 · v26.06.22-M

**改了什么**
四个新处理器（置于行循环最前，先于通用名字模式）：
1. **乘机人编号列表**：`乘机人： 1.MIAO/XIAOKANG 2.…` 逐个建档（此前整行被误当一个巨型名字并偷走后续生日）
2. **名/姓明细块**：Adult|Child 标签 + （名）First name/（姓）Last name + 性别行 + 生日行，状态机跨行合并；与乘机人列表同名合并；Child→forcedType（YIXIN 儿童价自动生效）
3. **USD 分型运价**：`成人 4453.73 USD/人，儿童4423.73 USD` → usd=成人价 + fareByType 按 settings.rate 转 CNY
4. **全程<舱位>**：全程商务→商务舱

**端到端结果（用户输入）**
- ✓ 3航段（含实际承运忽略、东京羽田HND/华盛顿杜勒斯IAD）
- ✓ 4乘客全带生日性别，MIAO/YIXIN 标 CHILD
- ✓ 商务舱，usd 4453.73，fareByType {adult/child CNY}，全行识别

**测试通过（8项+26核心回归 全过）**
- ✓ 各处理器单独可用；处理器前置未破坏 JIANG/LI 分行、GONG/KE、标准SSR 等既有路径

**改动位置**
- ~10187：nb 状态变量；~10393：四处理器块（行循环 trimmed 之后）

**风险或注意事项**
- ⚠ 明细块的性别/生日行仅在 nbPax 已解析后消费，独立 MALE/FEMALE 行（如 JIANG/LI 格式）不受影响（已测）
- ⚠ USD→CNY 依赖 settings.rate；rate 缺失时仅存 usd

**回滚方式**
从 .backups/ 找上一版覆盖。
---
## 2026-06-21 — 解析器新增：SSR尾段残片乘客（23AUG92/女 /HUANG/MINGJIN/P1）· v26.06.21-P

**改了什么**
新增独立 doc 尾段乘客模式：`生日/性别/姓/名[/Pn]`，无 SSR 前缀、无证件号：
- 性别支持 中文男女 与 M/F/MALE/FEMALE
- 斜杠周围容忍空格（"女 /HUANG"）
- 单位数日补零；4位年归一为2位
- 与既有同名乘客合并（SSR 后到时补 expiry，不重复）

**端到端结果（用户输入）**
- ✓ HUANG/MINGJIN FEMALE 23AUG92，未识别清零

**测试通过（7项+26核心回归 全过）**
- ✓ 中文/字母性别、单位日、无Pn、与SSR同名合并
- ✓ 防误伤：标准SSR、裸P/行、航段行 全不受影响

**改动位置**
- index.html ~10733：docTail 模式（bareDocMatch 后）

**风险或注意事项**
- ⚠ 模式要求整行匹配且以 DOB 开头，普通斜杠文本不会误触

**回滚方式**
从 .backups/ 找上一版覆盖。
---
## 2026-06-20 — 解析器新增：Delta官网订票页文本 · v26.06.20-T

**改了什么**
新增 Delta.com 页面倾倒文本专用块。检测签名：逗号航班列表行（DL388,DL337）+ 点分路由行（PVG.MCO），两者独有不与其他格式冲突。解析策略：
- 中转链：整体起点 → Layover (XXX) 括号码按序 → 整体终点；航班数须等于链长-1（守卫）
- 时间：独立 am/pm 行按文档序两两配对为各段起降（12小时制转24）
- 各段舱位字母：行尾 "(G)"/"(W)"
- 价格：取 "¥13,637 CNY"（Amount Due，唯一带 CNY 后缀的¥金额），非票面价
- 舱位：Premium Select→超级经济舱 / Delta One|Business→商务舱 / First→头等舱

**端到端结果（用户输入）**
- ✓ DL388 21JUL PVG-DTW 09:55-12:10 (G) / DL337 21JUL DTW-MCO 14:00-16:46 (W)
- ✓ rmb=13637（Amount Due），cabin=超级经济舱，33行全识别

**测试通过（5项+26核心回归 全过）**
- ✓ 端到端；逗号列表无点路由不触发；Expedia括号/携程带IATA互斥不受影响

**改动位置**
- index.html ~9700：Delta 块（Expedia括号块前）

**风险或注意事项**
- ⚠ 无乘客信息（Delta页此段不含姓名），需另贴
- ⚠ 全段共用同一日期（跨天到达时段2日期可能差1天，源文本无标记）
- ⚠ 中转链依赖 Layover (XXX) 括号；缺失时航班数≠链长-1 → 整块放弃回落主流程

**回滚方式**
从 .backups/ 找上一版覆盖。
---
## 2026-06-19 — 复制行程重做：未打折美金运价+乘客信息(国籍默认USA)+行程确认 · v26.06.19-I

**改了什么**
buildItineraryCopy 三处重做：
1. **行程确认单值**：TRIP 行严格输出 ONE WAY / ROUND TRIP / MULTI-CITY 之一。优先用订单 trip 字段，缺失才按路由推断；去掉旧的 "(MULTI-STOP)" 附注和重复括号
2. **乘客信息段**：每位乘客输出 PASSENGER N / FIRST NAME / LAST NAME / DOB(展开为 26 OCT 1970) / GENDER / **NATIONALITY 默认 USA**；缺失字段标(待补全)；多人用 --- 分隔
3. **价格改为未打折美金运价**：basePrice ÷ 汇率（与卡片蓝色"人均运价 USD"同款），标注 /PERSON；多人加 TOTAL ≈ $X (N PAX)。回退链：basePrice÷rate → o.usd → paxPrices均值÷rate → 最后才 computeFinalPrice。**不再输出折扣后收入**

**端到端（截图那单还原）**
输出与需求一致：TRIP: ROUND TRIP / 4段 / PASSENGER 1 完整含 NATIONALITY: USA / APPROX. PRICE $7,390.88 /PERSON（=50272÷6.8019，非×90%后的6,651）

**测试通过（6项+25核心解析回归 全过）**
- ✓ 多乘客 TOTAL 行、MULTI-CITY 推断、缺失字段待补全、o.usd/paxPrices 回退、折扣不渗入价格

**改动位置**
- index.html ~19979：buildItineraryCopy 重写

**风险或注意事项**
- ⚠ 国籍字段乘客数据暂无存储，固定输出 USA（可传 p.nationality 覆盖）
- ⚠ 一口价订单走最后回退（无折扣概念，语义不变）

**回滚方式**
从 .backups/ 找上一版覆盖。
---
## 2026-06-18 — Expedia往返无航班号+N TRAVELERS总价平摊 · v26.06.18-R

**改了什么**
1. **路由+日期+时间同行内联变体**：`Hong Kong (HKG) HKG to Los Angeles LAXJul 21 . 12:40 pm to 11:15 am . Nonstop` 一行直接出航段（两行式 cityCarry 保持不变）。航班号源数据没有→留空
2. **(N TRAVELERS) 总价平摊**：捕获人数；在 usd→rmb 换算之后，若有总价、无每人价、无乘客名单 → rmb/usd 除以人数、paxPrices 填充（rmb 人均语义）
3. 往返自动判定（两段镜像回起点）复用既有逻辑

版本徽章 → v26.06.18-R。

**为什么改**
用户 Expedia 往返报价：两段一粘一分、全程无航班号、$2320.73 为 3 人总价。原状只出第二段、rmb 存了总价（违反人均语义）。

**端到端结果（用户输入）**
- ✓ HKG-LAX 21JUL 12:40-11:15 / LAX-HKG 29JUL 12:50-18:30，trip=ROUND TRIP
- ✓ usd=773.58/人，rmb=5608.43/人，paxPrices=[×3]

**测试通过（5项+25核心回归 全过）**
- ✓ 端到端；Expedia两行 cityCarry 不受影响
- ✓ Expedia总价块（自带 Travelers: 除法）不双重除（!parsedPaxPrices 守卫）
- ✓ 单人不除；有乘客名单时不按人头除（pax.length===0 守卫）

**改动位置**
- ~10390：cityInline 内联变体；~10131：travelerCount 声明+捕获；~11970：换算后平摊块（曾误插在换算前，已移正）

**风险或注意事项**
- ⚠ 航段无航班号（源数据缺失），需手动补
- ⚠ 平摊仅在"无每人价+无乘客名单"时触发，护栏防双除

**回滚方式**
从 .backups/ 找上一版覆盖。
---
## 2026-06-17 — 代码体检：去冗余合并 + 死代码清理（零行为变更）· v26.06.17-O

**改了什么**
纯优化，不改任何解析行为：
1. **共享常量合并**（parseSingleBooking 顶部）：月份表 _MN3（函数内5份副本并入）、_monDay 日期助手（3份同款合并2份）、_CN_CABIN 中文舱位映射超集（4份分散映射统一，含公务→商务归一化）
2. **死代码删除**：6个零引用遗留函数（updatePricing/updateFromRMB/updateCost/updateCostFromRMB/renderProfitField 旧UI shim + buildRouteKey 废弃助手）。_trimRollingBackupRing/checkDailyBackupReminder 为活跃 IIFE，保留；doPost/doGet 为 GAS 收单器模板，保留
3. **重复计算提升**：waitingForRelease 放位正则 7 处内联 → 1 次计算复用

版本徽章 → v26.06.17-O。

**指标**
- 文件 1,318,901 → 1,317,108 字节（净省 ~1.8KB）
- 月份表副本 8 → 4（余3份在其他函数域，安全起见未跨域合并）
- 每步之后 25 项基线回归全过；语法/重复函数/console.log 审计全绿

**测试**
- 建 25 项基线回归（覆盖今天全部新格式：eterm、全角破折号、横排新变体、浓缩单行、公务加号、分行粘连等），优化前先跑绿，每步优化后复跑，全程 25/25

**风险或注意事项**
- ⚠ _CN_CABIN 为超集映射，各调用点只查自己正则捕获的键，多余键不生效——语义不变
- ⚠ 跨函数域的3份月份表未合并（作用域安全优先，收益小）

**回滚方式**
从 .backups/ 找上一版覆盖。
---
## 2026-06-16 — 浓缩单行识别：ETAS中文舱位+行内美金+行中P/证件 · v26.06.16-D

**改了什么**
支持一行浓缩全部信息的格式 `1. UA888 超经 FR04SEP SFOPEK DK1 1045 1525+1 ... 2020美金 行程2 P/CHN/.../XIE/CHUNCHENG/P1`：
1. ETAS 舱位槽接受中文舱位词（超经/经济舱/商务舱/公务舱/头等舱）→ 订单级 cabin（超经→超级经济舱），航段 cls 留空
2. 预处理新增：非 SSR 行的行中 " P/XX/" 前切分，裸证件独立成行走 bareDoc（SSR 行排除，其 P/ 是语法一部分）
3. 航段匹配后行内提取美金/美元价 → usd（范围 50–100000）

版本徽章 → v26.06.16-D。

**端到端结果（用户输入）**
- ✓ UA888 04SEP SFO-PEK 1045-1525
- ✓ cabin=超级经济舱，usd=2020（rmb 按汇率自动折算）
- ✓ 乘客 XIE/CHUNCHENG FEMALE 05MAY63

**测试通过（6项+21核心回归 全过）**
- ✓ 超经/商务舱在舱位槽；标准单字母舱位不受影响
- ✓ SSR行不被切分（expiry 保留）；独立 P/ 行仍工作；美金不误采
- ✓ 21项核心回归全通过

**改动位置**
- index.html ~10107：ETAS 舱位槽字符类
- ~10156：预处理 P/ 切分分支
- ~11195：seg处理器中文舱位映射+美金提取

**风险或注意事项**
- ⚠ 美金提取仅在航段行匹配成功后进行，普通聊天句不误采
- ⚠ 公务舱→商务舱归一化与全局一致

**回滚方式**
从 .backups/ 找上一版覆盖。
---
## 2026-06-15 — 携程横排变体：无"公司"航司名+独立时间行+粘连IATA · v26.06.15-H

**改了什么**
横排携程块四处放宽以兼容新变体：
1. 检测与航司锚点：`航空公司` → `航空(?:公司)?`（"美国联合航空UA199"无公司也认）
2. 上/下扫的段边界 break 条件同步放宽（2处）
3. 上扫新增独立时间行捕获（回程出发时间"13:15"单独成行）
4. 到达 IATA 回退用 lookaround 代替 \b（"…>08-24PVG" 数字紧贴字母时 \b 无边界匹配不到）

版本徽章 → v26.06.15-H。

**端到端结果（用户输入）**
- ✓ UA199 30JUL PVG-LAX 20:10-17:20
- ✓ UA198 23AUG LAX-PVG 13:15-（到达时间源数据缺失，仅有跨天日期08-24）

**测试通过（4项+21核心回归 全过）**
- ✓ 新变体双段；旧横排（人+公司）双段不受影响
- ✓ 竖排（当地时间）、带IATA（平均到达时间）互斥依旧
- ✓ 21项核心回归全通过

**改动位置**
- index.html ~9634：横排块检测/锚点/break/上扫/dest回退

**风险或注意事项**
- ⚠ 锚点放宽后横排块能吃更多"航空XX123"行，但它在竖排/带IATA之后短路，且需 MM-DD周X 配合，互斥仍成立
- ⚠ UA198 到达时间为空是源数据缺失，非解析遗漏

**回滚方式**
从 .backups/ 找上一版覆盖。
---
## 2026-06-14 — 中文城市航段分隔符扩展：全角破折号—/en-dash– · v26.06.14-C

**改了什么**
中文城市航段的城市分隔符字符类 [-→－] 扩展为 [-→－—–]，新增全角破折号 —（U+2014）和 en-dash –（U+2013）。三处模式同步（cnSeg两个变体 + cnSegIata）。版本徽章 → v26.06.14-C。

**为什么改**
用户输入 `1.  UA1429  7月19日  堪萨斯城—旧金山  19:59  21:50` 五段全丢。根因：城市间用的是全角破折号—，原字符类只认 -、→、－。城市表本身已覆盖（堪萨斯城MCI/丹佛DEN/首尔仁川ICN）。

**端到端结果（用户输入5段）**
- ✓ UA1429 MCI-SFO / UA805 SFO-ICN / UA806 ICN-SFO / UA2348 SFO-DEN / UA586 DEN-MCI
- ✓ 中转备注（转:2时5分）自动忽略，+2 次日标记正确剥离

**测试通过（5项+21核心回归 全过）**
- ✓ 全角破折号5段、en-dash
- ✓ 防误伤：普通横线、全角－、UA820带IATA前缀 全不受影响

**改动位置**
- index.html ~11090：三处分隔符字符类（sed 全局替换 [-→－]→[-→－—–]，共3处）

**风险或注意事项**
- ⚠ em-dash 也常用于中文破折号语义（如"——"双破折），但此模式要求两侧紧贴中文城市名+前后有航班/时间锚定，误触风险极低

**回滚方式**
从 .backups/ 找上一版覆盖。
---
## 2026-06-13 — 解析器新增：eterm黑屏指令格式（nm/ss/co）· v26.06.13-E

**改了什么**
新增代理下单指令格式三模式：
- `nm1LIN/YAN` — 名字指令 → 乘客（后续 SSR DOCS 同名自动合并生日/性别，不重复）
- `ss  DL388  V 18JUL  PVGDTW 1` — 卖座指令 → 航段（航班粘连、单字母舱位、无星期日期、粘连路由、尾部座位数忽略）
- `co11197元，1件行李经济` — 成本备注 → 价格 11197 + 舱位经济舱（舱位词可选）

版本徽章 → v26.06.13-E。

**端到端结果（用户输入）**
- ✓ DL388 18JUL PVG-DTW V / DL5846 18JUL DTW-IND V
- ✓ LIN/YAN FEMALE 24FEB85（nm+SSR 合并为 1 人）
- ✓ rmb=11197，cabin=经济舱，未识别清零

**测试通过（8项+21核心回归 全过）**
- ✓ nm单独、ss单数字日补零、co无舱位、co商务
- ✓ 防误伤：SSR行不被ss误抢（^ss\s+ 要求空格，SSR 的 R 挡住）；编号GDS航段不受影响；copy中文词不误触
- ✓ 21项核心回归全通过

**改动位置**
- index.html ~10963：nm/ss/co 三模式（cnDateEnSeg 前）

**风险或注意事项**
- ⚠ nm 推的乘客无生日性别，依赖后续 SSR 补全；无 SSR 时需手动补
- ⚠ co 行价格范围 100–500000
- ⚠ ss 舱位是单字母 cls（非订单级 cabin）；订单级舱位来自 co 行的中文舱位词

**回滚方式**
从 .backups/ 找上一版覆盖。
---
## 2026-06-12 — GDS单行救援 + 版本徽章升级 v26.06.12-F

**改了什么**
1. **GDS单行预切分**：整单从聊天软件复制丢失全部换行时（≤2行且含GDS段签名 DK\d），在段标记（2. UA152）、乘客名（NAME/NAME 女）、运价（成人25260 / 公务N+M）、TOTAL 前重插换行。多行输入零改动
2. **版本徽章 v26.06.11-P → v26.06.12-F**：之前多个包共用同一版本号，无法区分部署是否落地。本包起每次交付变更版本号

**为什么改**
用户报"还是无法识别"。验证输出包代码对多行版完全正常 → 两个嫌疑：部署未落地（版本号无法区分）或粘贴丢换行（GDS整单变一行时运价确实丢失，实测复现）。两个都堵。

**测试通过（5项+21核心回归 全过）**
- ✓ 整单一行：2航段+3乘客+成人儿童价全恢复
- ✓ 一行含plusFare
- ✓ 防误伤：正常多行GDS零改动、携程带IATA单行仍走自己的救援、普通短句不触发
- ✓ 21项核心回归全通过

**改动位置**
- index.html ~9393：parseSingleBooking 开头 GDS 单行救援
- 版本徽章两处文字

**部署验证（重要）**
部署后标题栏应显示 **✓ v26.06.12-F**。若仍是 v26.06.11-P 或更旧 = 新包未上线，识别问题无从谈起。

**风险或注意事项**
- ⚠ 预切分仅当输入≤2行且含 DK\d GDS签名时触发，与携程单行救援条件互斥
- ⚠ 乘客名切分需 全大写/全大写+男女，中文姓名行不受影响

**回滚方式**
从 .backups/ 找上一版覆盖。
---
## 2026-06-11 — 运价识别修复：类型词粘连金额（成人25260 分行）

**改了什么**
类型词+裸金额子模式的空格从必需（\s+）放宽为可选（\s*），支持粘连写法：
```
成人25260
儿童22101
```
此前该行被 fareTypeMatch 外层匹配（以成人/儿童开头）但所有子模式要求空格/冒号/运价关键词，全不中 → 行被静默吞掉，价格丢失。

**端到端结果（用户输入）**
- ✓ fareByType {adult:25260, child:22101}，rmb=25260
- ✓ paxPrices=[25260,25260,22101] — HE/ZHIYUAN(10岁)自动分到儿童价
- ✓ 未识别清零

**测试通过（9项+20核心回归 全过）**
- ✓ 粘连分行、婴儿粘连、三行全粘连
- ✓ 防误伤：原带空格写法、人数行（成人 2 儿童 1 / 成人2）、日期行（婴儿2026年）都不误判；dualFare/plusFare 不受影响
- ✓ 20项核心回归全通过

**改动位置**
- index.html ~11358：equivMatch 裸金额子模式 \s+ → \s*

**风险或注意事项**
- ⚠ 人数误判防线：金额需≥3位数字（\d{3,6}），人数1-2位不中；全行锚定拦截日期行
- ⚠ 理论边界：粘连"成人250"会当作250元运价——极低国内价场景真实存在，可接受

**回滚方式**
从 .backups/ 找上一版覆盖。
---
## 2026-06-11 — 解析器新增：加号连接的成人/儿童运价（公务25260+22101）

**改了什么**
新增运价格式：舱位词（可选）+ 2-3个金额用"+"连接：
- `公务25260+22101` → 商务舱，成人25260 + 儿童22101
- `经济舱8000+6000+800` → 成人+儿童+婴儿三价
- 两价时第二价<成人价30% → 判定为婴儿价（儿童价通常为成人75-90%，婴儿约10%）
- 裸 `N+M`（无舱位词）也支持，金额下限提高到500防误触

配合既有的按年龄自动分配链路：乘客按航班日年龄自动拿对应价（<2婴儿、<12儿童、其余成人）。

**端到端结果（用户输入）**
- ✓ 2航段 UA153/UA152（尾部机型数据自动忽略）
- ✓ 3乘客（含缺"日"字的中文日期）
- ✓ cabin=商务舱（公务→商务舱）
- ✓ fareByType {adult:25260, child:22101}
- ✓ paxPrices=[25260,25260,22101] — HE/ZHIYUAN(2015年生,10岁)自动分到儿童价

**测试通过（9项+19核心回归 全过）**
- ✓ 三价格式、婴儿比例判定、裸N+M
- ✓ 防误伤：GDS航段"1830+1"不误触、dualFare不受影响、小数字被拒
- ✓ 19项核心回归全通过

**改动位置**
- index.html ~11225：plusFare 模式（dualFare 后）

**风险或注意事项**
- ⚠ 两价时用30%比例区分儿童/婴儿——若遇到五折以下儿童特价会被误判为婴儿，届时手动改或告知调整阈值
- ⚠ 裸N+M下限500；带舱位词下限100
- ⚠ 舱位词映射：公务/商务→商务舱

**回滚方式**
从 .backups/ 找上一版覆盖。
---
## 2026-06-11 — 部署通道切换：官方 Actions workflow（修复持续 deploy 失败）

**改了什么**
新增 `.github/workflows/deploy-pages.yml` — GitHub Pages 官方 Actions 部署通道，替代默认 "pages build and deployment"（该通道 #121、#122 连续 deploy 失败，报 "Deployment failed, try again later"，build 均成功，属 Pages 发布环节配置性故障）。

workflow 要点：push 到 main 自动跑；支持 Actions 页手动触发；官方三件套 configure-pages→upload-pages-artifact→deploy-pages；并发控制新部署顶掉排队旧部署。

**部署操作变化（重要）**
1. 解压后把 `.github` 文件夹（含 workflows/deploy-pages.yml）连同 index.html、CHANGELOG.md 一起拖进本地仓库
2. GitHub Desktop commit + push
3. **必须一次性设置**：仓库 Settings → Pages → Source 改为 **GitHub Actions**（否则新旧两条通道并行，旧的还会报错干扰）
4. 之后每次 push，Actions 页会出现 "Deploy to GitHub Pages" 运行，绿了即上线

**风险或注意事项**
- ⚠ Source 不切到 GitHub Actions 的话，旧通道继续跑继续红，但不影响新通道实际部署成功
- ⚠ .github 是隐藏文件夹，拖拽时确认它进了仓库根目录
- ⚠ 首次运行若报权限错误，检查 Settings → Actions → General → Workflow permissions 为 Read and write

**回滚方式**
删除 .github/workflows/deploy-pages.yml，Settings → Pages → Source 改回 Deploy from a branch。
---
## 2026-06-11 — 全面审核审查：31项改动核对 + 边界统一 + ReDoS压测

**改了什么**
系统性审核今天全部改动，发现并修复1处不一致：
- **pTotal 价格边界统一**：主流程 pTotal 原为 >100 无上限，与 _totalFromRaw（100~5000000）不一致。统一为 ≥100 且 ≤5000000，防止证件号等大数误采为价格

**审查覆盖**
1. **基础健康**：语法✅ 重复函数0 console.log 0 debugger 0 花括号7486平衡
2. **31项改动完整性**：今天全部改动逐项核对存在于代码（含B6航司、表G、3种携程、2种Expedia、SSR空格、黑名单按钮、改运价、POLISH层、版本徽章等）
3. **short-circuit互斥性**：6个专用格式块交叉测试互不抢夺（携程横排/竖排/带IATA、Expedia括号/紧凑、United详细）
4. **边界鲁棒性16项**：空输入/病态字符/超长行不崩溃；价格100/5000000边界统一；折扣50%下限正确
5. **性能**：20单批量解析24ms；典型单次0.1ms；循环内innerHTML+= 为0
6. **ReDoS压测**：6种病态输入（长姓名/病态日期/重复token/300斜杠）全部<15ms，无灾难性回溯

**测试通过**
- 18项最终核心回归全过
- 16项边界测试全过（含本次修复的2项）

**改动位置**
- index.html ~11400：pTotal 金额范围统一

**风险或注意事项**
- ⚠ setInterval 4处未清理为世界时钟等常驻功能，单页应用合理
- ⚠ USD 价格路径下限仍为 >10（美元语义不同，未动）

**回滚方式**
从 .backups/ 找上一版覆盖。
---
## 2026-06-11 — 界面精致化升级（Polish Layer）+ 版本校验徽章

**改了什么**
1. 追加 POLISH 层（纯附加 CSS，不覆盖任何结构规则），一批克制的质感升级：
   - 主操作按钮（.accent）：柔和阴影，悬停微抬起、按下回落
   - 输入框/下拉：聚焦时 3px 柔和 accent 光晕
   - 标签页：发丝级 accent 下划线（带入场动画）代替填充块观感
   - 卡片：顶边极淡高光（"lit from above"），悬停浮现
   - 数字：等宽 tabular-nums（价格/汇率列对齐，变值不跳动）
   - 键盘聚焦：统一清晰 focus-visible 描边（无障碍floor）
   - 滚动条变细中性、字体抗锯齿、text-rendering 优化
   - 全部遵守 prefers-reduced-motion
2. 版本徽章升级为可点击的部署校验入口：`✓ v26.06.11-P`，点击说明"版本号不符=加载旧版"，直接服务部署验证

**为什么改**
趁重新部署，整体打磨质感。这是天天用的高密度工作台，升级方向为精致克制专业，非花哨。版本徽章帮助确认部署是否生效。

**测试通过**
- 15项核心回归全过（美化未破坏任何解析逻辑）
- 语法✅ 重复函数0 console.log0 花括号平衡

**改动位置**
- index.html ~5551：POLISH 层（第二个 style 块末尾追加）
- index.html ~2986：版本徽章可点击化 + 版本号更新

**风险或注意事项**
- ⚠ POLISH 层纯附加，用 :where() 低优先级选择器 + 作用域类，不与现有规则冲突
- ⚠ 阴影/抬起只加在 .accent 主按钮，中性扁平按钮保持原设计
- ⚠ 部署后版本号应显示 v26.06.11-P，可作为新版生效的直观标志

**回滚方式**
从 .backups/ 找上一版覆盖。
---
## 2026-06-11 — 黑名单增强：订单菜单一键「⛔ 加入黑名单」

**改了什么**
黑名单功能已存在（Settings 里可管理、录入命中弹窗强提醒）。本次新增便捷入口：订单卡片菜单加「⛔ 加入黑名单」按钮（在「改运价」后），一键把此单所有乘客加入在线开票黑名单，不用去 Settings 手敲名字。

**为什么改**
用户要求在线开票黑名单，避免白费精力。核心功能上个 session 已做，缺的是便捷标记入口——之前只能在设置里手动敲。

**功能细节**
- 一键拉黑此单全部乘客（去重、大小写/空格不敏感）
- 已在名单的自动跳过，提示哪些跳过
- confirm 确认后加入，toast 反馈
- 若设置面板开着，自动刷新黑名单列表
- 下次录入到同名乘客 → alert 强提醒（订单仍照常录入，原有逻辑）

**测试通过**
- 去重：zhang/san 与 ZHANG/SAN 合并
- 大小写/空格：zhang/ san 命中 ZHANG/SAN
- 未拉黑不误判；重复加入自动跳过（toAdd=0）
- 审计：语法✅ 重复函数0 console.log0

**改动位置**
- index.html ~14746：菜单加「⛔ 加入黑名单」按钮
- index.html ~8450：blacklistOrderPax 函数

**风险或注意事项**
- ⚠ 黑名单存 settings.blacklist，跟随现有同步机制
- ⚠ 按 normalizeBlacklistName 规范化后精确匹配（姓名级，非姓氏模糊）
- ⚠ 命中仅提醒不拦截，订单照常录入（符合"强提醒"需求）

**回滚方式**
从 .backups/ 找上一版覆盖。
---
## 2026-06-11 — TOTAL CNY 全格式统一采集为运价

**改了什么**
新增统一价格提取 helper `_totalFromRaw`，让所有专用格式解析器（short-circuit return）都采集 `TOTAL CNY XXXX`（及 ¥/CN¥/RMB、千分位）。此前携程横排、携程竖排、Expedia括号三个格式 short-circuit 时丢失 TOTAL 价格，现全部补齐。

**为什么改**
用户要求：`TOTAL CNY XXXX` 在任何情况下都识别为运价。排查发现多个专用解析器各自 return 时未采集 TOTAL，导致有航段无价格。

**端到端结果（各格式验证）**
全部采集到 TOTAL：
- ✓ 携程带IATA（原已支持）
- ✓ 携程横排（本次补）
- ✓ 携程竖排（本次补）
- ✓ Expedia括号（本次补）
- ✓ Expedia总价（原已支持）

**测试通过（6项+15核心回归 全过）**
- ✓ TOTAL CNY/千分位/无CNY/横排+千分位 各写法
- ✓ 防误伤：无TOTAL时GDS自身价格不被覆盖；无价格时不误采
- ✓ 15项核心回归全通过

**改动位置**
- index.html ~9248：parseSingleBooking 开头定义 _totalFromRaw helper
- index.html ~9441/9532/9599：携程竖排/横排/Expedia括号 return 采用 _totalFromRaw(rmb)

**风险或注意事项**
- ⚠ TOTAL 金额范围 100-5000000 校验，避免证件号等误采
- ⚠ 优先匹配 "TOTAL 数字"，其次 CN¥/¥ 前缀；rmb 已有值时 helper 用 fallback 不覆盖
- ⚠ 这是统一入口，未来新增 short-circuit 格式也应 rmb: _totalFromRaw(rmb)

**回滚方式**
从 .backups/ 找上一版覆盖。
---
## 2026-06-11 — 解析器新增：Expedia "City (IATA) to City (IATA) on Day" 详细格式

**改了什么**
新增 Expedia/OTA 详细行程格式，特点：城市带IATA括号、航班号粘连行尾、时间/中转独立成行、首行是总览：
```
Tokyo (NRT) to Oklahoma City (OKC) on Sun, Jul 5           ← 总览(跳过)
Tokyo (NRT) to Houston (IAH) on Sun, Jul 5United 6         ← 分段1
Houston (IAH) to Oklahoma City (OKC) on Mon, Jul 6United 6189 (operated by...)  ← 分段2
5:00 PM to 3:00 PM on Sun, Jul 5 (12h 0m)Boeing 787        ← 分段1时间
LAYOVER IN IAH (23H 35M)
2:35 PM to 4:15 PM (1h 40m)                                ← 分段2时间
Business / First (C)
```
策略：只取带航班号的分段行（总览行无航班号自动跳过）；时间行按顺序配对分段；航司名→IATA（United→UA）；LAYOVER行忽略；舱位取 Business/First 等。

**端到端结果（用户输入）**
- ✓ UA6 05JUL NRT-IAH 17:00-15:00
- ✓ UA6189 06JUL IAH-OKC 14:35-16:15
- ✓ 舱位 头等舱，未识别清零

**测试通过（6项+16核心回归 全过）**
- ✓ 双段+总览跳过；单段American
- ✓ 防误伤：Expedia紧凑总价、Expedia两行、GDS 不被吞
- ✓ 有括号IATA但无"on Day"不误触发
- ✓ 16项核心回归全通过

**改动位置**
- index.html ~9532：Expedia括号格式块（在Expedia紧凑块前）

**风险或注意事项**
- ⚠ 检测需 (IATA) to + on Day + 航司名，三者齐全防误触发
- ⚠ 时间按顺序配对分段（分段i配时间i）；若时间行数与分段不符可能错位，但常见双段场景正确
- ⚠ 无价格/乘客，纯航段——需另贴

**回滚方式**
从 .backups/ 找上一版覆盖。
---
## 2026-06-11 — SSR解析补强：斜杠周围空格 + 单位数日补零

**改了什么**
1. **斜杠空格容错**：SSR/P 文档行匹配前，从"P/"起把斜杠周围空格清除（"P/CHN/ F4910309/.../  ZHANG/ XIAOMEI/P1" → 紧凑）
2. **单位数日补零**：dob/expiry 单位数日补前导零（2MAR71→02MAR71），与标准两位日格式统一。SSR 和 bare P/ 两条路径都补

**为什么改**
用户 SSR 行斜杠后带多余空格（P/CHN/ 空格证件号、/ 空格姓名），且日期是单位数日，现有严格正则匹配失败，乘客全丢。

**端到端结果（用户输入）**
- ✓ ZHANG/XIAOMEI 男 02MAR71 护照有效期 02MAR30
- ✓ 未识别清零

**测试通过（8项+16核心回归 全过）**
- ✓ 斜杠空格+单位日；姓名前后空格
- ✓ 防误伤：标准SSR、多SSR、无expiry、儿童标签、两位日、带尾价 全不受影响
- ✓ 16项核心回归全通过

**改动位置**
- index.html ~10316：ssrLine 斜杠空格规范化
- index.html ~10331/10367：ssrMatch + bareDocMatch 日期补零

**风险或注意事项**
- ⚠ 斜杠空格清除只从"P/"token起，不影响行首 SSR DOCS 航司 等文本
- ⚠ 补零仅对"单位日+3字母月+2位年"格式，两位日不变

**回滚方式**
从 .backups/ 找上一版覆盖。
---
## 2026-06-11 — 三项识别：无航班号航段 + 姓名分行乘客 + 完整格式

**改了什么**
用户输入含三处盲区，全部支持：
```
8月20日 UA SFO-HKG        ← 无航班号航段
TOTAL CNY 24112
JIANG                     ← 姓（分行）
LI                        ← 名（分行）
08 APR 1960               ← 生日
FEMALE                    ← 性别
```
1. **无航班号航段**：中文日期+航司+路由，无航班号（航班号存航司码待补，如"UA"）
2. **姓名分行乘客**：姓和名在两行（JIANG换行LI），护照卡合并块扩展识别，后接生日/性别时合并为 JIANG/LI

**为什么改**
用户此格式乘客和航段全丢（价格正常）。无航班号航段现有变体都要求航班号；姓名分行现有只认逗号/斜杠同行。

**端到端结果（用户输入）**
- ✓ UA 20AUG SFO-HKG（无航班号，航班号待补）
- ✓ 乘客 JIANG/LI FEMALE 08APR60
- ✓ rmb=24112，未识别清零

**测试通过（9项+16核心回归 全过）**
- ✓ 无航班号航段；姓名分行（性别在前/后两序）；完整端到端
- ✓ 防误伤：逗号姓名、斜杠姓名仍工作；TOTAL CNY/城市对/SSR行 不误判为分行姓名
- ✓ 16项核心回归全通过

**改动位置**
- index.html ~10730：cnDateNoFlight 无航班号航段变体
- index.html ~9954：护照卡块加姓名分行识别（后接DOB/性别才触发，防误判）

**风险或注意事项**
- ⚠ 无航班号航段的 flight 字段仅存航司码，需人工补航班号
- ⚠ 姓名分行只在"两个纯大写词 + 下一行是生日或性别"时触发，且排除 TOTAL/TAX/城市 等词，防止误判
- ⚠ 无航班号变体放在有航班号变体之后，有号时优先

**回滚方式**
从 .backups/ 找上一版覆盖。
---
## 2026-06-11 — 解析器新增：中文日期+航司-路由-航班号顺序

**改了什么**
新增中文日期航段变体：`8月31日 UA LAX-EWR 199 12:40-12:50`。字段顺序特殊——航司在路由前、航班号在路由后（航班号被路由隔开）。

**为什么改**
用户输入此格式，乘客(HUANG/BO)和价格(11117)正常但航段丢失。现有 cnDateEnSeg 都是"航班号紧跟航司"，这次航班号在路由之后，顺序不符。

**端到端结果（用户输入）**
- ✓ UA199 31AUG LAX-EWR 12:40-12:50
- ✓ 乘客 HUANG/BO，rmb=11117，未识别清零

**测试通过（8项+16核心回归 全过）**
- ✓ UA LAX-EWR 199 / 无时间 / B6含数字航司
- ✓ 防误伤cnDateEnSeg：DL38 G、UA 199 经济舱、UA 1195 路由在后 各变体不受影响
- ✓ 其他格式（GDS、中文城市航段）不变
- ✓ 16项核心回归全通过

**改动位置**
- index.html ~10712：cnDateRouteFirst 变体（日期/航司/路由/航班号/时间）

**风险或注意事项**
- ⚠ 整行锚定 + IATA-IATA 路由 + 航班号纯数字，与航班号紧跟航司的变体互斥
- ⚠ 时间可选

**回滚方式**
从 .backups/ 找上一版覆盖。
---
## 2026-06-11 — 新功能：订单菜单「💰 改运价」一键修正总运价

**改了什么**
订单卡片菜单新增「💰 改运价」按钮（在「无位」后）。点击弹出输入框，填总运价（人民币）即可一键修正——用于解析价格不对或没识别到时快速改。

实现：复用现有「一口价」(flatPriceMode) 机制，把输入的总价设为一口价总价模式（优先级高于折扣公式），自动平摊为人均 rmb。表G单也可用。

**为什么改**
用户要求运价快速修正入口。之前改价要进「完整编辑」或依赖解析，现在卡片菜单一键搞定。

**功能细节**
- 弹框默认填当前总价（不跳数）
- 多乘客显示平摊提示「共N人，人均¥X」
- 输入容忍逗号/¥/空格（"25,728"→25728）
- 改后 toast 确认，含人均
- 留空/取消不动；非数字提示无效

**测试通过**
- 价格计算：折扣模式18000 → 改总价15000生效，人均7500；单乘客6314精确
- 审计：语法✅ 重复函数0 console.log0

**改动位置**
- index.html ~14583：菜单加「💰 改运价」按钮
- index.html ~18185：editOrderPrice 函数（prompt输入→设一口价总价→平摊）

**风险或注意事项**
- ⚠ 改运价=设一口价总价模式，覆盖折扣公式。如需改回折扣计价，用现有「↩ 改回折扣」
- ⚠ 用 prompt 而非内联编辑，因不依赖卡片展开状态，更可靠
- ⚠ 多乘客时总价自动平摊为人均存储（与系统 rmb 人均语义一致）

**回滚方式**
从 .backups/ 找上一版覆盖。
---
## 2026-06-11 — 携程带IATA格式：兼容单行无换行粘贴

**改了什么**
携程带IATA逐行格式新增"单行预切分"：当输入是一整行（复制时换行丢失）时，先在关键标记（航空+航班号、日期+时间+IATA、飞行、中转、返程、TOTAL）前插入换行，再走原有按行解析。单行/多行现都支持。

**为什么改**
用户同一格式两次粘贴：上次多行（已支持），这次复制时变成单行无换行（wc -l=1），按行解析切不开 → 航段0。本质是同内容的换行差异，非新航段类型。

**端到端结果（用户单行输入）**
- ✓ 4段：UA199 PVG-LAX / UA2397 LAX-EWR / UA2445 EWR-LAX / UA198 LAX-PVG
- ✓ rmb=33679

**测试通过（17核心回归 全过）**
- ✓ 携程带IATA 单行版 + 多行版 都4段+价格
- ✓ 其他16种格式（含携程横排/竖排、各GDS、Expedia等）全不受影响

**改动位置**
- index.html ~9272：带IATA块入口加单行预切分（≤2行时触发）

**风险或注意事项**
- ⚠ 仅当输入≤2行时才预切分，多行输入不动（零影响）
- ⚠ 切分点是结构标记，普通多行格式不会被误切（预切分只在单行时启用）
- ⚠ 重要：此格式多行版上一版已支持。用户连续两次"不识别"，第一次是部署未生效（多行版我方可解析），第二次是真单行（本次修复）

**回滚方式**
从 .backups/ 找上一版覆盖。
---
## 2026-06-11 — 携程带IATA格式补充：采集TOTAL价格

**改了什么**
携程带IATA逐行格式（多段含中转）的短路返回前，补充采集 TOTAL/CN¥ 价格。之前该格式只出航段、价格为空。

**为什么改**
用户反馈"依旧没有识别"，但经核查该格式航段本就能解析（4段全出）。本次补的是价格：TOTAL CNY 33679 → rmb=33679。

**端到端结果（用户输入）**
- ✓ 4段：UA199/UA2397/UA2445/UA198（含中转返程）
- ✓ rmb=33679（本次新增）

**改动位置**
- index.html ~9312：带IATA块 return 前 TOTAL 价格采集

**风险或注意事项**
- ⚠ 价格采集容忍千分位逗号；范围≥100校验
- ⚠ 注意：该格式航段识别在上一版已实现；若用户线上仍不识别，是部署未生效，非代码问题

**回滚方式**
从 .backups/ 找上一版覆盖。
---
## 2026-06-11 — 中文日期航段支持中文舱位词（8月21日 UA 199 经济舱 PVG-LAX）

**改了什么**
cnDateEnSeg（中文日期前置紧凑航段）的舱位段从仅单字母 `[A-Z]` 扩展为也接受中文舱位词：经济舱/商务舱/头等舱/公务舱/超级经济舱/超经。中文舱位归一化（公务舱→商务舱）并写入 parsedCabin（订单级舱位）；单字母仍作为航段 cls。

**为什么改**
用户输入 `8月21日 UA 199 经济舱 PVG-LAX 20:10-17:20`，乘客(2人)和价格(6314)正常，但航段丢失。根因：舱位是中文"经济舱"，现有正则舱位段只认单字母 → 可选段失败致路由对不齐。（航班号 UA 199 带空格本就被 \s* 兼容。）

**端到端结果（用户输入）**
- ✓ UA199 21AUG PVG-LAX 20:10-17:20
- ✓ cabin=经济舱
- ✓ 2 乘客 ZHOU/XUHAN、ZHOU/FANYUE，rmb=6314，未识别清零

**测试通过（8项+17核心回归 全过）**
- ✓ 中文舱位：经济舱/商务舱/公务舱→商务舱
- ✓ 防误伤：单字母舱位 DL38 G、B6123 I 仍作为 cls；无舱位格式不变
- ✓ 其他格式（GDS、中文城市航段）不受影响
- ✓ 17项核心回归（覆盖今天全部格式）全通过

**改动位置**
- index.html ~10660：cnDateEnSeg 舱位捕获组 + 中文舱位归一化

**风险或注意事项**
- ⚠ 中文舱位写入订单级 parsedCabin（不是航段 cls，因订单单一舱位语义）；单字母仍是航段 cls
- ⚠ 舱位词后必须空格再接 IATA，避免粘连误匹配

**回滚方式**
从 .backups/ 找上一版覆盖。
---
## 2026-06-11 — 解析器新增：携程带IATA逐行格式（多段含中转返程）

**改了什么**
新增携程第3种变体：航司行含"平均到达时间"无舱位，出发/到达行把"日期+时间+IATA+机场名"粘在一行（这次有 IATA 码）：
```
美国联合航空UA199 平均到达时间16:36说明
07-2520:10PVG浦东国际机场 T2
飞行12小时10分
07-25 17:20LAX洛杉矶国际机场T6
```
解析策略：航司行锚点；其后两个"日期+时间+IATA"行=出发/到达（飞行/中转/返程/行李行不解析自动跳过）。直接用行内 IATA，无需城市映射。

**端到端结果（用户输入，4段含中转返程）**
- ✓ UA199 25JUL PVG-LAX 20:10-17:20
- ✓ UA2397 25JUL LAX-EWR 21:00-05:18（中转）
- ✓ UA2445 04AUG EWR-LAX 07:00-09:37（返程）
- ✓ UA198 04AUG LAX-PVG 13:15-17:45
- ✓ 19行全识别，未识别清零

**测试通过（5项+16核心回归 全过）**
- ✓ 4段含中转返程
- ✓ 防误伤：携程竖排（无IATA有当地时间）、横排、中文城市航段、GDS 各走各的不互吞
- ✓ 16项核心回归（覆盖今天全部格式）全通过

**改动位置**
- index.html ~9260：携程带IATA块（在竖排携程块前）

**风险或注意事项**
- ⚠ 三种携程格式现并存：横排粘连(人...航空)、竖排详细(当地时间+中文城市)、带IATA逐行(平均到达时间+IATA)。检测条件互斥
- ⚠ 该格式无价格/乘客，纯航段——需另贴或手动补
- ⚠ 行内必须有 IATA 码；时长行(飞行X小时)和中转行自动忽略

**回滚方式**
从 .backups/ 找上一版覆盖。
---
## 2026-06-11 — 解析器新增：携程竖排详细行程（中文城市+逐行字段+公务舱）

**改了什么**
新增携程竖排详细格式解析器。与上次横排粘连格式不同，这次每个字段单独成行、城市是纯中文无IATA：
```
因09-24 周四 中国香港洛杉矶(美国)
美国联合航空UA821 波音789(大)公务舱
当地时间
21:45
香港国际机场T1
13h25m
当地时间
20:10
洛杉矶国际机场T7
```
解析策略：航司行（含航班号+舱位）为锚点；日期+城市从上方MM-DD行取；下方两个HH:MM行=出发/到达时间，含"机场"的行=出发/到达机场。城市中文名→IATA 用内联映射表（含40个常见城市，避免引用后置声明的 CN_CITY_IATA 造成 TDZ）。公务舱→商务舱。

**端到端结果（用户输入）**
- ✓ UA821 24SEP HKG-LAX 21:45-20:10 商务舱
- ✓ UA152 06OCT LAX-HKG 12:35-18:30
- ✓ 14行全部识别，未识别清零

**测试通过（5项+16核心回归 全过）**
- ✓ 竖排双段+公务舱识别
- ✓ 防误伤：横排携程（上次格式）不被吞、中文城市航段、GDS 不受影响
- ✓ "含航空但无当地时间/MM-DD"不误触发
- ✓ 16项核心回归（覆盖今天全部格式）全通过

**改动位置**
- index.html ~9260：携程竖排块（在横排携程块前）

**风险或注意事项**
- ⚠ 内联城市映射含40城；若遇表外城市航段会丢，需补 V_CITY
- ⚠ 该格式无价格/乘客，纯航段——需另贴或手动补
- ⚠ 用内联映射而非 cnCityToIATA，因块位置早于 CN_CITY_IATA 声明（TDZ）

**回滚方式**
从 .backups/ 找上一版覆盖。
---
## 2026-06-11 — 运价识别补强：同一行多个"类型：金额"（TOTAL 成人：X TOTAL 儿童：Y）

**改了什么**
fareByType 提取支持一行内多个类型价格：
```
TOTAL 成人：25,728 TOTAL 儿童：25,555
```
新增"同行多类型"预处理：全局匹配行内所有 (可选TOTAL)+类型+冒号+千分位金额 对，≥2 对时全部提取。

**为什么改**
上次只支持分两行的 TOTAL 成人：/TOTAL 儿童：，用户实际粘贴时两个 TOTAL 在同一行，整行匹配不到 → 运价丢失。

**端到端结果（用户输入）**
- ✓ UA153/152 往返航段
- ✓ fareByType={adult:25728, child:25555}, rmb=25728
- ✓ 未识别清零（含空序号"3. "行）

**测试通过（10项+18核心回归 全过）**
- ✓ 同行双TOTAL / 无前缀 / 三类型
- ✓ 分行格式（上次的）仍工作；单个TOTAL婴儿仍工作
- ✓ 防误伤：TOTAL CNY、大人运价、同行双运价空格版、裸金额、CNY后缀 全不变
- ✓ 18项核心回归（覆盖今天全部格式）全通过

**改动位置**
- index.html ~10725：fareTypeMatch 前加同行多类型预处理（≥2对触发，要求冒号防误判）

**风险或注意事项**
- ⚠ 要求每对都有冒号，且至少2对才走此路径（单对仍走原 fareTypeMatch）
- ⚠ 金额去逗号；范围 100-500000 校验

**回滚方式**
从 .backups/ 找上一版覆盖。
---
## 2026-06-11 — 运价识别：TOTAL 类型：千分位金额（TOTAL 成人：25,728）

**改了什么**
fareByType 提取支持 `TOTAL 成人：25,728` / `TOTAL 儿童：25,555` 格式——可选 TOTAL 前缀 + 类型词 + 冒号 + 千分位逗号金额。两处改动：
1. fareTypeMatch 外层条件加可选 `TOTAL` 前缀（原只匹配行首类型词，TOTAL开头的行进不来）
2. equivMatch 追加"TOTAL?类型：千分位"模式 + 金额解析去逗号（25,728→25728）

**为什么改**
用户输入 UA153/152 往返 + TOTAL 成人/儿童运价，航段正常但运价丢失。该行以 TOTAL 开头、金额带千分位逗号，两个特征都不被现有逻辑覆盖。

**端到端结果（用户输入）**
- ✓ UA153 06AUG HKG-LAX + UA152 26AUG LAX-HKG
- ✓ fareByType={adult:25728, child:25555}, rmb=25728
- ✓ 未识别清零

**测试通过（10项+18核心回归 全过）**
- ✓ TOTAL成人/儿童千分位、无TOTAL前缀成人：、婴儿千分位
- ✓ 防误伤TOTAL逻辑：TOTAL CNY 13555、TOTAL CNY 17365 成人运价、裸TOTAL金额 不变
- ✓ 防误伤运价格式：大人运价14250、同行双运价、裸金额、CNY后缀 不变
- ✓ 18项核心回归（覆盖今天全部格式）全通过

**改动位置**
- index.html ~10725：fareTypeMatch 加 TOTAL 前缀
- index.html ~10801：equivMatch 追加 TOTAL千分位模式 + 去逗号解析

**风险或注意事项**
- ⚠ 金额去逗号解析对所有 equivMatch 路径生效（之前无逗号格式不受影响，replace无逗号是空操作）
- ⚠ TOTAL CNY 类带货币代码的走更早的专门分支，不受本改动影响

**回滚方式**
从 .backups/ 找上一版覆盖。
---
## 2026-06-11 — 解析器新增：携程App行程格式（高度粘连多行去返程）

**改了什么**
新增携程 App 行程格式专用解析器。这是迄今最难的格式——信息严重粘连跨行、去程返程布局还不同：
```
程07-10周五总时长13h20m
LAX洛杉矶(美国)洛杉矶国际机场T311:45
人美国达美航空公司DL039 空客350-900(大)有餐食〉07-1116:05PVG上海浦东国际机场T1
07-20周一|总时长12h24m返程
18:25PVG上海浦东国际机场T1
人美国达美航空公司DL038|空客350-900(大)有餐食>
15:49LAX洛杉矶(美国)洛杉矶国际机场B
```

解析策略：以"人...航空公司XXNNN"航司行为航段锚点，提取航班号；出发IATA+时间从上方行找、到达IATA+时间从本行或下方行找。兼容两种布局：
- 去程：城市行(LAX...11:45) → 航司行(含目的地 ...16:05PVG)
- 返程：出发行(18:25PVG) → 航司行(无目的地) → 到达行(15:49LAX)
日期"MM-DD周X"映射到最近航段。

**端到端结果（用户输入）**
- ✓ 去程 DL039 10JUL LAX-PVG 11:45-16:05
- ✓ 返程 DL038 20JUL PVG-LAX 18:25-15:49
- ✓ 未识别清零

**测试通过（6项+16核心回归 全过）**
- ✓ 去程返程双段；防误伤中文城市航段/恒途/DL38前置/GDS 不被吞
- ✓ "含航空公司但无MM-DD周"不误触发
- ✓ 16 项核心回归（覆盖今天全部格式）全通过

**改动位置**
- index.html ~9260：携程块（在Expedia总价块前）

**风险或注意事项**
- ⚠ 该格式无价格/乘客，纯航段——价格乘客需另贴或手动补
- ⚠ 依赖航司行"航空公司+航班号"锚点；若航司名后航班号格式不同需再适配
- ⚠ 容器曾重置，本版从 outputs 恢复后继续；.backups 重新计数

**回滚方式**
从 .backups/ 找上一版覆盖。
---
## 2026-06-11 — 解析器新增：Expedia总价格式 + 总价÷人数自动算人均

**改了什么**
新增 Expedia/OTA 紧凑"Total due"格式专用解析器（parseSingleBooking 开头，命中短路）。特点：字段粘连、只给总价、乘客是人数构成：
```
Los Angeles LAX to Honolulu HNLJul 27 . 5:05 pm to 7:44 pm . Nonstop
Travelers: 4
Flight Number: UA 1170Aircraft type: Boeing777-200
2 adults 18+2 children
CN¥26,948.00
```
**核心：总价÷人数自动算人均**。总价 26948 ÷ 4 人 = 人均 6737，存为 paxPrices=[6737,6737,6737,6737]（app 对 paxPrices 求和重建总价，分摊正确）。

解析要点：城市行 IATA + 紧贴日期（HNLJul 27）+ ` . ` 分隔时间；航班号从 Flight Number 提取（粘着 Aircraft 也能切）；人数从"N adults M children"或 Travelers 取；总价支持 CN¥/¥/CNY 带千分位。

**端到端结果（用户输入）**
- ✓ UA1170 27JUL LAX-HNL 17:05-19:44（时间转24h）
- ✓ paxPrices=[6737×4]，人均¥6737
- ✓ rmb=26948（总价保留），备注「（2成人+2儿童，人均¥6737）」
- ✓ 未识别清零

**测试通过（7项 全过）**
- ✓ 4人/2人/含婴儿 总价均分
- ✓ 防误伤：United详细格式、老Expedia两行、GDS 不被吞；无CN¥不误触发
- ✓ 十二套历史回归（97项）全通过

**改动位置**
- index.html ~9260：Expedia总价块（在United块前）

**风险或注意事项**
- ⚠ 人均 = 总价/总人数（平均分摊）。成人儿童单价实际可能不同，但源数据只给总价，平均是唯一可行方案；adult/child 构成已存入备注供人工核对
- ⚠ 检测需同时含 Flight Number: + to + CN¥/¥ + 人数，条件严格防误触发
- ⚠ 乘客为人数非姓名，pax 数组为空，需手动补乘客姓名（或粘贴 SSR）

**回滚方式**
从 .backups/ 找上一版覆盖。
---
## 2026-06-11 — 双修复：无舱位GDS航段 + 乘号折扣(*0.9)与价格行舱位

**改了什么**
1. **无舱位 ETAS 航段**：`1.  UA153     SU28JUN  HKGLAX DK1   1240 1115    SEAME ----` 航班号后直接跟星期、无舱位代码，现有 ETAS 正则要求舱位字母 → 失配。新增无舱位变体（星期 SU/MO 作锚点，6 组匹配 length===6 分支，cls=''），尾部 SEAME/---- 等垃圾忽略
2. **乘号折扣 *0.9**：`3689元/人*0.9` 的折扣此前漏（旧逻辑只认 0.87 两位小数且要关键词）。新增独立乘号折扣捕获 `[*×x]0.9`（一两位小数皆可），不依赖价格走哪条路径
3. **价格行舱位**：`官网普通经济舱价格：...` 的"普通经济"舱位此前漏。新增价格行舱位提取（限价格/元-人/运价/折扣上下文）

**为什么改**
用户输入 UA153 HKG-LAX（4成人），乘客和价格(3689)正常，但航段、折扣(0.9)、舱位(普通经济)三者全丢。

**端到端结果（用户输入）**
- ✓ UA153 28JUN HKG-LAX
- ✓ 4 乘客 XIE/LIU/LIU/JIAN（含 CN 和 CHN 两种国籍写法）
- ✓ rmb=3689, 折扣=90(0.9), 舱位=经济舱, 未识别清零

**测试通过（9项+防误伤 全过）**
- ✓ 无舱位航段 + 尾标垃圾；有舱位 DL7841 仍正常
- ✓ *0.9 / ×0.88 / *0.87 各折扣；价格行舱位提取
- ✓ 防误伤：原 0.87 折扣、GONG/KE 9折端到端、无折扣不误加、航班号*不误判
- ✓ 十一套历史回归（88项）全通过

**改动位置**
- index.html ~9508：segPatterns 加无舱位 ETAS 变体
- index.html ~10747：乘号折扣捕获 + 价格行舱位提取

**风险或注意事项**
- ⚠ 无舱位变体的星期是必需锚点；该格式时间(1240 1115)未捕获（航段/日期/航线齐全，时间可从原文看）
- ⚠ 乘号折扣范围 50-100 校验，避免误判
- ⚠ 价格行舱位需价格/元-人/运价/折扣上下文词，避免裸舱位词误触发

**回滚方式**
从 .backups/ 找上一版覆盖。
---
## 2026-06-11 — 三项识别增强：同行双运价 + 小写名中文逗号 + 婴儿DOB兜底

**改了什么**
1. **同行双类型运价**：`成人  22153  婴儿2308` 一行两个类型+价格，此前只取第一个、第二个丢失。新增 dualFare 预处理，两价都进 fareByType
2. **小写名+中文逗号+点日期**：`Liu/Raina    女，2026.6.16` 此前不识别。cnInlinePax 正则性别后允许中文逗号分隔（原只接受空格）
3. **婴儿 DOB 兜底**：性别只写"女/男"（非"女婴"）时，按出生日期自动判断——2 岁以内标记 INFANT。修复了出生 2026 年的乘客未被识别为婴儿的问题

**为什么改**
用户输入含三处盲区：成人/婴儿同行运价、小写婴儿姓名行、SR DOCS（少个S，但 SSR 解析器仍接住）。前两项整行丢失/丢价。

**端到端结果（用户输入）**
- ✓ 3 个 SSR 乘客（XIONG/LIU/WANG）+ 第4个 LIU/RAINA 女 16JUN26（自动识别婴儿）
- ✓ fareByType={adult:22153, infant:2308}, rmb=22153
- ✓ 未识别清零

**测试通过（10项+6边界 全过）**
- ✓ 同行双运价：成人婴儿/大人儿童/儿童在前 三种顺序
- ✓ 小写名Liu/Raina + 中文逗号 + 点日期2026.6.16
- ✓ 婴儿DOB兜底边界：今年/1岁/2岁→婴儿；3岁/35岁/老人→非婴儿
- ✓ 防误伤：单类型运价、原性别空格分隔、裸金额、人数行、日期行 全部不受影响
- ✓ 十套历史回归（78项）全通过

**改动位置**
- index.html ~10518：dualFare 同行双类型预处理
- index.html ~10226：cnInlinePax 性别后分隔符加中文逗号
- index.html ~10234：cnInlinePax 婴儿 DOB 兜底（≤2岁）

**风险或注意事项**
- ⚠ 婴儿 DOB 兜底阈值 2 岁；2 岁以内一律 INFANT（航空业婴儿票标准 0-2 岁，符合）
- ⚠ dualFare 要求整行恰好两个"类型+数字"对，三个及以上不匹配（未见此格式）
- ⚠ 年份跨世纪判断：两位年 ≤ (今年%100)+1 视为 20xx，否则 19xx

**回滚方式**
从 .backups/ 找上一版覆盖。
---
## 2026-06-11 — 运价识别：类型词+运价/票价紧贴数字（无CNY无冒号）

**改了什么**
fareByType 提取链补一个模式：`大人运价14250` / `儿童运价 14080` —— 类型词 + 运价/票价/净价/价格 + 紧贴或空格分隔的裸数字，无冒号、无 CNY 关键词。

**为什么改**
用户输入 UA821/820 往返 + 大人/儿童运价，航段和乘客（含女童识别为CHILD）正常，但两行运价丢失。已有模式覆盖"运价：14250"（带冒号）和"大人运价 ADT 14250 CNY"（带后缀），漏了"运价14250"这种紧贴无后缀写法。该行被 fareTypeMatch 命中却提不出金额，静默丢失（不在未识别名单）。

**端到端结果（用户输入）**
- ✓ UA821 HKG-LAX + UA820 LAX-HKG 往返
- ✓ REN/YANQING 女 + WANG/JINGYU 女童(CHILD)
- ✓ fareByType={adult:14250, child:14080}, rmb=14250, 未识别清零

**测试通过（10项 全过）**
- ✓ 大人运价14250紧贴 / 儿童运价 14080空格 / 成人票价 / 婴儿净价
- ✓ 防误伤：带冒号(成人运价：10785)、带CNY(ADT 43648 CNY)、裸金额(儿童 8200)、TOTAL链 各路径不变
- ✓ 日期行(婴儿 2026年1月1日)、人数行(成人 2 儿童 1) 不误判
- ✓ 九套历史回归（GONG/KE 6 + AS120 8 + UA820 4 + Expedia 9 + 护照卡 10 + United 6 + 城市 7 + B6 9 + DL38 9 = 68项）全通过

**改动位置**
- index.html ~10595：equivMatch 提取链尾部追加"运价/票价紧贴数字"模式

**风险或注意事项**
- ⚠ 必须有"运价/票价/净价/价格"关键词 + 整行锚定 + 4-6位数字，避免误吞普通文本
- ⚠ 金额范围校验 100-500000 沿用既有

**回滚方式**
从 .backups/ 找上一版覆盖。
---
## 2026-06-11 — 解析器扩展：日期前置紧凑航段支持舱位代码 + AM/PM时间

**改了什么**
扩展现有 cnDateEnSeg 分支（中文日期+英文IATA紧凑格式），新增两项能力：
```
9月19日 DL38 G PVG-LAX 6:25 PM - 3:25 PM
```
1. **可选舱位代码**：航班号后单字母舱位（DL38 **G** PVG-LAX）
2. **AM/PM 时间**：12 小时制带 AM/PM + 空格（6:25 PM → 18:25），自动转 24h
3. 顺带：航司代码支持含数字（B6/9C/3U），原来只 [A-Z]{2}

**为什么改**
用户提供新格式（日期前置 + 舱位 + AM/PM），现有 cnDateEnSeg 只认 24h 无舱位格式，整行未识别。

**端到端结果（用户输入）**
- ✓ DL38 19SEP PVG-LAX 18:25-15:25 [舱位G]
- ✓ 6:25 PM→18:25、3:25 PM→15:25 正确转换
- ✓ 未识别行清零

**测试通过（9项+防误伤 全过）**
- ✓ 舱位代码 + AM/PM；含数字航司 B6+舱位；AM 时间转换
- ✓ 防误伤原格式：24h无舱位(UA1195)、航班号连写(UA805)、无时间(CA888) 全不变
- ✓ 防误伤其他格式：中文城市航段(UA820)、GDS(AS120)、B6含数字GDS(B61523) 不被吞
- ✓ 八套历史回归（GONG/KE 6 + AS120 8 + UA820 4 + Expedia 9 + 护照卡 10 + United 6 + 城市 7 + B6 9 = 59项）全通过

**改动位置**
- index.html ~10311：cnDateEnSeg 正则 + 处理块（舱位捕获组、AM/PM 转换 conv()）

**风险或注意事项**
- ⚠ 舱位代码捕获组是可选的，必须是单个大写字母在航班号后空格分隔；不影响无舱位的原格式
- ⚠ AM/PM 可选，无 AM/PM 时按 24h 原样保留
- ⚠ 路由仍要求 IATA-IATA（3字母）连字符/箭头分隔

**回滚方式**
从 .backups/ 找上一版覆盖。
---
## 2026-06-11 — 表G自动识别：触发词→CHASE TRAVEL·92折·基础经济标识

**改了什么**
新增表G批量自动识别。粘贴文本含 `表G`/`表 G` 或 `官网[爱心]`（[爱心] 支持文字"[爱心]"、爱心、❤♥💗💖♡ emoji）时，整批订单自动：
1. tableLabel='G' → 归入 💳 CHASE TRAVEL 标签（复用现有路由，自动排除出待出票）
2. 强制 92 折（基础经济价 × 92%）
3. 折扣备注「表G·基础经济·人工预定」
4. CHASE 卡片头部 + 待出票渲染加绿色「♡ 基础·人工」徽章，悬停显示「基础经济 · 92折 · 需人工客服预定」

**为什么改**
用户要求表G像表Y一样自动识别打标。表G在系统里已是现成 tableLabel（=CHASE TRAVEL，已有 92% 规则、已从待出票/USD面板排除），缺的是"从文本标记自动设为表G"这一环。本次补齐触发→打标→徽章全链路。

**端到端结果（用户6单输入）**
全部正确解析：
- 单1 UA888 SFO-PEK 2客 ¥5045
- 单2 DL388 PVG-DTW 1客 ¥10398
- 单3 UA858/857 PVG-SFO往返 2客 ¥11611
- 单4 UA869 SFO-HKG 1客 ¥5761/$849.50
- 单5 UA153/152 HKG-LAX往返 2客 ¥8794/$1296.93
- 单6 UA1429/877 MCI-SFO-HKG 2客 ¥6311（含带空格英文名 GESNER/SOPHIA JADE）
- 6单全部 0 行未识别，均自动归入 CHASE TRAVEL + 92折 + 徽章

**测试通过**
- 触发词检测 12 项：表G/表 G/官网[爱心]/官网爱心/官网❤ 等命中；表A/表B/"表Group缩写"/第5单 等正确不触发（\b边界保护）
- 八套历史回归（GONG/KE 6 + AS120 8 + UA820 4 + Expedia 9 + 护照卡 10 + United 6 + 城市 7 + B6 9 = 59项）全通过
- 审计：语法 ✅ 重复函数 0 console.log 0

**改动位置**
- index.html ~11666：parsePNR 加 _isTableGBatch 检测 + sharedTableLabel 覆盖
- index.html ~11741：折扣链加表G分支（92折，优先级仅次于手动特殊折扣）
- index.html ~12503：待出票卡头部表G徽章
- index.html ~21294：CHASE卡片头部表G徽章

**风险或注意事项**
- ⚠ 表G触发后覆盖录入框选的 tableLabel（即使手选了表A也会被改成表G）——这是预期行为，文本标记优先
- ⚠ 92折与现有 matchDiscountRule 的表G规则一致（双保险）；若将来要改表G折扣率，两处都要改：本检测分支 + settings.productDiscounts
- ⚠ "官网"单独出现不触发，必须配爱心；"表G"可单独触发

**回滚方式**
从 .backups/ 找上一版覆盖。
---
## 2026-06-11 — 解析器修复：含数字航司代码（B6/9C/3U）GDS航段

**改了什么**
ETAS/TravelSky GDS 航段正则的航司代码从 `[A-Z]{2}` 放宽为 `[A-Z0-9]{2}` + 前瞻 `(?=[A-Z0-9]{0,1}[A-Z])`（确保至少含一个字母，非纯数字）。支持含数字的两字母航司代码：B6(JetBlue)、9C(春秋)、3U(川航)、5X、2I 等。

**为什么改**
用户粘贴 B6 航段：
```
1.  B61523 I   TH09JUL  JFKLAX DK1   1515 1816   M 0 RE 5 1
```
乘客(YU/JIACE)、价格(USD 1236.28/EQUIV CNY 8370/TOTAL CNY 9103)、舱位(公务→商务舱)全部正常，仅航段丢失。根因：`B6` 航司代码含数字 6，被 `[A-Z]{2}` 拒绝。"未识别行"提示精准点名该航段。

**端到端结果（用户输入）**
- ✓ B61523 09JUL JFK-LAX 1515-1816 I（舱位代码）
- ✓ 乘客 YU/JIACE 女 19APR90 护照有效期 30JAN33
- ✓ rmb=9103、usd=1236.28、cabin=商务舱、fareByType={adult:9103}、未识别行清零

**测试通过（9项+防误伤 全过）**
- ✓ B6/9C/3U 三种含数字代码识别
- ✓ 防误伤：纯字母 DL/AS/UA 航段不变；序号"1."不被误当航司码（前瞻保证含字母）
- ✓ 其他格式（中文航段、Expedia、护照卡）不被吞
- ✓ 七套历史回归（GONG/KE 6 + AS120 8 + UA820 4 + Expedia 9 + 护照卡 10 + United 6 + 城市 7 = 50项）全通过

**改动位置**
- index.html ~9505：ETAS segPattern 航司代码捕获组

**风险或注意事项**
- ⚠ 仅放宽 ETAS 这一个正则（带星期前缀 TH/MO 等的 GDS 格式）。其他 segPattern 仍 [A-Z]{2}；若后续出现别的格式的含数字代码航段，同样手法逐个放宽
- ⚠ 前瞻 (?=[A-Z0-9]{0,1}[A-Z]) 确保代码至少 1 个字母，排除纯数字误匹配

**回滚方式**
从 .backups/ 找上一版覆盖。
---
## 2026-06-11 — 城市映射补充：圣安娜等41个美国二三线机场（修复中文航段丢失）

**改了什么**
CN_CITY_IATA 城市映射表补充 41 个此前缺失的美国二三线机场城市名（含别名去重后）。关键：圣安娜(SNA)、棕榈泉(PSP)、长滩(LGB)、孟菲斯(MEM)、图森(TUS)、里诺(RNO)、罗利(RDU) 等。

**为什么改**
用户粘贴 UA5351 圣安娜-旧金山 航段，乘客(2人)/价格(2599)/舱位(头等舱)全部正常，但航段丢失。根因不是解析逻辑——cnSeg 正则能匹配，是 `圣安娜`(Santa Ana/SNA) 不在城市映射表里，cnCityToIATA 返回 null 导致航段被丢弃。这是数据缺口，非逻辑 bug。

**端到端结果（用户输入）**
- ✓ UA5351 22JUN SNA-SFO 16:12-17:50
- ✓ 乘客 YU/QI、NIU/SIHAN（含 YY 占位航司、护照有效期）
- ✓ rmb=2599、cabin=头等舱、未识别行清零

**测试通过（7项+防误伤 全过）**
- ✓ 圣安娜→SNA / 棕榈泉→PSP / 长滩→LGB / 孟菲斯→MEM
- ✓ 防误伤：旧金山仍→SFO、北京仍→PEK
- ✓ 关键防歧义：圣何塞胡安→SJO 不被新增的"圣荷西"部分匹配误伤
- ✓ 六套历史回归（GONG/KE 6 + AS120 8 + UA820 4 + Expedia 9 + 护照卡 10 + United 6 = 43项）全通过

**补充清单**
圣安娜/橙县/约翰韦恩(SNA)、棕榈泉(PSP)、伯班克(BUR)、安大略机场(ONT)、长滩(LGB)、圣荷西(SJC)、图森(TUS)、阿尔伯克基(ABQ)、埃尔帕索(ELP)、里诺(RNO)、博伊西(BOI)、斯波坎(GEG)、塔尔萨(TUL)、俄克拉荷马城(OKC)、奥马哈(OMA)、得梅因(DSM)、麦迪逊(MSN)、水牛城/布法罗(BUF)、罗切斯特(ROC)、奥尔巴尼(ALB)、哈特福德(BDL)、普罗维登斯(PVD)、里士满(RIC)、罗利(RDU)、杰克逊维尔(JAX)、萨凡纳(SAV)、查尔斯顿(CHS)、孟菲斯(MEM)、路易斯维尔(SDF)、代顿(DAY)、大急流城(GRR)、诺克斯维尔(TYS)、亨茨维尔(HSV)、伯明翰(BHM)、小石城(LIT)、威奇托(ICT)

**改动位置**
- index.html ~9460：CN_CITY_IATA 表末尾追加美国二三线机场段

**风险或注意事项**
- ⚠ 刻意排除歧义项：用"安大略机场"而非"安大略"（避免匹配加拿大省）；未加"杰克逊/斯普林菲尔德"（多城市同名）；未加"奥克兰加州/圣何塞加州"（奥克兰/圣何塞已有映射）
- ⚠ 部分匹配兜底仍存在：若未来出现含这些城市名子串的其他地名可能误匹配，目前未见
- 此次仅补数据，解析逻辑零改动，回归风险极低

**回滚方式**
从 .backups/ 找上一版覆盖。
---
## 2026-06-11 — 解析器新增：United/航司官网超详细行程（标签分行 + 多航段块）

**改了什么**
新增 United 官网行程格式专用解析器（在 parseSingleBooking 开头，命中即短路返回）。这是迄今最复杂的格式——信息高度分散、靠标签词组织、多航段：
```
Flight 1 of 2
departs at
12:10 pm on Aug. 9
from
Shanghai Pudong International Airport (PVG)
...
arrives at
8:55 am on Aug. 9
to
San Francisco International Airport (SFO)
* Business
* United Airlines UA 858
Flight 2 of 2
...
```
解析方式：按 `Flight N of M` 切块；块内用标签词（departs at / from / arrives at / to）定位值行；机场 IATA 取括号内 `(XXX)`；航班号取 `* United Airlines UA 858`→UA858；舱位 `* Business`→商务舱；时间 12:10pm→24h；日期 `Aug. 9`→09AUG。

**为什么改**
用户粘贴 United 两段行程（PVG-SFO-PSP），44 行全部未识别。这种格式与所有现有路径都不同，需独立处理。

**端到端结果（用户输入）**
- ✓ UA858 09AUG PVG-SFO 12:10-08:55 商务舱
- ✓ UA5705 09AUG SFO-PSP 18:38-20:26 头等舱
- ✓ 44 行未识别清零

**测试通过（6项变体+防误伤 全过）**
- ✓ 单航段 / 多航段 United
- ✓ 舱位 Business→商务舱 First→头等舱 Economy→经济舱
- ✓ am/pm 时间转 24h；Aug. 9 带点日期
- ✓ 防误伤：检测要求同时含 Flight N of M + departs at + arrives at 三特征，GDS/Expedia/中文航段/护照卡 全部不被误吞
- ✓ 五套历史回归（GONG/KE 6 + AS120 8 + UA820 4 + Expedia 9 + 护照卡 10 = 37项）全通过

**改动位置**
- index.html ~9259：parseSingleBooking 共享变量声明后插入 United 块解析器 + 短路 return

**风险或注意事项**
- ⚠ 此格式为纯行程，无价格/乘客——需另行粘贴或手动补
- ⚠ 航班号取块内首个"航司名+代码数字"组合；若某航段缺 `* Airline XX NNN` 行则航班号留空（待补）
- ⚠ 跨日界线航段（如 PVG-SFO 到达时间早于出发）按原值保留，不强行加跨天标记
- ⚠ Operated by / 机型 / 设施等行直接忽略（非行程核心字段）

**回滚方式**
从 .backups/ 找上一版覆盖。
---
## 2026-06-11 — 护照卡解析增强：中文逗号姓名 + OCR月份修正（0CT→OCT）

**改了什么**
扩展现有"护照卡多行合并"逻辑，识别此前漏识别的证件 OCR 粘贴：
```
BAO, DANYANG          ← 英文逗号 姓,名
26 0CT 1967           ← OCR: 0CT 实为 OCT（零→O）
女/F

GU， JEFFREY          ← 中文逗号 ，
20 0CT 2018
M
```
三处修复：
1. **姓名逗号**：正则从仅 ASCII `,` 扩展为 `[,，]`，接受全角中文逗号（护照 OCR/输入法常产出）
2. **OCR 月份修正**：DOB 行的 3 字母月份段先做字符还原 `0→O 1→I 5→S 6→G 8→B`，修复 `0CT→OCT`、`AU6→AUG` 等。仅作用于日与年之间的月份捕获组，不碰日/年数字
3. （性别行 DOB 行顺序无关、4位年份已由原逻辑支持）

**为什么改**
用户粘贴护照识别文本，两个乘客全部丢失。"未识别行"提示点名两个姓名行（`BAO, DANYANG` / `GU， JEFFREY`），定位精准。根因是逗号类型 + OCR 月份双重阻断。

**端到端结果（用户输入）**
- ✓ BAO/DANYANG 女 26OCT67
- ✓ GU/JEFFREY 男 20OCT18
- ✓ 未识别行清零

**测试通过（10项变体+防误伤 全过）**
- ✓ 英文逗号 / 中文逗号 姓名
- ✓ 正常 OCT 不被破坏；OCR AU6→AUG 修复
- ✓ 原有斜杠卡格式（GAO,PENG 男/M 21 MAY 1989）不变
- ✓ 两位年份 09 JAN 88 不变
- ✓ 防误伤：税费逗号行(INCL TAX, FEE)、标准斜杠姓名(GONG/KE)、SSR证件行、城市名(Shanghai, China) 全部不误判
- ✓ 四套历史回归（GONG/KE 6 + AS120 8 + UA820 4 + Expedia 9 = 27项）全通过

**改动位置**
- index.html ~9478：nameCard 正则逗号类型 `[,，]`
- index.html ~9495：DOB 行匹配前插入 OCR 月份字符还原

**风险或注意事项**
- ⚠ OCR 月份还原仅在"日 月 年"三段结构且月份为 3 字符时触发，孤立数字不受影响
- ⚠ 6→G 的还原：若真实月份缩写本应含数字（无此情况，所有月份纯字母）才会有风险——安全
- ⚠ 该格式无证件号/航段，仅姓名+性别+生日；护照号需另行粘贴或手动补

**回滚方式**
从 .backups/ 找上一版覆盖。
---
## 2026-06-11 — 解析器新增：Expedia/OTA 英文行程格式（城市行+日期行两行配对）

**改了什么**
新增 `cityCarry` 两行航段配对机制，识别此前完全漏识别的 Expedia/在线旅行社英文行程：
```
Shanghai (PVG) PVG to Los Angeles LAX  BUSINESS
Jul16·8:10 pm to 5:20 pm ·Nonstop
Los Angeles LAX to Shanghai (PVG) PVG   BUSINESS
Aug 20 · 1:15 pm to 5:45 pm · Nonstop
TOTAL CNY 31076.204
```

格式特征（与现有所有航段格式都不同）：
1. 路由+舱位在第一行，日期+起降时间在第二行
2. **无航班号**——生成的航段 flight 留空（沿用现有 partial 语义，待人工补）
3. 城市端点 IATA 码出现一或两次：`City (XXX) XXX` 或 `City XXX`
4. 日期英文月份，有空格(`Aug 20`)或无空格(`Jul16`)皆可
5. 时间中点 `·` 分隔 + am/pm，自动转 24 小时制
6. 舱位 BUSINESS/ECONOMY/FIRST/PREMIUM → 商务舱/经济舱/头等舱/超级经济舱

**为什么改**
用户粘贴该格式订单，价格(TOTAL CNY 31076.204)识别正常但两段航段全空。"未识别行"提示点名全部 4 行航段，定位精准。

**端到端结果（用户输入）**
- ✓ PVG-LAX 16JUL 20:10-17:20 商务舱
- ✓ LAX-PVG 20AUG 13:15-17:45 商务舱
- ✓ rmb=31076.204，cabin=商务舱，未识别行清零

**测试通过（9项变体+防误伤 全过）**
- ✓ Jul16 无空格 / Aug 20 有空格 日期
- ✓ 单 IATA 端点（无括号重复）
- ✓ am 时间正确转换（9:00am→09:00）/ pm（8:10pm→20:10）
- ✓ 舱位映射 BUSINESS→商务舱 ECONOMY→经济舱
- ✓ 防误伤：恒途中文航段、UA820 IATA前缀、GDS两行(partial+route)、乘客行、"含to无IATA的英文备注行" 全部不受影响
- ✓ 三套历史回归（GONG/KE 6 + AS120 8 + UA820 4）全通过

**改动位置**
- index.html ~9398：新增 cityCarry 变量声明（紧邻 segCarry）
- index.html ~9565：segCarry 处理块后插入 cityCarry 日期行消费 + 城市行检测两段逻辑

**风险或注意事项**
- ⚠ 航段无航班号，列表里该航段航班号列为空，需人工补填航班号
- ⚠ 城市行要求出现至少 2 个 3 字母大写 IATA 码 + 中间有 "to"。纯文本含 "to" 但无 IATA 码（如英文备注）不会误判（已测）
- ⚠ 跨天到达（如夜航次日到）未单独标记 +N，与现有航段一致

**回滚方式**
从 .backups/ 找上一版覆盖。
---
## 2026-06-11 — 出发倒计时标记扩展：72h三档 → 全程九级渐进

**改了什么**
`buildImminentDepartureHTML` 从"仅 ≤72h 显示三档"扩展为覆盖全部未来出发日的九级系统。`getHoursUntilDeparture` 计算逻辑未动（复用现有日期/年份推断/depTime 解析）。

分级（越临近颜色越重）：
| 触发 | 显示 | 颜色 |
|---|---|---|
| ≤24h | 24h | 红 #b7321f |
| ≤48h | 48h | 橙红 #a85518 |
| ≤72h | 72h | 橙 #a85518 |
| ≤7天 | N天 | 琥珀 #8b6914 |
| ≤14天 | N天 | 金 #7d6510 |
| ≤31天 | N天 | 绿 #2f7d3f |
| ≤95天 | N个月 | 青 #246b6b |
| >95天 | N个月 | 灰 #5a5a66 |
| 已起飞>2h | （不显示） | — |

**为什么改**
用户要求标记更全面——之前只有临近 72 小时才有提示，远期航班看不出还有多久。现在 10天/1个月/3个月/5个月 等都有对应等级和颜色，一眼区分紧急度。

**实现要点**
- 单位自适应：小时档保留 24/48/72 整数节奏；周内/月内显示实际天数；超月显示「N个月」（round(天/30)）
- 颜色用调色板 helper `C{}` + `pick()` 收敛，避免大段重复内联样式
- tooltip 精确到天：「约 3 个月后起飞（90 天）」
- 两个调用点（gradient chip ~12255、列表 ~13531）只接收返回 HTML，无需改动

**测试通过**
- 12 档边界验证：12h→24h红 / 1.5天→48h / 2.5天→72h / 5天→琥珀 / 10天→金 / 20天→绿 / 1-3个月→青 / 5个月→灰 / 已起飞→空，颜色与文字全部正确
- 三套解析回归（GONG/KE 6项 + AS120 8项 + UA820 4项）全部保持通过
- 审计：语法 ✅ 重复函数 0 console.log 0

**改动位置**
- index.html ~7386：buildImminentDepartureHTML 整体重写

**风险或注意事项**
- ⚠ 月份用 30 天近似（round），1个月=31天边界、3个月=90天边界显示平滑，无跳变
- 标记每分钟随渲染刷新（现有 re-render 机制），倒计时会自然推进
- 列表里现在每个未起飞订单都会带标记（之前只有 72h 内才有）——若觉得远期标记太多想只保留 N 天内，告诉我加阈值

**回滚方式**
从 .backups/ 找上一版覆盖。
---
## 2026-06-11 — 解析器修复：中文航段（航班号带舱位代码 + IATA前缀城市名）

**改了什么**
新增第三个中文航段正则 `cnSegIata`，处理此前漏识别的格式：
```
1. UA820 W  06月15日(星期一)  LAX洛杉矶 - HKG香港  23:55  05:40+2
```
与现有两个 cnSeg 正则的差异（三处同时存在导致全部失配）：
1. 航司代码前**无中文**（现有正则要求 `[\u4e00-\u9fa5]*([A-Z]{2})`，UA 前没中文时 LAX 的 LA 会被误当航司码）
2. 航班号与日期间夹**单字母舱位代码** `W`（现有正则航班号后直接接 `\d+月`，无舱位位置）
3. 城市名自带 **IATA 前缀**（LAX洛杉矶 / HKG香港）——直接捕获前 3 字母，不依赖 cnCityToIATA 映射表

**为什么改**
用户粘贴 UA820 LAX-HKG 订单，乘客和价格（含税:6832元/人）都正常，但航段为空。"未识别行"提示直接点名该航段行，定位到位。

**端到端结果（用户输入）**
- ✓ 航段 UA820 15JUN LAX-HKG 23:55-05:40
- ✓ 乘客 LIANG/YULING（含 YY 占位航司、4位年护照有效期 20MAR2027）
- ✓ rmb=6832，未识别行清零

**防误伤测试通过（现有4种中文航段格式全部不变）**
- ✓ 恒途带星期+航站楼 AS1352 06月03日(星期三) 洛杉矶T6 → LAX-SJO
- ✓ 中文航司名 联合航空UA771 洛杉矶-北京首都 → UA771 LAX-PEK
- ✓ 裸点格式 . UA869 旧金山I-香港T1 → SFO-HKG
- ✓ IATA前缀城市（无舱位）CA988 PVG上海-LAX洛杉矶 → PVG-LAX
- ✓ GONG/KE 套件、AS120 行李/婴儿运价套件 全部保持通过

**改动位置**
- index.html ~10130：cnSeg 链后插入 cnSegIata 分支（独立 if，匹配后 continue；不触碰现有两个正则）

**风险或注意事项**
- ⚠ cnSegIata 要求城市名以 3 个大写字母开头。若某天出现纯中文城市名 + 舱位代码的组合，走的是现有 cnSeg 分支（不带舱位），仍需后续补——目前用户数据未见
- 到达时间取行内最后一个时间值（05:40），跨天标记 +2 未单独存储（与现有航段一致）

**回滚方式**
从 .backups/ 找上一版覆盖。
---
## 2026-06-11 — 解析器修复×2：行李件数复合运价行 + 类型裸金额行

**改了什么**
1. **pBagFare 扩展**：`2PC 16046  2pc *4`（前缀件数 + 金额 + 重复件数 + *人数）此前两个严格锚定正则都不匹配 → rmb 为空。现支持可选尾部 `\d+pc` 与 `[*xX×]N` 人数后缀，并兼容 pc 前空格。同时该分支金额镜像写入 `fareByType.adult`（与 TOTAL CNY 分支行为一致），保证成人+婴儿混贴时分型完整。
2. **类型运价提取链加第 5 模式**：`婴儿 1713  0pc` 此前被 `^(婴儿|…)` 分支吃掉但 4 个金额模式全要求 CNY/价格关键词/冒号 → 金额静默丢失，只设了 `_lastFareType`。新模式：类型词后裸金额 + 可选 pc/人数后缀，整行锚定（`婴儿 2026年1月1日` 日期行、`成人 2 儿童 1` 人数行不会误中），复用既有赋值代码（rmb/fareByType/范围校验）。

**为什么改**
用户粘贴 AS120 ICN-SEA 订单（4成人+1婴儿）解析不出价格。新上线的"未识别行"提示直接点名 `2PC 16046  2pc *4`；`婴儿 1713` 则是被吞型故障（不在未识别名单里，靠对比 fareByType 为空发现）。

**端到端结果（用户输入）**
- ✓ 4 乘客全解析（含 SSR `航司` 中文占位、USA 护照）
- ✓ rmb=16046，fareByType={adult:16046, infant:1713}
- ✓ 末尾截断行 `SSR DOCS CA HK1 P/SAS/A8` 正确拒绝（未造假乘客）且被未识别提示报告——婴儿证件行不完整，需人工补

**防误伤测试通过（8项）**
- ✓ 旧式 `2pc 16046` / `16046 2pc` 不变
- ✓ `婴儿 2026年1月1日` 日期行、`成人 2 儿童 1` 人数行不误判
- ✓ 传统 CNY 链（大人运价 ADT 43648 CNY / 婴儿运价 INF 3425 CNY）不变
- ✓ TOTAL CNY 17365 成人运价 链不变
- ✓ `儿童 8200` 裸价 → child 不占 rmb；`成人 16046 2pc *4` → adult+rmb
- ✓ GONG/KE 套件、未识别行套件 全部保持通过

**改动位置**
- pBagFare 正则 + 处理块（~10619/~10668）
- fareTypeMatch 的 equivMatch 提取链尾部追加模式（~10310 附近）

**风险或注意事项**
- ⚠ 类型词后裸 3-6 位数字现在会被当运价（整行锚定保护下）。若出现"婴儿 1234"实为非价格语义的行，会误收——目前未见此类格式
- 婴儿乘客本体因源数据截断无法生成，需手动添加该婴儿

**回滚方式**
从 .backups/ 找上一版覆盖。
---
## 2026-06-11 — 代码审查：清理387行死代码 + 解析"未识别行"自动提示

**改了什么**
1. 删除 19 个从未被调用的死函数（共 387 行，文件 26146→25789 行）：_speedColor、orderSummary、getEffectiveMultiplier、scrollToOrderCard、showBackToTopButton、checkPassportExpiry、detectPriceAnomalies、buildOverflowMenu、highlightNextStep、getAutoAgeClass、displayCabin、updatePendingCardholder、refreshAllOrders、getViewModeFilter、promptChaseCardNumber、moveOrderToActionsByIdx、moveTicketedToActionsByIdx、setChasePanelField、tt
2. 删除孤儿变量 `_parseDebugLog`（声明后从未读写——路线图遗留项闭环）
3. **新功能**：解析"未识别行"自动提示——任何解析分支都没消费的行，解析完成 2.4 秒后 toast 警告（显示前 2 行 + 总数），**不需要开 🔍 调试**。噪音过滤：纯数字/符号行、短标签行（如"乘机人："）、不足 4 字符的行不报。
4. `toast()` 支持可选时长参数 `toast(msg, ms)`，默认 2200ms 行为不变

**为什么改**
用户要求整体审查优化。死函数是历次功能迭代的残留（最大的 refreshAllOrders 有 119 行）。未识别行提示直接针对 GONG/KE 静默丢失这类问题——以后粘贴时立即可见，不用等对账才发现少了人。

**审查中确认正常、未改动的**
- 每日备份提醒 / 滚动快照清理：是 IIFE 自执行，工作正常（检测误报，非死代码）
- updatePricing 等 5 个一行函数：注释明确标注的兼容垫片，保留
- doPost/doGet：是给用户复制到 Apps Script 的模板字符串内容，非死代码
- 🔍 调试面板：已完整接通，开启后正常显示逐行解析日志（旧记录"面板恒空"已过时）
- console.warn×11 / console.error×3：诊断用途，按规范保留

**测试通过**
- 回归：GONG/KE 端到端 + 6 项防误伤全部保持通过
- 新功能 3 组：正常输入无误报 / 垃圾行捕获 / 纯符号与标签行已过滤
- 审计：语法 ✅ 重复函数 0 console.log 0 死函数残留引用 0

**改动位置**
- ~7534 删孤儿变量；~9551 `_d` 双轨改造（调试日志照旧 + 未识别常开收集）；parseSingleBooking 返回对象加 `unrecognizedLines`；parsePNR 聚合 + 延迟 toast；`toast()` 签名；19 处函数整体删除

**风险或注意事项**
- ⚠ 未识别行提示初期可能偏敏感（如自由文本备注行会被报）——遇到烦人的告诉我，加过滤词即可
- ⚠ 若有极老缓存页面引用被删函数会报错，Ctrl+Shift+R 后无此问题

**回滚方式**
从 .backups/ 找上一版覆盖。

---
## 2026-06-11 — 修复乘客名撞航司代码被误删（GONG/KE 丢失）

**改了什么**
`isMetaName` 新增强上下文参数：同一行带 男/女+出生日期 时跳过"名字=航司代码"过滤。

**为什么改**
用户输入解析缺一个乘客：
```
GONG/KE  男  1981年 6月24日
LIU/MINGZHU  女  1983年1月19日
```
诊断：`isMetaName` 的航司代码启发式（本用于过滤 FSICH/CA 类 GDS 元数据行）把 **KE（大韩航空代码）**当成元数据标记，GONG/KE 整行被丢弃。日期里的空格（1981年 6月24日）是无辜的，extractDOB 本就支持。同理会受影响的常见名拼音：KE（柯/可/科）、BA（巴）。

**修复方案**
- `isMetaName(n, hasStrongPaxContext)`：强上下文（性别+生日同行）时跳过航司代码判定；精确元词表和 FSI 前缀过滤不变
- 仅 cnInlinePax 分支以 `true` 调用；其余路径（nameRe 兜底等）过滤强度不变

**端到端测试（用户输入）通过**
- ✓ GONG/KE 男 24JUN81 + LIU/MINGZHU 女 19JAN83 两乘客齐全
- ✓ DL280/DL281 双航段、7401×0.9（识别为9折）、经济舱 全部正常

**防误伤测试通过（6项）**
- ✓ FSICH/CA 无上下文仍过滤
- ✓ WANG/KE 女 1990年5月1日 → 正常识别
- ✓ 儿童行 LIU/QINGHE 儿童 女10JAN2022 正常
- ✓ 婴儿撞码 CHEN/BA 女婴 → 正常识别为婴儿
- ✓ 普通经济6129 舱位粘连价正常
- ✓ 无上下文裸名 SMITH/KE 仍过滤（保守策略）

**改动位置**
- index.html ~10003：isMetaName 签名 + 航司代码分支加 `!hasStrongPaxContext` 条件
- index.html ~10053：cnInlinePax 提交处改为 `isMetaName(name, true)`

**风险或注意事项**
- ⚠ 无性别/生日上下文的裸名行，若名恰为航司代码（如单独一行 SMITH/KE）仍会被过滤——保守策略；SSR/证件行有自己的解析路径，不受影响
- 顺带确认：`普通经济7401×0.9` 的价格+折扣+舱位本就由其他分支正确解析，此次未改动

**回滚方式**
从 .backups/ 找上一版覆盖。回滚后 GONG/KE 类名字又会丢失。

---
## 2026-06-10 — 修复 SSR 折行导致的假乘客（乘客数错误）

**改了什么**
新增 SSR 续行合并预处理：GDS 终端折断的 SSR 证件行自动拼回一行，修复乘客数识别错误。

**为什么改**
用户反馈这段识别错误（实际只有 2 个乘客）：
```
SSR DOCS MU HK1 P/USA/A88984286/USA/04JUL08/F/07MAY36/KRAFT/CHRISTINE
TIANTIAN/P1
SSR DOCS MU HK1 P/USA/572194822/USA/15MAY72/F/23MAY27/WANG/YANLIALICE/P2
```

诊断：第一条 SSR 被 GDS 终端宽度**折断成两行**——`...KRAFT/CHRISTINE`（没有 /P1 结尾）+ 孤立的 `TIANTIAN/P1`。真实乘客名是 **KRAFT/CHRISTINE TIANTIAN**（名字两个词）。

旧解析的错误行为：
- 行3 SSR 匹配成 KRAFT/CHRISTINE（名字缺 TIANTIAN）
- 行4 "TIANTIAN/P1" 被姓名 fallback **误判为第三个乘客** "TIANTIAN/P"
→ 解析出 3 个乘客（1个名字不完整 + 1个假乘客），实际只有 2 个

**修复方案**
预处理阶段加"SSR 续行合并"：
- 条件1：SSR DOC 行**没有**以 /Pn（或儿童/婴儿标记）结尾
- 条件2：下一行是短的纯大写片段（≤30字符，可带 /Pn 结尾，不是另一条SSR/航段/价格）
- 两个条件都满足 → 拼回一行

**端到端测试（用户输入）通过**
- ✓ 折断的 SSR 正确合并
- ✓ 正好 2 个乘客（假乘客 TIANTIAN/P 消失）
- ✓ KRAFT/CHRISTINE TIANTIAN 名字完整（DOB 04JUL08，到期 07MAY36）
- ✓ WANG/YANLIALICE 正常（DOB 15MAY72，到期 23MAY27）

**防误伤测试通过（5项）**
- ✓ 完整SSR + 下一条SSR → 不合并
- ✓ 完整SSR + TOTAL价格 → 不合并
- ✓ 缺Pn的SSR + 价格行 → 不误合并（含数字不匹配续行模式）
- ✓ 缺Pn的SSR + 航段行 → 不误合并
- ✓ 缺Pn的SSR + 中文行 → 不误合并

**改动位置**
- index.html 9449 前：新增 SSR 续行合并预处理（在护照卡合并之前）

**风险或注意事项**
- ⚠ 续行判定要求纯大写字母片段 ≤30 字符，更长或含数字的行不会被当续行（保守策略）
- ⚠ 顺便注意：KRAFT/CHRISTINE TIANTIAN 出生 2008 年，是未成年人
- ⚠ 不影响完整的（未折行的）SSR 解析

**回滚方式**
从 .backups/ 找上一版覆盖。回滚后折行 SSR 又会产生假乘客。

---
